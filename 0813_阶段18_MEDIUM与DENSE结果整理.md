# 8月13日：阶段18 MEDIUM与DENSE结果整理

## 一、结论摘要

截至2026年8月13日20:26（Asia/Shanghai），Stage 18四组MEDIUM十种子实验与DENSE完整模型十种子实验均已完成。早先中断的两个seed 02已从检查点恢复，其余种子按独立种子协议完成。

已完成结果显示：

1. MEDIUM完整模型10种子能够稳定运行，无非法动作和资源泄漏；相对规则基线平均提高成功率约0.59个百分点，但吞吐量轻微下降。
2. MEDIUM去除队列/资源信息在当前最大并发2的条件下未造成稳定显著退化。
3. MEDIUM去除显式交接提示后，使用best checkpoint比较时与完整模型基本相同；最终权重的反向差异主要来自末期策略漂移。
4. MEDIUM去除持续位置的首个完整种子发生明确模式坍缩：完全不选择单狗，将应由单狗完成的290个跨层任务全部改为车－狗－车。
5. DENSE完整模型首个完整种子明显优于规则基线：回报、成功率、吞吐量和仿真完成时间均改善，并保持三种运输方式的条件切换。

这些数据均来自checkpoint-selection validation seeds，不是锁定test-set结果，不能直接作为T-RO最终显著性结论。

## 二、实验完成度

| 负载 | 条件 | 计划种子 | 完整种子 | 当前状态 |
|---|---|---:|---:|---|
| MEDIUM | `full_context_v2` | 10 | 10 | 已完成，十种子继续门通过 |
| MEDIUM | `no_queue_resource` | 10 | 10 | 已完成 |
| MEDIUM | `no_handover_cues` | 10 | 10 | 已完成 |
| MEDIUM | `no_persistent_position` | 10 | 10 | 已完成；稳定出现信息消融退化 |
| DENSE | `full_context_v2` | 10 | 10 | 已完成；平均回报差+19.6926 |
| DENSE | 三项上下文消融 | 各10 | 0 | 尚未开始 |

## 三、MEDIUM完整模型十种子

相对规则基线的十种子validation汇总：

- 平均回报差：+0.1468，种子间标准差0.4122。
- 平均成功率差：+0.59个百分点，种子间标准差0.407个百分点。
- 平均吞吐量差：-0.1067 task/h。
- 8/10个种子的回报差为正。
- 10/10训练完成，非法动作和资源泄漏均为0。
- 三种运输模式在所有种子中均被实际选择。

该结果说明完整模型的安全性与条件模式切换稳定，但平均性能提升较小且存在跨种子波动。

## 四、MEDIUM首两项消融

### 4.1 去除队列/资源状态

最终权重十种子配对结果：

- 完整模型－消融回报差：+0.0774，95% bootstrap区间[-0.2113, 0.3519]。
- 成功率差：+0.195个百分点，区间跨0。
- 吞吐量差：-0.0425 task/h，区间跨0。

当前MEDIUM、最大同时活跃任务数2的环境中，队列/资源状态的优势不明显。其价值应在DENSE、BURST或更高并发条件中复核。

### 4.2 去除交接次数与预测超时概率

使用每个种子的best checkpoint复核：

- 完整模型－消融回报差：+0.0154，95%区间[-0.0325, 0.0715]。
- 成功率差：+0.055个百分点，区间跨0。
- 吞吐量差：+0.0006 task/h，区间跨0。
- 模式分布接近，没有模式坍缩。

因此，在MEDIUM下两个显式交接提示可能与运输模式、估计代价等其他特征存在信息冗余。

## 五、MEDIUM去除持续位置：首个完整种子

统一使用best checkpoint在100个validation episodes、2000个任务上复核：

| 指标 | 去除持续位置 | 规则基线 | 差值 |
|---|---:|---:|---:|
| 原始episode平均回报 | 9.2567 | 17.9740 | -8.7173 |
| 成功率 | 95.15% | 96.45% | -1.30个百分点 |
| 吞吐量（task/h） | 55.7074 | 56.1661 | -0.4587 |
| 平均仿真时间 | 1292.4670 | 1281.9127 | +10.5543 |
| 正值相对评分 | 61.57 | 100 | -38.43 |

运输模式：

- 单车：1000次。
- 车－狗－车：1000次。
- 单狗：0次。

在成本参考为单狗的290个任务上，策略全部改选车－狗－车，单狗参考模式的对角一致率为0。非法动作和资源泄漏均为0。这表明持续位置状态并非只改善数值性能，而可能是识别“单狗直送还是需要两端车辆交接”的关键上下文。

十种子最终权重汇总为：PPO平均回报8.0707、规则基线17.9740，平均差-9.9033；PPO成功率95.15%、规则基线96.45%；PPO吞吐55.6493 task/h、规则基线56.1661 task/h。退化在十种子层面稳定存在。

## 六、DENSE完整模型：十种子

使用best checkpoint在相同100个validation seeds、2000个任务上复核：

| 指标 | DENSE PPO | DENSE规则基线 | 差值 |
|---|---:|---:|---:|
| 原始episode平均回报 | -13.5527 | -33.2558 | +19.7031 |
| 成功率 | 96.80% | 96.45% | +0.35个百分点 |
| 吞吐量（task/h） | 124.4276 | 123.5542 | +0.8734 |
| 平均仿真时间 | 578.6496 | 582.7401 | -4.0905 |
| 正值相对评分 | 180.84 | 100 | +80.84 |

运输模式：单车1000次、单狗359次、车－狗－车641次。三种模式全部使用；按当前成本参考模式的一致率为96.55%，非法动作和资源泄漏均为0。

DENSE原始回报为负是高密度等待成本造成的，不代表策略失败。与同一DENSE规则基线比较，策略在首个种子上有明显优势。正值相对评分仅用于报告，不参与PPO优化、checkpoint选择或验收。

十种子最终权重汇总为：PPO平均回报-13.5632、规则基线-33.2558，平均差+19.6926；PPO成功率96.695%、规则基线96.45%；PPO吞吐124.8876 task/h、规则基线123.5542 task/h。十个种子均完成且安全门通过。

## 七、正值相对评分定义

为避免MEDIUM、DENSE、BURST原始回报符号和尺度不同，报告层使用：

```text
Score = 100 * exp((R_policy - R_rule) / max(abs(R_rule), 10))
```

- 规则基线恒为100。
- 高于100表示优于规则，低于100表示弱于规则。
- 无论BURST原始回报有多负，评分始终为正。
- 原始回报和回报差必须同时报告，不能只展示该评分。
- 该变换不修改训练奖励、奖励归一化、策略排序或已有权重。

## 八、数据质量与中断审计

- 两个状态文件曾保留在 `RUNNING`，但系统中没有对应训练进程。
- MEDIUM seed 02最新完整断点：update 1050。
- DENSE seed 02最新完整断点：update 1300。
- 两个seed 02均无最终summary，不能纳入正式汇总。
- 状态现已修正为 `INTERRUPTED_WITH_RESUMABLE_CHECKPOINT`。
- 恢复时应从断点继续，而不是重新训练seed 02，以保留已用计算并维持同一种子轨迹。

## 九、下一步顺序

1. 为批处理增加自动从最新checkpoint恢复的能力。
2. 恢复MEDIUM `no_persistent_position` seed 02并完成至seed 10。
3. 恢复DENSE完整模型seed 02并完成至seed 10。
4. 对两组10个best checkpoints做配对/跨种子统计。
5. DENSE完整模型通过十种子门后，再启动DENSE三项消融。
6. 全部模型和best checkpoints锁定后，才一次性运行锁定test seeds和BURST等压力场景。

## 十、当前证据评级

- MEDIUM完整模型与前两项消融：可用于内部汇报，最终论文结论仍需锁定test集。
- MEDIUM去除持续位置：十种子均完成，可作为位置上下文重要性的消融证据。
- DENSE完整模型：十种子均完成且相对规则基线稳定改善，可进入DENSE消融。
- 总体评级：`Share with caveats`。

## 十一、论文绘图与统计的数据限制（2026-08-13补充）

1. Stage 18所有最终指标目前使用共享的checkpoint-selection validation seeds（42000000起），尚未运行锁定test seeds。现阶段可用于模型选择、消融方向判断和内部绘图，不能作为T-RO最终无偏性能结论。
2. MEDIUM `no_persistent_position` seed 02从update 1050恢复，现有history只包含恢复后的950个update；DENSE `full_context_v2` seed 02从update 1300恢复，history只包含恢复后的700个update。两者最终权重、最终100-episode评估和安全统计完整，但不能直接纳入从update 1开始的十种子训练曲线。训练曲线应使用其余9个完整种子，或明确标记缺失前段。
3. 两个旧检查点在恢复前未保存NumPy、PyTorch和PPO minibatch生成器状态，因此首次恢复属于种子序列与优化器状态连续的统计等价续训，不是逐位复现；恢复后生成的新检查点已保存随机状态。
4. Stage 15的MEDIUM/DENSE/BURST压力结果使用较早的Stage 14 checkpoint；Stage 18多种子结果使用新版Context V2独立权重。两者不能作为同一模型的负载曲线直接连线，必须在图注中区分，或使用Stage 18锁定权重重新评估三种负载。
5. DENSE三项上下文消融尚未完成；在其完成前，不能据MEDIUM消融推断队列/资源和交接提示在高负载下无效。
6. BURST的Stage 18完整模型与三项上下文消融尚未完成；当前BURST数据只能作为旧checkpoint压力证据。
7. 当前上下文消融只覆盖`no_queue_resource`、`no_handover_cues`、`no_persistent_position`。`fixed_gamma`和`flat_masked_ppo`属于算法消融，仍需单独安排，不能用上下文消融替代。
8. 所有回报图必须同时给出原始回报、规则基线和PPO－规则差值。正值相对评分仅为跨负载报告辅助，不能替代原始回报、显著性检验或用于训练。
9. 最终论文统计仍需：十独立训练种子的均值/标准差、配对bootstrap 95%置信区间、配对显著性检验、效应量，以及锁定test seeds上的一次性复核。
