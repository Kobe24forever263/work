# 双层仓储 MDP/SMDP 设计修正版

> 修订日期：2026-08-07。本文替代此前默认“每次任务均执行车—狗—车交接”的描述。

## 1. 任务分类与合法运输模式

任务自身只描述货物起点、目标点、楼层、区域、优先级和截止时间。任务不会预先固定为
“单狗任务”或“交接任务”；高层策略应根据当前机器人位置、异构速度、资源状态和ETA，
在该任务的合法运输模式之间选择。

| 任务类型 | `SINGLE_CAR` | `SINGLE_DOG` | `CAR_DOG_CAR` | 说明 |
|---|---:|---:|---:|---|
| 一楼同区域 | 合法 | 合法 | 屏蔽 | 比较高速小车与附近Go2直送 |
| 一楼跨区域 | 合法 | 合法 | 屏蔽 | Carter允许跨共享航道；不进行车车交接 |
| 二楼同区域 | 合法 | 合法 | 屏蔽 | 仅当前在二楼的Go2可成为单狗候选 |
| 二楼跨区域 | 合法 | 合法 | 屏蔽 | 不存在二楼车车交接 |
| 一楼到二楼 | 屏蔽 | 合法 | 合法 | Carter禁止走楼梯 |
| 二楼到一楼 | 屏蔽 | 合法 | 合法 | Go2可单独携货下楼或与两层车辆协作 |

系统不存在 `CAR_CAR_HANDOVER`。同层任务只允许单车或单狗直接完成，跨层任务允许
单狗直接完成或车—狗—车协作。

## 2. 异构速度

| 机器人 | 名义速度 | 用途 |
|---|---:|---|
| Carter | 2.0 m/s | 车辆ETA、关键路径时长、动作成本与执行配置 |
| Go2 | 0.8 m/s | 同层/跨层单狗ETA、接近楼梯和楼梯通行ETA |

累计里程仍按各机器人实际距离分别累加；任务持续时间按并行协作的关键路径计算。
候选动作比较的是时间成本，而不是裸距离，防止因忽略速度差导致策略系统性偏向单狗。

## 3. MDP/SMDP总表

| 要素 | 修正版定义 | 关键边界 |
|---|---|---|
| 状态 `S` | 固定284维：全局8维、任务8×13维、机器人10×14维、楼梯4×8维 | 全局状态增加执行模式与在途任务比例；机器人状态显式包含名义速度 |
| 动作 `A` | 最多1536个合法候选，每个28维 | 第一层是运输模式，随后选择任务、执行机器人、楼梯和任务后策略；候选直接绑定任务几何/时限/相对代价 |
| 转移 `P` | 按所选运输模式调用相应任务状态机 | 不再把交接视为所有任务必经步骤 |
| 基础奖励 `R` | 未完成任务时间积分 | 当前优先优化完成速度和队列积压；阶段13再增加交接/资源/失败权重 |
| 折扣 | 建议时间感知 `γ_k=γ_0^(Δt/τ)` | `delta_time`已记录，参数在阶段13确定 |
| `task_reset` | 清理本任务货物、临时状态与资源锁 | 保留机器人位置、楼层、里程、负载和泊位 |
| `episode_reset` | 达到任务数/时间上限或不可恢复故障时整体重置 | 训练种子和配置保留用于复现 |

## 4. 三种状态转移

### 4.1 `SINGLE_CAR`

适用于所有同层任务：选择同层可用 Carter → 前往起点 → 装货 → 直接前往目标点 →
卸货 → 前往空闲泊位。过程中不存在 Go2、楼梯和交接事务。

### 4.2 `SINGLE_DOG`

- 同层：选择同层可用 Go2 → 前往起点 → 装货 → 直接前往目标点 → 卸货 → 泊位；
- 跨层：选择源楼层 Go2 和楼梯 → 前往起点装货 → 前往楼梯入口 → 通过楼梯并确认
  楼层切换 → 前往目标点卸货 → 目标楼层泊位。

单狗模式没有 Carter，也没有货物所有权交接，但跨层时仍必须预约和释放楼梯。

### 4.3 `CAR_DOG_CAR`

仅适用于跨层任务：源楼层 Carter 取货，Go2并行接近楼梯发送区，目标楼层 Carter
并行接近接收区 → Carter向Go2交接 → Go2通过已预约楼梯 → Go2向目标楼层 Carter
交接 → Carter送达目标平台 → 三台机器人分别进入空闲泊位。

## 5. 动作结构

组合动作定义为：

`transport_mode × task × pickup_carter_option × dog_option × stair_option × receiving_carter_option × post_task_policy`

| 模式 | 必填执行者 | 必须为空的字段 |
|---|---|---|
| `SINGLE_CAR` | 一个Carter，同时作为取货和送货机器人 | Go2、楼梯、接货车交接字段 |
| `SINGLE_DOG` | 一个Go2；跨层时还需一条楼梯 | 取货Carter、接货Carter、交接点 |
| `CAR_DOG_CAR` | 源层Carter、Go2、楼梯、目标层Carter | 无 |

## 6. 动作掩码

动作掩码除原有忙碌、故障、带货、错误楼层和资源占用检查外，还必须检查：

- 同层任务禁止 `CAR_DOG_CAR`；
- 跨层任务禁止 `SINGLE_CAR`；
- `SINGLE_DOG`不能同时填入任何Carter；
- `SINGLE_CAR`不能填入Go2或楼梯；
- `CAR_DOG_CAR`必须具有两层Carter、源层Go2和空闲楼梯；
- 所有 `CAR_CAR_HANDOVER` 输入一律拒绝；
- 无合法候选时仅允许受控 `WAIT`。

## 7. 基础奖励与模式公平性

当前基础奖励仍为：

`r_k = -(1/C) × ∫ N_outstanding(t) dt`

由于动作持续时间已经使用2.0m/s和0.8m/s计算，慢速Go2长距离直送会自然产生更大的
时间成本；但当Go2恰好靠近起点和目标、且两次交接代价更高时，单狗仍可能成为合理
选择。阶段13应在此基础上增加少量可解释项：交接次数/风险、机器人占用时间、超时、
失败与恢复、负载均衡。不能使用固定的“单狗惩罚”，否则会人为破坏模式选择。

## 8. 当前验证结果

- 核心测试：20/20通过；
- 修正版Medium：20/20通过；
- 正式六类任务模式分布：14次单车、6次车—狗—车；单狗作为合法候选保留，规则
  基线没有在该固定序列中误判为更优；早期探索序列曾出现1次几何上更优的单狗选择；
- 六类任务回归平均等待2.20秒，P95等待13.13秒，队列峰值1；
- 固定观测284维，Context V2动作张量`1536 × 28`；
- 车—车交接不存在且被动作掩码拒绝。

## 9. 阶段13正式验收（2026-08-09）

基础回报已经实现为可解释分量之和：未完成任务时间积分、任务完成、截止时间、交接
开销、参与机器人占用时间和距离项。距离系数暂为0，以避免在已有时间成本之外重复
惩罚；没有固定的单狗惩罚或车—狗—车奖励。

连续时间折扣采用 `γ(Δt)=0.99^(Δt/10s)`。任务数达到上限时以
`terminated=true, reason=TASK_LIMIT`结束；仿真时间达到上限时以
`truncated=true, reason=TIME_LIMIT`结束，两者互斥。

严重不可恢复故障增加统一`-10.0`失败分量，并在episode退出前执行任务级清理。
`CARGO_UNRECOVERABLE`、`NO_FEASIBLE_CHAIN`和`GLOBAL_DEADLOCK`均属于正常
`terminated`边界；任务截止时间超时只产生`-5.0`截止时间分量，不自动结束episode。

核心测试25/25通过；30个随机种子的Medium六类任务完成600/600，模式分布为单车
420次、车—狗—车172次、单狗8次。各episode平均回报范围2.039222～2.123345，
时间感知折扣范围0.914572189～0.989937334。截止时间超时、仿真时间上限和三类严重
故障边界5/5通过，所有故障退出均无残留货物、任务字段和资源锁。阶段13正式验收并
冻结，阶段14开始MAPPO课程训练。

## 10. 阶段14第一轮课程与训练接口（2026-08-09）

训练episode继续保持20任务和机器人状态不逐任务复位，但任务构成改为同层10个、
跨层10个。跨层课程中设置7个协作优势几何和3个单狗优势几何；两种模式始终同时
进入合法动作集合，由策略根据状态、候选动作特征和回报自行选择，不在任务标签中
预设运输模式。

Gymnasium接口初版输出282维全局状态；串行/并发双模式接入后扩为284维，继续输出
Context V2现输出`1536 × 28`候选动作特征和1536维动作掩码。
10个随机种子的规则基线通过该接口处理200个任务；接入P95交接超时后成功196、受控
失败4，模式分布为单车100、车—狗—车70、单狗30，固定种子重放一致。该结果只表示
训练接口和课程第一课通过；策略网络、
MAPPO训练、同序列基线比较和泛化评估仍属于阶段14后续工作。

## 11. 阶段14候选动作网络热启动（2026-08-09）

策略网络采用共享候选打分结构：全局状态编码与每个28维候选特征显式交互后逐候选输出
logit，再应用动作掩码；价值头只读取全局状态。网络没有固定动作编号参数，能够处理
同一动作槽在不同决策时代表不同任务、机器人和楼梯的情况。

400个规则决策标签完成监督热启动后，5个未见种子共100任务全部进入明确结果，其中
成功96、P95交接超时失败4，非法动作为0，三种模式均被使用，模式选择与规则一致率
100%，完整联合动作一致率79%。该权重仅是
后续强化学习的安全初始化，不作为RL优于规则的证据。阶段14下一门禁是接入SMDP
变时折扣和优势估计的PPO/MAPPO训练，并以相同任务序列对照规则基线。

## 12. 阶段14 P95交接超时门禁（2026-08-09）

现有阶段8记录缺少起止时间戳，不能作为真实P95证据。当前使用车→狗和狗→车各500条
可复现逻辑仿真样本，得到统一nearest-rank P95 `2.697870903389276 s`。严格大于
该阈值的交接先回滚唯一所有者，再令当前任务`FAILED`且不重试；任务级清理不重置
机器人位置，也不终止整个episode。

500个episode共处理10000任务，成功9643、交接超时失败357；6866次交接尝试中
357次超时，单次超时率5.20%，3500个双交接协作任务失败率10.20%。全部任务有明确
结果且无资源泄漏。该阈值只用于逻辑训练，后续必须以每类至少500条RViz/实机计时
替换。正式RL不人为注入错误动作，非零失败来自交接耗时分布及P95边界。

## 13. 串行/并发双模式与训练分组（2026-08-09）

环境现在支持两种任务执行模式。`SERIAL`保持原有语义：任一时刻最多一个主运输任务；
`CONCURRENT`允许最多4个无资源冲突的任务同时在途。并发任务在完成事件前锁定参与
机器人、楼梯、交接点、区域与计划待命泊位；位置和可用状态只在对应完成事件到达时提交。
两种模式都保留任务间连续机器人状态，并使用同一运输模式动作集合，不存在车—车交接。

观测由282维扩为284维：全局新增当前执行模式标志和在途任务比例。SERIAL、CONCURRENT
和MIXED入口用于环境门禁和可选消融；正式部署训练固定采用CONCURRENT。每个任务的
动作候选仍共同包含合法的单车、单狗、车—狗—车方案，策略在一次联合动作中选择运输
方案、执行者、楼梯和待命点，绝不为三种运输方案分别训练权重。

100个配对种子的规则基线门禁共400 episodes/8000任务通过。Medium负载下并发吞吐
提升0.49%、平均等待降低62.92%；Dense负载下并发吞吐提升44.67%、平均等待降低
75.33%。四组成功率均为96.90%，失败仍只来自P95交接超时，无状态或资源泄漏。
这些数字用于冻结环境与建立对照，不属于PPO/MAPPO结果。正式学习效果只评价一套
CONCURRENT策略与同环境规则基线；SERIAL/MIXED若后续运行，只能列为附加消融。

## 14. SMDP-PPO短时门禁与长训练边界（2026-08-09）

PPO优势估计现直接使用每个调度事件自带的`gamma(delta_time)`，并在episode结束时切断
bootstrap。SERIAL、CONCURRENT和MIXED曾各完成4个训练episode的接口门禁，三组PPO
参数均发生更新，指标有限，80个留出任务均进入明确结果，非法动作和资源泄漏均为0。
这只证明通用入口实现正确，不代表正式训练需要三套权重，也不代表策略收敛。

正式长训练不由自动agent启动。用户只手动训练一套`CONCURRENT + MEDIUM`权重；同一
策略同时选择三种运输方案，并用独立留出种子与并发规则基线对比。权重、断点和测试
命令见`阶段14_SMDP_PPO训练与测试说明.md`。

## 15. Reward V2与运行回报归一化（2026-08-09）

首轮Reward V1长训在600任务留出集中产生跨层全单狗塌缩，final仅使用7次车—狗—车。
这不是动作空间缺失，而是失败惩罚相对时间效率过强。Reward V2不引入模式奖金，只修改
共同目标尺度：`outstanding_time_scale=30.0`、`timeout_penalty=2.0`、
`active_robot_time_penalty=0.0002`。4000任务方向门禁证明Reward V2下高效混合运输的
平均回报17.0695，高于全单狗避险的15.6158；旧奖励的排序与此相反。

PPO训练奖励按运行的可变折扣回报标准差缩放，不减均值并裁剪至±10。该统计属于模型
状态，随所有权重和检查点保存；旧权重不得用于Reward V2续训。留出评估继续报告原始
episode总回报，保证不同策略可在固定任务流上解释和比较。新正式手动训练为2000个
update、每update 4个episode，进度条实时显示，每20个update输出、评估和保存一次。

## 16. Context V2按环境选择方案（2026-08-09）

首轮策略出现“同层单车、跨层近乎全单狗”，说明仅有模式合法性和Reward V2仍不足以证明
策略按环境决策。旧候选只用任务槽序号关联284维全局状态，状态嵌入又与候选嵌入简单
相加，容易形成全局模式先验。Context V2将候选由16维扩为28维，新增当前任务优先级、
等待时间、剩余时限、起终点坐标、相对最低合法候选的代价差/比值和参与机器人数量；
actor改为显式拼接状态嵌入、候选嵌入及逐元素交互。

不设置任何模式频率目标或模式奖励。评估新增“最低代价参考模式→实际选择模式”条件矩阵
和“同层/跨层→实际选择模式”矩阵。正式门禁要求同一权重在三类参考环境中均能产生对应
选择，三类对角率各至少20%，只用于排除无脑固定模式，不要求复制规则到100%。400任务
CONCURRENT规则预热留出100任务中，模式一致率100%、完整联合动作一致率94%；随后10个
episode的Reward V2 PPO短门禁保持单车50、车—狗—车35、单狗15，条件矩阵三类对角率
均为100%，非法动作和资源泄漏为0。该短结果仅证明接口方向正确，正式2000-update长训
仍由用户手动启动。
