# Stage 25：严格在线因果观测修复与长训练入口

日期：2026-08-31  
状态：短门禁通过；正式长训练尚未启动，等待用户手动运行

## 1. 本阶段解决的问题

Stage 23 的 `MARKOV_CONTEXT_V3` 会把一个 episode 中尚未发布的任务明细加入策略状态，包括未来任务的起终点、优先级、截止时间和发布时间；并且会暴露正在执行任务的精确完成时间、已采样交接时长和最终机器人位置。这一口径只适用于“全部订单预先公布”的场景，不能用于严格在线任务发布器。

Stage 25 新增 `CAUSAL_RELEASED_V4`：

- 策略只看到已经发布的等待任务和正在执行任务；
- 不显示未发布任务的数量、内容、顺序或未来负载阶段；
- 不显示正在执行任务尚未实现的随机交接结果、精确完成时刻或最终位置；
- 保留机器人当前位置、占用状态、当前资源预约、已发布任务、动作候选和安全掩码；
- 状态宽度仍为 1720，动作特征仍为 1536×30，继续复用原有候选动作策略网络；
- 正式表述改为“因果观测下的部分可观测 SMDP”，不再把严格在线输入称为完全 Markov 状态。

Stage 23 的既有结果不删除，但只解释为“全订单预先公布”场景的证据。

## 2. 已完成验证

### 2.1 因果反事实门禁

结果：`results/stage25_causal_online/gates/stage25_causal_interface_gate.json`

- 将 79 个未来任务改成 40 个，并改写任务方向、优先级、截止时间和发布时间；
- 新状态最大变化：0；
- 新动作特征最大变化：0；
- 新动作掩码变化数量：0；
- 同一修改在旧 V3 状态中的最大变化：6.7476；
- 修改已发布任务后，新状态最大变化：0.6667，证明编码器没有冻结当前信息；
- 修改未实现的主动任务完成时刻和最终位置，新状态变化：0，旧 V3 变化：1.4000；
- 完整 80 任务回合全部有结局，零非法动作，零资源泄漏。

### 2.2 TASK_KEYED 短训练

结果：`results/stage25_causal_online/smoke_v2_task_keyed/stage25_causal_smoke.summary.json`

- 2 updates、4 episodes，仅用于接口烟雾验证；
- PPO 参数发生更新；
- 损失、熵、梯度等训练指标有限；
- 变时长折扣、奖励归一化和候选动作掩码正常；
- 评估任务全部有结局，零非法动作，零资源泄漏；
- 该权重没有收敛，不用于论文性能结论。

### 2.3 全新随机种子验证

结果：`results/stage25_causal_online/validation/stage25_causal_smoke_v2_task_keyed_fresh_validation.json`

- 8 个未参与训练的随机种子，共 640 个任务；
- 短训练策略完成 640/640，规则基线完成 618/640；
- 策略零非法动作、零资源泄漏；
- 策略平均回报仍比规则基线低 121.142，且模式选择尚未达到正式的上下文切换标准；
- 结论：接口安全且可训练，但短权重不能证明性能优越。

### 2.4 回归与冻结完整性

- 核心回归测试：55/55 通过；
- Stage 23 冻结文件的 SHA-256 与冻结清单一致：
  - `stage12_encoding.py`
  - `stage14_training.py`
  - `stage14_ppo.py`
  - `run_stage14_smdp_ppo.py`
- Stage 25 全部通过新增模块和运行时注入实现，没有改写上述冻结源。

## 3. 推荐的长训练顺序

不直接启动 10 个种子。先跑 seed 01 哨兵：

- 2000 updates；
- 每个 update 4 episodes；
- 总计 8000 episodes；
- 每个 episode 80 个任务，约 64 万个任务决策；
- `CONCURRENT + MIXED_CURRICULUM + TASK_KEYED`；
- 三种运输模式同时开放；
- Reward V3、变时长折扣、duration-scaled GAE；
- 每 20 updates 打印一次进度；
- 每 100 updates 保存检查点并验证。

seed 01 完成后，在 40 个新随机种子上执行哨兵质量门禁。只有同时满足以下条件，seed 02–10 才允许启动：

1. 零非法动作；
2. 零资源泄漏；
3. 所有任务有结局；
4. 所有训练指标有限；
5. 三种运输模式均在合法候选中出现；
6. 策略按任务/资源上下文切换运输模式；
7. 新种子平均回报不低于规则基线。

如果第 7 项或上下文切换门禁失败，下一步不是继续堆积前馈 PPO 种子，而是测试带记忆的循环策略，以适应严格在线场景的部分可观测性。

## 4. 用户手动运行命令

进入工作区：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
```

先打印计划，不启动训练：

```bash
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
scripts/run_stage25_causal_long.py 1
```

确认后手动启动 seed 01 长训练：

```bash
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
scripts/run_stage25_causal_long.py 1 --execute --confirm-long
```

中断后从最新检查点恢复：

```bash
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
scripts/run_stage25_causal_long.py 1 --resume-latest --execute --confirm-long
```

训练完成后执行哨兵质量门禁：

```bash
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
scripts/run_stage25_sentinel_gate.py
```

## 5. 输出位置

- 最终权重：`results/stage25_causal_online/long/seed_01/stage25_causal_ppo.pt`
- 最优权重：`results/stage25_causal_online/long/seed_01/stage25_causal_ppo.best.pt`
- 训练历史：`results/stage25_causal_online/long/seed_01/stage25_causal_ppo.history.json`
- 训练总结：`results/stage25_causal_online/long/seed_01/stage25_causal_ppo.summary.json`
- 周期检查点：`results/stage25_causal_online/long/seed_01/checkpoints/`
- 新种子验证：`results/stage25_causal_online/long/seed_01/sentinel_fresh_validation.json`
- 扩展门禁：`results/stage25_causal_online/long/seed_01/sentinel_quality_gate.json`

冻结协议：`src/warehouse_bringup/config/experiment_stage25_causal_online.yaml`

