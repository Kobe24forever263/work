# 0809 交接文档

## 阶段 11：强化学习前置准备与 MDP/SMDP 调度环境

阶段 11 接下来应按“**异步任务到达、单主任务执行、事件驱动高层调度、机器人状态持续保留**”的方式落地，并先将调度问题完整定义成事件驱动的 MDP。由于一次车—狗—车组合动作的持续时间并不固定，从数学上看该问题更接近半马尔可夫决策过程（SMDP），但工程接口仍可按照 Gymnasium 的 `reset()` / `step()` 形式实现。首先需要修改任务生成器，使任务按照固定随机种子和可配置的到达间隔持续生成；任务到达后立即进入等待队列，即使当前已有任务正在运输也不得丢弃或覆盖。阶段 11 暂时只允许一个主运输任务进入实际执行状态，其余任务继续在队列中等待并累计等待时间。调度决策采用事件驱动方式，只在系统空闲且队列非空、当前任务完成、任务受控失败，或者可恢复故障需要重新分配等事件发生时触发，而不是按固定短时间步让 10 台机器人分别选择动作。任务异步到达与任务并发执行需要明确区分：本阶段允许任务异步产生和排队，但实际运输暂时串行执行，待基础环境稳定后再扩展到多个任务并发执行。

将调度环境定义为 \(M=(S,A,P,R,\gamma)\)。状态 \(S\) 使用固定顺序和固定维度编码，由任务、机器人、资源及全局状态四部分组成。任务部分第一版最多向策略提供 \(K=8\) 个等待任务，不足部分补零并附带 `task_mask`；每个任务应包含任务 ID、取货点 ID、送达点 ID、起点楼层、终点楼层、是否跨层、优先级、截止时间、已等待时长、当前任务阶段，以及候选机器人到取货点或交接点的距离和预计到达时间（ETA）。实际等待队列超过 \(K\) 时不得删除任务，应由规则层从完整队列中筛选优先级最高的 \(K\) 个任务进入策略观测。机器人部分按照固定 ID 顺序编码 10 台机器人，每台机器人包含类型、当前楼层、位置或区域、是否可用、是否故障、导航状态、当前载货 ID、当前任务 ID、到当前目标的 ETA、累计路程、累计任务数、连续空闲时间、近期任务负载、上一任务结果及上一任务结束位置。资源部分包括四条楼梯的空闲、占用、排队和持有者状态，发送与接收交接点的占用状态，区域锁、货物所有权及当前执行任务阶段。全局部分包括当前仿真时间、等待任务数、全部未完成任务数、已完成任务数、机器人平均负载、最大与最小负载差以及上一任务结果。坐标、距离、ETA、等待时间和累计路程等连续量需要统一单位并归一化，机器人、任务和资源 ID 的排列顺序必须保持稳定。

动作 \(A\) 不采用“每台机器人分别从多个动作中独立选择”所形成的超大联合动作空间，而是由集中式高层策略一次输出一个完整且合法的协作组合。动作可编码为 `task_index × pickup_carter × dog_option × stair × pickup_handover_point × receive_handover_point × receiving_carter × post_task_policy`。其中 `dog_option` 必须包含“不使用 Go2”，同层任务只能选择该项；`post_task_policy` 第一版可设置为原地等待、返回所属待命点和前往预测热点三个离散选项。所有动作在策略采样之前必须经过 `action_mask`，屏蔽 Carter 上楼、忙碌或故障机器人接单、取货 Carter 与起点楼层不一致、接货 Carter 与目标楼层不一致、Go2 与楼梯侧不匹配、楼梯或交接点被占用、资源存在冲突，以及同层任务错误调用 Go2 等非法组合。当系统暂时没有可执行的合法组合时，可保留受规则约束的 `WAIT` 动作。高层策略只负责选择任务和协作组合，并下发 `ExecuteSkill`；具体导航仍由 SCAN-Planner 根据机器人当前真实位置规划执行，高层策略不得直接输出速度、关节角或步态控制。

状态转移 \(P\) 不需要另行训练动力学模型，而应直接由现有任务状态机、货物管理器、交接管理器、楼梯与区域资源管理器、失败检测与恢复模块以及 SCAN-Planner 执行链共同产生。策略给出组合动作后，环境开始执行完整运输链；执行期间任务生成器继续异步接收新任务并加入队列，直到当前任务完成、受控失败，或出现需要重新分配的恢复事件，环境才返回下一状态。因此每次 `step()` 除常规返回值外还必须记录并提供 `delta_time`，以表达不同组合动作的实际持续时间。

第一版奖励 \(R\) 使用累计未完成任务时间成本，在第 \(k\) 个决策区间计算

\[
r_k=-\frac{\Delta t_k\,N_{\text{outstanding},k}}{C},
\]

其中，\(N_{\text{outstanding},k}\) 同时统计正在执行和在队列中等待的全部任务，\(C\) 根据单个 episode 的预期总时长或任务规模确定，用于将回报归一化到稳定数量级。阶段 11 暂时不混入大量人工奖励项，以免多个权重相互干扰，但必须同步记录任务完成时间、空驶距离、交接等待时间、楼梯等待时间、失败与恢复次数、吞吐量和负载方差，待阶段 13 再决定是否把这些指标加入正式奖励。折扣因子建议采用时间感知形式

\[
\gamma_k=\gamma_0^{\Delta t_k/\tau},
\]

其中可先取 \(\tau=10\) 秒作为一个折扣单位，避免持续 20 秒和 100 秒的动作获得相同折扣。如果第一版实现时间感知折扣存在困难，可以暂时固定使用 \(\gamma=0.99\)，但日志和经验数据中必须保留 `delta_time`，以便后续切换为 SMDP 折扣。

重置边界必须分为 `task_reset` 和 `episode_reset`。`task_reset` 仅允许删除已经完成或失败的任务、对应货物、临时任务状态及本任务持有的楼梯、交接点和区域资源锁，不得重置机器人的位置、楼层、累计路程、累计任务数、近期负载、连续空闲时间和上一任务结束位置。一个 episode 建议包含 20～50 个连续任务，只有达到任务数量上限、总时长上限或发生严重且不可恢复的故障时，才能执行 `episode_reset` 并整体复位系统。

具体实现顺序为：先完成任务等待队列、异步到达机制和可复现的随机任务序列；再建立统一的 `get_mdp_state()`、`build_action_mask()`、`decode_action()` 和 `execute_joint_assignment()` 接口；随后让现有规则调度器作为第一个策略运行在同一套 MDP/SMDP 环境中。规则策略第一版可先按照优先级、等待时长和截止时间选择任务，再依据预计完成时间、空驶距离和近期负载从合法组合中选择协作链路。每个决策点必须保存完整的 `(state, action, action_mask, reward, delta_time, next_state, terminated, truncated)` 数据，并同时保存随机种子、资源占用、货物所有权、机器人结束位置和失败恢复事件，保证相同随机种子与相同初始状态能够重放相同的任务到达序列。

阶段 11 的验收标准是：机器人不整体复位的情况下连续运行不少于 30 分钟，或连续完成至少 20 个任务，推荐完成 30～50 个任务；测试必须覆盖一楼同层、二楼同层、一楼到二楼和二楼到一楼四类任务；运行期间不得出现货物复制、丢失或残留，不得出现楼梯、交接点和区域资源永久锁死，不得出现任务长期卡在中间状态，所有任务最终必须进入明确的 `COMPLETED` 或受控 `FAILED` 状态；任务之间应保留真实机器人位置和累计状态，规则调度结果也应随任务顺序和机器人分布变化，而不能继续使用固定的车—狗—车链路。完成这些工作后，阶段 11 将形成一套规则策略和后续 MAPPO 策略都能直接接入的统一 MDP/SMDP 调度环境；阶段 12 只需进一步完成观测张量化、组合动作网络输出和动作掩码验证，阶段 13 再正式确定奖励权重、终止条件和训练重置规则。

## 2026-08-09 实施进展

阶段 11 第一个纯逻辑里程碑已完成：持久机器人状态、无损异步任务队列、固定
`K=8` 策略视图、动态 Carter/Go2/楼梯组合候选，以及事件驱动 `step()` 已建立。
5 个连续任务实测为 `5/5 PASS`；机器人位置和累计状态未复位，`dog_2` 上楼后的
二楼状态被后续下楼任务正确继承，全部任务结束后无货物或任务残留。核心回归
测试为 `13/13 PASS`。详细证据见
`results/stage11/stage11_persistent_smoke_5_report.md`。

下一执行项为固定种子的 20 个连续任务自动门禁，进一步验证四类任务覆盖、任务
优先级/截止时间/等待时长排序、动态分配多样性、动作掩码、跨任务状态连续性和
逐决策 SMDP 数据完整性。

### 20任务自动门禁结果

固定种子 `20260805` 已完成同种子双跑复验，正式结果为 `20/20 PASS`。一楼
同层、二楼同层、上楼和下楼各5次；实际使用14种不同的机器人/Go2/楼梯组合，
40个非法动作探针全部被掩码屏蔽，10台机器人均产生持续状态变化，任务结束后
队列、货物和任务字段全部清理。逐决策SMDP数据完整，同种子任务和分配序列完全
一致。详细证据见 `results/stage11/stage11_persistent_fixed_20_report.md`。

本轮2–6秒任务到达间隔快于串行服务速度，平均等待时间为 `412.14 s`，应作为
过载排队基线记录，不应误判为死锁。下一执行项改为RViz 3–5任务连续人工验收；
通过后再增加正常到达率序列并运行30–50任务稳定性测试。

### 三种任务发布负载对比

任务发布器已增加三档可复现负载，并使用相同的20任务内容完成对比：

| 负载 | 到达间隔 | 平均等待 | P95等待 | 队列峰值 | 结果 |
|---|---:|---:|---:|---:|---:|
| normal | 50–70秒 | 1.44秒 | 3.35秒 | 1 | 20/20 |
| medium | 30–50秒 | 8.34秒 | 23.35秒 | 1 | 20/20 |
| overload | 2–6秒 | 387.45秒 | 643.46秒 | 17 | 20/20 |

三档均通过状态连续性、货物清理、动作掩码、四类任务覆盖和同种子重放检查。
`normal` 作为阶段11默认运行和RViz演示负载，`medium` 用于调度策略比较，
`overload` 保留为后期压力与泛化基线。完整结果见
`results/stage11/stage11_load_profiles_comparison_report.md`。

### 送达后的待命泊位策略

阶段11已开始将 `HOLD` 原地驻留替换为 `NEAREST_STANDBY`。货物送达后先结束
配送并解除货物与任务绑定，参与本任务的 Carter 和 Go2 随后驶离装卸、交接及
楼梯作业点，进入同楼层、同类型的最近空闲离散泊位。泊位采用唯一 ID，机器人
接到下一任务时立即释放原泊位；任务结束后的重新停放会重新选择空闲泊位，因此
不同机器人不得占用同一泊位或拥有相同最终坐标。

这一版先验证确定性的逻辑占用，不把具体泊位选择交给强化学习。20任务门禁新增
两项检查：所有执行过任务的机器人都持有待命泊位，以及全部机器人最终坐标唯一。
旧版 `stage11_persistent_medium_20` 结果保留作为 `HOLD` 基线，新结果使用
`stage11_persistent_medium_20_standby` 文件名，避免覆盖前后方案的对比证据。

下一门禁是 SCAN-Planner/RViz 末端可达性：逐一验证 Carter 与 Go2 从主要作业
点进入和离开每个泊位，并验证相邻泊位已有模型占用时仍可抵达空闲泊位。该门禁
通过前，当前泊位坐标只视为逻辑候选坐标，不能作为物理导航最终验收结果。

### 阶段12方向：可学习的楼梯权衡

货物方块显示、平台初始放置、Carter—Go2—Carter 瞬时所有权切换与卸货解绑已
通过人工验收，后续不再把货物物理显示作为当前主线。阶段12不得把楼梯与任务象限
固定绑定。同一个跨层任务必须同时保留多个合法楼梯候选，策略输入对每个候选分别
提供取货侧成本、Go2到楼梯及上下楼成本、二楼接货侧成本和接货后到目标平台成本。
例如一楼取货点靠近东北楼梯、二楼目标靠近西南楼梯时，东北与西南楼梯都必须保留
为可选动作，由策略结合机器人当前位置、空驶距离、楼梯占用和后续二楼运输距离做
权衡。SCAN-Planner继续只负责执行策略选出的具体路线，不替高层策略固定楼梯。

### 阶段11正式封板（2026-08-09）

阶段11遗留的三个逻辑门禁已经补齐并通过。动作掩码现在会统一拒绝忙碌、故障、
已有任务或仍携带货物的 Carter/Go2；每次任务会真实登记区域、交接点和楼梯资源的
占用与释放事件，跨层期间楼梯状态进入 `OCCUPIED`，任务结束或异常退出时统一释放；
SMDP 回报改为按照异步任务的实际到达时刻分段积分未完成任务时间，并把决策间隔
纳入 `delta_time`，不再把整个执行区间近似为固定队列长度。

核心自动测试结果为 `17/17 PASS`。随后使用 `medium` 到达负载、固定种子
`20260805` 完成正式 20 任务双跑复验，结果为 `20/20 PASS`、失败 0 次，四类任务
各 5 次，使用 14 种动态协作组合，40 个非法动作探针全部被拒绝。全程产生 120 条
资源占用/释放事件，数量严格配对；结束时全部楼梯为 `FREE`、资源持有表为空、货物
和任务字段清空、机器人最终泊位互不重叠，同种子重放结果一致。

正式机器可读证据为：
`results/stage11/stage11_persistent_medium_20_final_gate.summary.json` 与
`results/stage11/stage11_persistent_medium_20_final_gate.jsonl`。据此，阶段11的
纯逻辑 SMDP/规则调度环境正式验收通过并冻结。阶段12继续完成固定维度观测、组合
动作编码、多楼梯候选权衡和策略接口；不在阶段12重新修改已验收的货物显示逻辑。

### 阶段12第一轮门禁（2026-08-09）

固定维度观测与组合动作接口已接通。该处初版272维/`1024 × 13`规格已于
2026-08-09先被282维/`1536 × 16`规格替代，Context V2再升级为284维/
`1536 × 28`；任务槽同时编码起点和目标点，
跨层任务保留全部 4 条楼梯候选；
编码动作可以无歧义还原为原始 `Assignment`，没有合法候选时输出唯一受控 `WAIT`。
核心自动测试 `19/19 PASS`，medium 固定种子回归 `20/20 PASS`。详细结果见
`results/stage12/stage12_encoding_medium_20.summary.json` 和对应 JSONL。

### Medium等待时间修复（2026-08-09）

旧模型把多机器人并行移动错误地按总路程串行计时，且30～50秒的medium到达间隔
快于约50秒的修复后服务时间。现已改为协作关键路径计时，多机器人进入交接区域和
任务后进入不同泊位均按并行过程计算；累计里程仍保持逐机器人相加。medium重新
标定为55～75秒，normal为80～100秒，overload仍为2～6秒。最终medium 20任务
平均等待5.64秒、P95等待20.13秒、最大等待32.61秒、队列峰值1，20/20通过。
详细对比见 `results/stage11/stage11_medium_wait_optimization_report.md`。

### 阶段12正式验收（2026-08-09）

修正版异常动作掩码矩阵19/19类通过；观测在串行/并发双模式接入后由282维扩为284维，
Context V2动作张量已升级为`1536 × 28`，在修复后的
medium 20任务中保持稳定，每个跨层任务保留4条楼梯候选，动作可逆解码，阶段11
清理回归通过。归一化规格见 `results/stage12/stage12_normalization_spec.md`，训练
样本见 `results/stage12/stage12_encoding_medium_20.samples.npz`。阶段12正式验收
通过，下一步进入策略网络、奖励权重和训练实验设计。

### 运输模式与异构速度修正（2026-08-09）

任务门禁现覆盖一楼同区域、一楼跨区域、二楼同区域、二楼跨区域、上行和下行六类。
任务本身不预先固定执行链，策略动作先选择运输模式：同层为单车或单狗直送，跨层为
单狗直送或车—狗—车；车—车交接不存在并已从候选空间删除。Carter名义速度固定
2.0m/s，Go2固定0.8m/s，状态显式携带速度，候选代价与任务时长均按ETA计算。
六类任务20轮回归20/20通过，规则基线选择14次单车和6次车—狗—车，没有因少用
机器人或少交接而错误偏向单狗。详细定义见 `0807_MDP_SMDP设计修正版.md`。

### 阶段13正式验收（2026-08-09）

奖励接口已经从单一标量扩展为可审计的分量：未完成任务时间、完成奖励、截止时间、
交接开销、机器人占用时间和距离项。当前不设置固定的单狗惩罚或协作奖励；折扣改为
`0.99^(delta_time / 10s)`，正常任务数上限使用 `terminated/TASK_LIMIT`，仿真时间
上限使用 `truncated/TIME_LIMIT`。

核心自动测试25/25通过。最终使用30个随机种子完成Medium六类任务600/600回归：
单车420次、车—狗—车172次、单狗8次；三种合法模式均由持续状态和几何代价自然
选出。单任务平均回报为2.078105，各episode平均回报范围为2.039222～2.123345，
时间感知折扣范围为0.914572189～0.989937334。

截止时间超时、仿真时间上限、货物不可恢复、无可行执行链和全局死锁5类受控边界
全部通过。三类严重故障在终止前均清除了货物持有者、机器人任务字段和所有资源锁，
CLAIM/RELEASE严格配对。正式证据见
`results/stage13/stage13_final_medium_600.summary.json`、对应JSONL和
`results/stage13/stage13_final_medium_600_report.md`。

据此阶段13正式验收并冻结。下一步进入阶段14 MAPPO课程训练：先接通标准训练封装和
规则基线复现，再从短序列无故障课程逐步过渡到20任务Medium持久状态和失败样本。

### 阶段14第一轮训练接口门禁（2026-08-09）

Gymnasium事件驱动训练接口已经接通；初版使用282维状态，串行/并发双模式接入后扩为
284维，并在Context V2中把候选升级为`1536 × 28`
候选动作特征显式提供给策略，动作掩码保持1536维。没有待执行任务时只开放受控
`WAIT`；策略动作仍只选择高层联合分配，不允许输出速度、关节或步态控制量。

训练任务分布调整为每20个任务中同层10个、跨层10个。跨层任务再分为7个协作优势
场景和3个单狗优势场景，不直接指定运输模式。10个随机种子的规则基线复现共处理
200个任务；接入P95交接超时后为成功196、受控失败4，失败原因全部为
`HANDOVER_TIMEOUT`。最终模式分布仍为单车100、车—狗—车70、单狗30；所有选择
都在动作掩码内，相同种子完整复放一致，34/34核心测试通过。证据见
`results/stage14/stage14_balanced_rule_200.summary.json`、对应JSONL和报告。

该门禁只证明课程数据和训练接口正确，不代表强化学习策略已经训练；候选网络验证和
P95失败门禁见下文，阶段14整体仍未最终验收。

### 阶段14候选动作网络热启动（2026-08-09）

共享候选动作打分网络已经实现。初版网络编码282维状态；双模式接入后网络输入同步扩为
284维，Context V2编码每个候选的28维特征，按
候选输出logit后应用1536维动作掩码；模型没有绑定固定动作编号。当前34/34核心测试通过，
包括候选位置交换时得分随特征交换的检查。

使用20个训练种子的400个规则决策完成监督热启动，标签分布为单车200、车—狗—车
140、单狗60。接入P95交接超时后重新热启动，训练损失由4.208009降至1.010623，
动作分类准确率由20.25%提升至74.00%。在5个未见种子的100任务评估中成功96、
交接超时失败4，全部任务均进入明确结果；非法动作0次，模式一致率100%，完整联合
动作一致率79%，模式分布为单车50、车—狗—车35、单狗15。规则基线在同组种子上
同样为成功96、超时失败4，平均仿真时间相差约0.013%。

本轮是规则蒸馏热启动，只证明网络和数据管线可用，不属于PPO/MAPPO强化学习结果。
正式证据见`results/stage14/stage14_candidate_policy_smoke.summary.json`、训练历史、
留出决策JSONL、模型权重和对应报告。下一步接入阶段13的SMDP变时折扣与优势估计，
运行至少3～5个训练种子的PPO/MAPPO课程。

### P95交接超时与任务失败门禁（2026-08-09）

阶段8的300条交接记录只包含提交、拒绝和回滚结果，没有统一开始/结束时间戳，不能
据此计算真实交接P95。当前先使用1000条可复现逻辑仿真标定样本，其中车→狗500条、
狗→车500条；样本明确标记为`logic_simulation_provisional_v1`，只允许用于强化学习
前的逻辑环境，不得宣称为RViz或实机性能。

统一计时从“两台智能体在同一交接点开始稳定对准”起，到“货物唯一所有者完成切换并
验证”止。nearest-rank P95为`2.697870903389276 s`；仅当
`duration_s > P95`时判定`HANDOVER_TIMEOUT`，等于阈值仍成功。超时事务先回滚到
唯一安全所有者，当前任务随后进入`FAILED`且不重试，货物、机器人任务字段及楼梯、
交接点、区域资源全部清理。普通交接超时只结束当前任务，不提前终止整个episode。

500个持久状态episode共处理10000个任务：成功9643、交接超时失败357；3500个
车—狗—车任务产生6866次实际交接尝试，其中357次超时，单次超时率5.20%，协作任务
失败率10.20%。后者高于5%是正常的，因为一个车—狗—车任务有两次交接，独立暴露下
理论失败率为`1-0.95²=9.75%`。全部10000个任务进入明确结果，固定种子可重放，
无货物、机器人、楼梯或资源锁泄漏。

首个交接即超时时，Go2不会复位；它在任务失败清货后无货通过已预约楼梯，进入对面
楼层待命，从而保持持续状态和上下楼运力，避免整层无Go2的假死锁。正式证据见
`results/stage14/stage14_handover_timeout_gate.summary.json`、标定JSONL、任务JSONL、
交接事件JSONL和对应报告。后续每种交接至少收集500条RViz/实机时间戳样本后，用相同
P95算法替换暂定阈值；真实分布漂移时，不强行把失败率固定在5%。

下一步正式进入SMDP PPO/MAPPO训练。策略不注入人为错误动作；非零失败率来自上述
交接耗时分布和统一超时边界。训练验收不得要求100%成功，而应同时比较规则基线与
学习策略的任务成功率、单次交接超时率、双交接任务失败率、长期等待、总耗时、空驶
距离、模式分布和资源安全。

### 阶段14串行/并发双任务模式门禁（2026-08-09）

保留原有`SERIAL`单主任务模式，并新增`CONCURRENT`多任务模式。并发模式最多允许4个
互不冲突的任务同时在途；机器人、楼梯、交接点、区域和待命泊位在任务完成前持续占用，
因此同一机器人或同一关键资源不能被两个任务重复分配。机器人位置仍从上一任务结束状态
继续，不做逐任务复位。训练接口另提供`MIXED`课程，按episode在串行和并发之间交替，
且284维观测显式包含当前模式位和在途任务比例，避免把两种不同转移规律混为隐变量。

规则基线使用100个固定种子，分别在Medium和Dense任务到达负载下对照，共400个
episode、8000个任务。四组均成功率96.90%，失败全部为已定义的P95交接超时；未出现
机器人、货物、楼梯、待命泊位或资源锁泄漏。Medium下并发峰值2个在途任务，吞吐仅
提升0.49%，但平均等待由5.261秒降至1.951秒，P95等待由25.717秒降至14.758秒。
Dense下并发峰值达到4，吞吐由84.424升至122.133任务/小时（+44.67%），平均等待
由308.698秒降至76.145秒（-75.33%），平均流转由348.138秒降至116.164秒。

本门禁仍是规则策略的环境验证，不是强化学习成绩。SERIAL与MIXED保留为环境门禁及
可选消融；真实系统的正式学习策略固定使用CONCURRENT调度，一个权重同时面对单车、
单狗和车—狗—车候选，并选择具体机器人、楼梯与待命点。三种运输方案不能拆成三套
权重。规则对照必须使用相同任务种子、到达时间、初始机器人状态、交接耗时样本和奖励
权重，并报告成功率、吞吐、平均/P95等待、平均/P95流转、里程、交接超时、非法动作和
资源冲突。正式证据见
`results/stage14/stage14_execution_mode_comparison.summary.json`、任务JSONL和对应报告。

### 阶段14 SMDP-PPO短时训练门禁（2026-08-09）

掩码SMDP-PPO已经接通。优势估计使用每个事件动作自身的`delta_time`折扣，episode边界
禁止跨episode bootstrap；策略使用284维模式条件状态、`1536 × 28`动态候选特征
和1536维动作掩码。训练程序支持best/final权重、周期检查点、优化器状态恢复和独立留出
评估。

遵照“长时间训练由用户手动执行”的约束，本轮曾为SERIAL、CONCURRENT和MIXED各运行
4个训练episode作为接口门禁。三组均完成80个留出任务，非法动作0、资源泄漏0，PPO
参数发生实际更新，损失和变时折扣均为有限值；该门禁不表示需要训练三套正式策略。
正式CONCURRENT课程在同一动作空间暴露单车、单狗、车—狗—车三种合法运输方案。

正式长训练没有启动。手动训练命令、断点恢复、best/final权重位置和600任务留出测试方法
见`阶段14_SMDP_PPO训练与测试说明.md`。下一步由用户只手动训练一套
`CONCURRENT + MEDIUM`正式权重，完成后与并发规则调度器统一评估，再进入RViz验证。

### Reward V2归一化与防模式塌缩修正（2026-08-09）

首轮1000-episode权重未验收：600任务独立留出中final选择293次单狗、仅7次车—狗—车，
以规避P95交接失败换取99.83%成功率，但吞吐低于规则基线。历史显示约第50个update后
车—狗—车持续减少，因此不是单纯训练轮数不足，而是Reward V1目标偏向风险规避。

Reward V2不添加任何运输模式专属奖金。只将未完成任务时间尺度从100秒收紧至30秒，
交接超时/任务失败惩罚由5.0校准至2.0，机器人占用时间系数由0.001降至0.0002，使
更快完成任务的收益能够与交接风险竞争。50个固定种子、4000任务方向门禁中，旧奖励
偏好“同层单车、跨层全单狗”的避险策略（38.8446 > 32.9295）；Reward V2关系反转，
高效混合运输平均回报17.0695，高于全单狗15.6158，且吞吐56.644高于56.336任务/小时。

PPO现使用可保存的运行SMDP折扣回报标准差归一化训练奖励，不减均值，裁剪到±10；
归一化统计随best/final/检查点保存并在断点训练时恢复。报告和留出评估仍展示原始20任务
episode回报，训练进度同时标记`raw_episode_reward`和`normalized_step_reward`。
Reward V1权重缺少归一化状态，禁止续训。Context V2修正后43项核心测试和10-episode
PPO短门禁通过。

新正式训练由用户手动运行2000个update：每update 4个episode，约47万次高层SMDP
决策。终端使用进度条，每20个update才打印指标、执行评估并保存检查点。正式权重使用
新目录`results/stage14/ppo/long/realistic_concurrent_medium_reward_v2_context_v2/`，不会
覆盖首轮权重。

### Context V2按任务环境选方案修正（2026-08-09）

正式目标不是学习一种固定运输模式，也不是根据历史平均回报对以后任务无脑选择单狗。
每次下发任务都要结合起终点、楼层关系、期限、机器人当前位置和占用、楼梯可用性以及
各合法方案的相对时间代价重新打分。候选动作特征已由16维扩展至28维，直接绑定任务
优先级、等待、剩余时限、起终点坐标、相对最低合法候选的代价差/比值和参与者数量；
策略网络改为状态嵌入、候选嵌入和两者逐元素交互的显式融合，避免形成全局模式先验。

新验收不规定单车/单狗/车—狗—车的固定次数。评估同时输出“同层/跨层→所选模式”和
“当前最低代价合法模式→所选模式”两张条件矩阵。正式权重必须在三类参考环境中都能
切换，对角选择率各至少20%；这是防塌缩底线，不要求策略100%复制规则。400任务上下文
预热在100任务留出集中选择单车50、车—狗—车35、单狗15，模式一致率100%、完整联合
动作一致率94%。继续10个episode的PPO接口门禁后仍保持同一条件分布，三类对角率100%，
非法动作0、资源泄漏0。上述比例是该固定留出环境的自然结果，不是训练配额。

### 阶段15三方压力泛化门禁（2026-08-09）

Stage14 Context V2 best权重已在预注册的MEDIUM、DENSE、CROSS_HEAVY和BURST四种
场景中完成三方配对评估。每个场景100个episode、每个episode 20任务；PPO、原时间
贪心规则和新增风险感知规则使用完全相同的任务序列、初态与按任务ID绑定的交接耗时样本，
共解析24000个任务结果。风险规则不知道实际抽中的交接样本，只按单次5%、双交接链
9.75%的公开P95风险和Reward V2系数修正候选ETA，因此比原时间规则更强且仍为因果基线。

PPO在DENSE中相对风险规则平均回报提高15.171，95%置信区间为[12.420,17.799]，平均
等待减少18.112秒，P95等待减少59.453秒；BURST中回报提高28.452，置信区间为
[25.077,32.278]，平均等待减少33.949秒，P95等待减少74.869秒。两种高拥堵场景中
平均/P95流转和每任务总里程也显著降低，但吞吐差的置信区间跨0，不宣称吞吐显著更好。

MEDIUM和CROSS_HEAVY保留了重要反例：PPO相对风险规则的回报差置信区间均跨0，规则的
等待、流转和吞吐更好；PPO则成功率更高、总里程更低。这说明当前策略不是全指标统治，
而是在低交接风险、Go2速度0.8m/s和Carter速度2.0m/s之间形成偏保守的权衡。PPO在拥堵
时会减少同时占用三台机器人的车—狗—车链，这是风险规则只做单候选修正时没有学到的
队列/资源长期效应。

四场景三种策略全部任务均进入明确结果，非法动作0、资源泄漏0，任务指纹严格配对。
压力泛化和强基线门禁通过，但阶段15整体尚未结束。下一项是策略消融：保持同一批固定
任务与随机样本，分别屏蔽候选的队列/资源上下文、交接风险上下文和持久位置上下文，验证
DENSE/BURST优势是否确由上下文决策产生。该消融若需要重新训练权重，必须由用户手动
启动长训练；Agent只负责准备命令、检查点位置和自动评估脚本。

正式证据：`results/stage15/stage15_three_way_100ep.json`及同名Markdown报告。原始
PPO—时间规则两方结果`stage15_stress_100ep.*`继续保留，不覆盖历史证据。

### 阶段15连续混合负载门禁（2026-08-09）

新增单episode连续负载：20个NORMAL任务后直接进入20个DENSE任务，再进入20个BURST
任务。三段共用同一个CONCURRENT环境，机器人位置、所在楼层、累计里程、任务数、队列、
在途任务、楼梯和资源锁均不重置。使用100个固定种子分别运行PPO、时间规则和风险规则，
每种策略6000任务，共18000任务结果；任务指纹和按任务ID绑定的交接样本完全一致。

PPO跨层车—狗—车比例在NORMAL、DENSE、BURST中分别为47.60%、10.80%、5.70%，正式
证明同一权重会随队列压力改变运输模式。风险规则为59.40%、27.60%、18.10%，时间规则
为70.00%、69.60%、68.00%。相对风险规则，PPO成功率98.95%对98.23%，回报提高79.559，
平均等待减少36.890秒、P95等待减少119.158秒、平均流转减少35.730秒、每任务总里程
减少5.239米，配对95%置信区间均支持这些方向。

原“已解决吞吐”会把受控失败也计为已解决任务，因此新增成功任务吞吐口径。PPO与风险
规则成功吞吐分别为74.450和74.589任务/小时，配对差-0.140，95%置信区间
[-0.467,+0.173]跨0，不宣称吞吐存在显著差异。BURST绝对平均等待仍为327.163秒、P95
为620.570秒，说明策略缓解但没有消除1～3秒到达间隔造成的过载。

连续混合负载自适应验收通过。核心测试47/47通过；全部任务明确结束，非法动作0、资源
泄漏0。正式证据为`results/stage15/stage15_mixed_continuous_100ep.json`及同名Markdown，
复现入口为`scripts/run_stage15_mixed_continuous_evaluation.command`。下一项为策略消融，
随后增加BURST→NORMAL恢复段，验证负载下降后能否从单狗重新切回高效率协作链。

### SERIAL单任务策略训练管线（2026-08-09）

新增SERIAL对照策略：每个episode仍有20个异步任务，当前任务执行期间新任务继续排队，
但同一时间最多一个任务在途；机器人位置与累计状态跨任务保留。网络、Reward V2、速度、
任务种子、Context V2预热和PPO超参数与CONCURRENT正式策略保持一致，唯一主动变量是
最大并发数从4改为1。SERIAL不拆分运输模式，仍由一个权重选择单车、单狗和车—狗—车。

10 episode短门禁通过：参数实际更新，5个留出episode共100任务、成功97、交接超时3，
三种模式50/35/15；非法动作0、资源泄漏0、最大在途任务1。短权重只用于接口证据，不能
用于正式性能结论。2000-update长训练必须由用户手动运行
`scripts/run_stage14_realistic_serial_ppo_long.command`。正式best权重目标位置为
`results/stage14/ppo/long/realistic_serial_medium_reward_v2_context_v2/`。完整命令、断点
恢复和三方验收见`阶段15_SERIAL_PPO训练与验收说明.md`。

### SERIAL正式训练、三方验收与并发消融（2026-08-09）

用户已完成SERIAL正式2000-update训练，共8000 episode、160000任务。训练门禁通过，best
检查点为update 940。100个独立MEDIUM episode的三方配对评估中，每个策略2000任务：
PPO成功率97.15%，时间规则96.25%，风险规则96.90%；PPO相对风险规则成功吞吐增加0.153
任务/小时、每任务总里程减少2.310米，但平均流转增加0.166秒。因此验收结论限定为
“可靠性和里程略优、时延近似持平”，不宣称全面统治。

新增SERIAL与CONCURRENT独立best权重的同任务流消融。MEDIUM、DENSE、BURST各100个
episode、每套策略每场景2000任务，任务与交接随机样本严格配对，全部任务有明确结果，
非法动作和资源泄漏均为0。CONCURRENT相对SERIAL的平均等待分别减少3.482、84.114、
171.034秒，成功吞吐分别增加0.589、38.135、61.859任务/小时；负载越高并发收益越明显。
正式部署方向继续采用CONCURRENT，SERIAL保留为系统降级策略和并发能力消融基线。

正式证据：

- `results/stage15/stage15_serial_medium_three_way_100ep.json`及同名报告；
- `results/stage15/stage15_serial_vs_concurrent_100ep.json`及同名报告；
- 一键复现：`scripts/run_stage15_serial_concurrent_comparison.command`。

#### SERIAL回报对比补充

评估报告中的回报是每个20任务episode的原始累计回报，不是PPO优化时使用的标准差归一化
step reward，因此不会被限制在`[-1,1]`。DENSE和BURST中的负回报主要来自等待、未完成
任务和资源占用惩罚，不表示任务普遍失败；对应场景成功率仍约为97%～98%。

SERIAL MEDIUM三方评估中，PPO、时间规则和风险规则的平均episode回报分别为15.994、
15.444和15.825。PPO相对时间规则提高0.551，配对95%置信区间[0.237,0.891]，可认定
回报显著更高；相对风险规则提高0.170，置信区间[-0.018,0.396]跨0，只能记为小幅数值
优势，不能宣称显著更好。

同任务流SERIAL与CONCURRENT配对消融结果如下，差值均为CONCURRENT减SERIAL：

| 场景 | SERIAL回报 | CONCURRENT回报 | 回报提升 | 配对95%置信区间 |
|---|---:|---:|---:|---:|
| MEDIUM | 16.215 | 18.741 | +2.526 | [2.170,2.931] |
| DENSE | -83.462 | -17.830 | +65.632 | [63.308,67.961] |
| BURST | -218.935 | -86.668 | +132.268 | [126.750,137.498] |

三种负载下置信区间均不跨0，且并发回报优势随负载上升而扩大。SERIAL继续定位为单任务
降级策略；CONCURRENT继续作为正式多任务运行方向。完整解释见
`阶段15_SERIAL回报对比.md`。

下一阶段进入CONCURRENT策略上下文消融：分别屏蔽队列/资源、交接风险、持久位置三类候选
上下文，在同一DENSE/BURST任务流上复训并配对评估，以确认当前优势由哪些状态信息产生。
凡涉及2000-update长训练仍由用户手动启动；Agent只准备训练配置、短门禁、权重位置与自动
评估脚本。
