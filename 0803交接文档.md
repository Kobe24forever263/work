# 0803 项目交接文档

日期：2026-08-03  
工作区：`/Users/lab4099/Desktop/Mujoco/work`  
当前阶段：阶段 6——Carter 固定路线可靠性正式门禁（已完成）

## 最新交接状态（执行前先读）

- 四条“装货—一楼车—Go2 上楼—二楼车—卸货”任务已经完成路线验证。
- 二楼流程已经拆成两个独立规划阶段：车辆先到楼梯接收点，与 Go2 停留 `4 秒`，之后才去卸货平台。
- 东北路线已完成人工 RViz 验收；西北、西南、东南已完成无界面自动验证。
- 自动验证期间发现并修复了“到站失败后脚本仍打印通过”的问题；当前任一阶段失败都会使整条任务立即失败。
- 2026-08-03 曾启动一次 600 次测试，但用户随即手动停止；该次中断运行不计入正式结果。
- 正式 600 次现已完成：`600/600 PASS`，详见 `results/stage6/stage6_cargo_fixed_600_report.md`。

## 2026-08-03 路线范围更新

原先验收通过的是“停车位—楼梯发送/接收区”的导航基线，尚未经过货物平台。现已把固定运输主线扩展为：

1. 一楼 Carter 从停车位出发，先到对应象限的装货平台侧边停靠点；
2. 一楼 Carter 沿外圈抵达已验收的楼梯交接线；
3. Go2 按对应楼梯上至二楼；
4. 二楼 Carter 到达楼梯接收点，与 Go2 停留 4 秒完成交接；
5. 二楼 Carter 交接完成后，最后抵达对应卸货平台侧边停靠点。

四条固定任务为东北、西北、西南、东南，当前仍不加入随机初始扰动。此前 30/30 结果仅作为旧路线基线，不等同于新货运路线的 600 次正式验收。

### 新路线验收标准

- 一楼车的轨迹明确经过对应装货平台侧边，不穿入平台实体；
- 一楼车继续抵达原有楼梯交接线；
- Go2 完整上楼并在二楼停稳；
- 二楼车先经过对应楼梯接收点，再抵达对应卸货平台侧边；
- RViz 中车、狗、点云和完整轨迹同时可见，模型与 TF 不报红；
- 四个方向分别验收通过后，先做全路线短批量测试，再恢复长时间测试。

## 1. 当前已完成内容

### 1.1 场景与机器人

- 双层仓库、四条实体楼梯、二楼中央镂空、护栏、柱子和装卸平台已建立。
- 4 台 Go2 与 6 台 Carter 已接入独立 namespace、TF 和完整 URDF。
- Go2 与 Carter 统一使用 SCAN-Planner。
- 当前开发与阶段验收以 RViz 为主，Gazebo 动力学和性能优化移至阶段 16。

### 1.2 四条跨楼层协作链

以下任务均已实际运行；东北完成人工 RViz 验收，其余方向完成自动验证：

| 楼梯 | 协作链 | 状态 |
|---|---|---|
| 东北 | `car_f1_1 → dog_1 → car_f2_2` | 通过 |
| 西北 | `car_f1_2 → dog_2 → car_f2_1` | 自动验证通过 |
| 西南 | `car_f1_3 → dog_3 → car_f2_1` | 修复门禁后重跑通过 |
| 东南 | `car_f1_4 → dog_4 → car_f2_2` | 自动验证通过 |

### 1.3 Carter 固定路线预检

- 6 车冒烟测试：`6/6 PASS`。
- 每车 5 次稳定性预检：`30/30 PASS`。
- 初始位置扰动全部为 `[0.0, 0.0]`。
- 30 次测试最大终点误差：`0.061 m`。
- 到站容差：`0.35 m`。
- 30 次测试无超时、无规划器异常退出、无残留进程干扰。

预检证据：

- `results/stage6/stage6_fixed_smoke.jsonl`
- `results/stage6/stage6_fixed_smoke.summary.json`
- `results/stage6/stage6_fixed_30.jsonl`
- `results/stage6/stage6_fixed_30.summary.json`

## 2. 下一步任务

执行阶段 6 正式门禁：6 辆 Carter 每辆固定路线测试 100 次，总计 600 次。

本轮目标是验证已验收路线在重复启动、点云加载、规划、到站和进程清理方面的
长期稳定性。当前不测试随机起点、随机目标或动态障碍。

正式测试分配如下：

| 机器人 | 楼层 | 路线分配 | 次数 |
|---|---:|---|---:|
| `car_f1_1` | 1 | 东北发送路线 | 100 |
| `car_f1_2` | 1 | 西北发送路线 | 100 |
| `car_f1_3` | 1 | 西南发送路线 | 100 |
| `car_f1_4` | 1 | 东南发送路线 | 100 |
| `car_f2_1` | 2 | 西北、 西南接收路线各 50 次 | 100 |
| `car_f2_2` | 2 | 东北、东南接收路线各 50 次 | 100 |

总数：`600` 次。

## 3. 实现方式

### 3.1 使用的文件

- 路线与测试策略清单：
  `src/warehouse_bringup/config/scan_planner/stage6_carter_manifest.yaml`
- 单车 SCAN 启动器：
  `src/warehouse_bringup/launch/single_carter_scan_validation.launch.py`
- 正式批测程序：
  `scripts/run_stage6_batch.py`
- 到站判定程序：
  `scripts/wait_for_scan_pose.py`
- 路线静态检查：
  `scripts/validate_stage6_routes.py`

### 3.2 固定路线原则

- `initial_jitter_xy` 必须保持为 `0.0`。
- 不修改已通过的起点、航点和终点。
- 不在本轮加入随机扰动。
- 不启动 Gazebo，也不打开 RViz，以减少 Apple M4 的图形负载。
- 每次只启动一辆车的一套 SCAN-Planner；到站或失败后结束该套进程，再开始下一次。
- 使用 Cyclone DDS 和独立 ROS Domain，避免 Fast DDS 共享内存锁或旧 TF 污染。

## 4. 执行步骤

### 4.1 打开终端

打开 macOS“终端”，执行：

```bash
cd /Users/lab4099/Desktop/Mujoco/task2_handover
```

### 4.2 运行静态检查

```bash
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  /Users/lab4099/Desktop/Mujoco/work/scripts/validate_stage6_routes.py
```

预期输出：

```text
Stage 6 route validation: PASS (6 robots, 8 route variants)
```

如果这里失败，不要开始 600 次测试，应先修复清单或路线文件。

### 4.3 检查 600 次计划

```bash
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  /Users/lab4099/Desktop/Mujoco/work/scripts/run_stage6_batch.py \
  --schedule-only
```

预期第一行包含：

```text
Stage 6 schedule: 600 trials
```

同时确认4辆一楼车各100次，两辆二楼车各100次；二楼车的两个方向各50次。

### 4.4 启动正式测试

推荐直接运行已经整理好的入口脚本：

```bash
cd /Users/lab4099/Desktop/Mujoco/work/scripts
chmod +x run_stage6_cargo_600.command
./run_stage6_cargo_600.command
```

该脚本会自动完成路线静态检查、600 次任务分配检查和正式执行。如果发现同名的中断结果，会先自动改名保存，避免覆盖证据。

入口脚本内部沿用此前已经跑通的环境顺序：先执行 pixi shell-hook，再加载
`SCAN-Planner-Ros2/install/setup.zsh`、`work/install/setup.zsh`，最后补充
`work/install/warehouse_bringup` 到 `AMENT_PREFIX_PATH`。因此不需要手动猜测或拼接 ROS 2 环境变量。

本机已经在 macOS 系统设置中关闭自动休眠，因此不再使用 `caffeinate`。测试期间
终端窗口仍须保持打开，不要注销用户、重启或关机。

### 4.5 预计耗时

根据30次预检数据：

- 一楼长路线平均约 `71 s/次`。
- 二楼短路线平均约 `37 s/次`。
- 600 次预计约 `10 小时`，加上加载和清理开销可能更长。

建议在机器可以连续运行10至12小时的时间段执行。测试期间不要同时打开 Gazebo、
大型 RViz 场景或其他高负载程序。

## 5. 如何查看进度

终端会逐次输出：

```text
[1/600] car_f1_4 se: PASS
[2/600] car_f2_2 se: PASS
...
```

另开一个终端可查看当前已完成数量：

```bash
wc -l /Users/lab4099/Desktop/Mujoco/work/results/stage6/stage6_cargo_fixed_600.jsonl
```

查看最近5次结果：

```bash
tail -n 5 /Users/lab4099/Desktop/Mujoco/work/results/stage6/stage6_cargo_fixed_600.jsonl
```

逐次详细日志位于：

```text
/Users/lab4099/Desktop/Mujoco/work/results/stage6/stage6_cargo_fixed_600_logs/
```

ROS 自身日志位于：

```text
/private/tmp/warehouse_stage6_cargo_600_logs/
```

## 6. 安全停止与注意事项

### 6.1 正常停止

如需停止，在运行测试的终端按一次：

```text
Control+C
```

等待当前 Python 进程返回终端提示符。不要直接强制关闭 Mac 或终端应用。

### 6.2 中断后的结果

- 已完成的试验已经写入 `stage6_cargo_fixed_600.jsonl`。
- 只有全部600次完成后才会生成最终 `.summary.json`。
- 当前批测入口重新执行时会覆盖同名 JSONL，因此中断后重新运行前，应先把旧文件
  改名保存，避免丢失部分证据。
- 中断的部分结果不能和新的完整运行混合冒充一次连续600次正式门禁。

### 6.3 不要修改的内容

正式运行期间不要修改：

- PCD 点云文件
- Carter 路线 YAML
- `stage6_carter_manifest.yaml`
- SCAN 调参文件
- 到站容差
- ROS Domain

如果中途修改任何上述内容，本次测试必须作废并重新开始，以保证600次使用完全
相同的软件与配置。

## 7. 输出文件

正常完成后应得到：

```text
results/stage6/stage6_cargo_fixed_600.jsonl
results/stage6/stage6_cargo_fixed_600.summary.json
results/stage6/stage6_cargo_fixed_600_logs/
```

JSONL 每行代表一次测试，主要字段包括：

- `trial`
- `robot`
- `route`
- `floor`
- `goal_xyz`
- `success`
- `failure_code`
- `duration_s`
- `final_error_m`
- `initial_jitter_xy`
- `log`

本轮所有 `initial_jitter_xy` 都应为：

```json
[0.0, 0.0]
```

## 8. 验收标准

### 8.1 必须满足

- 总试验数必须等于 `600`。
- 6 辆车每辆必须完成 `100` 次。
- `car_f2_1` 西北/西南各 `50` 次。
- `car_f2_2` 东北/东南各 `50` 次。
- 所有初始扰动必须为 `[0.0, 0.0]`。
- 每次最终误差必须不大于 `0.35 m`。
- 不得出现跨楼层路线。
- Carter 不得规划进入楼梯或二楼中央镂空。
- 每次试验结束后相关规划器必须正常退出。
- 不得因为历史 TF、DDS 或点云残留导致下一次直接误判成功。
- 不得出现永久卡死或无限重规划。

### 8.2 正式通过条件

当前阶段采用严格门禁：

```text
600/600 PASS
failure_count = 0
```

如果出现任何失败，阶段6暂不标记完成。应按 `failure_code` 和对应日志分析，修复后
重新执行正式门禁，不能只删除失败记录。

### 8.3 完成后的人工核对

运行结束后检查：

```bash
wc -l /Users/lab4099/Desktop/Mujoco/work/results/stage6/stage6_cargo_fixed_600.jsonl
```

必须输出 `600`。

再查看汇总：

```bash
cat /Users/lab4099/Desktop/Mujoco/work/results/stage6/stage6_cargo_fixed_600.summary.json
```

必须包含：

```json
"trial_count": 600,
"success_count": 600,
"failure_count": 0
```

## 9. 如果测试失败，需要提交的信息

请保留并提供：

1. 终端最后30行输出。
2. `stage6_cargo_fixed_600.jsonl`。
3. `stage6_cargo_fixed_600.summary.json`（如果已生成）。
4. 失败条目中的 `log` 文件路径。
5. 失败时对应的机器人和方向。
6. 是否发生 Mac 休眠、终端关闭或其他高负载程序同时运行。

不要只提供“卡住了”的截图；逐次日志包含规划器退出、超时、终点误差和点云加载
信息，是判断路线问题、环境问题还是进程残留问题的主要证据。

## 10. 600次通过后的下一阶段

600次全部通过后：

1. 将阶段6标记为完成。
2. 暂不立即加入 Carter 随机扰动。
3. 按既定顺序进入 Go2 四条下楼路线的 RViz 人工验收。
4. 四条下楼路线通过后，执行 Go2 上下楼160次固定路线批测。
5. 再进入真实任务、货物所有权和正式交接状态机阶段。

### 2026-08-04 后续执行结果

- Go2 四条下楼路线已完成人工 RViz 验收。
- Go2 八条固定上下楼路线正式批测完成：`160/160 PASS`。
- 阶段 7 货物所有权固定任务完成：四条协作链各 50 次，`200/200 PASS`。
- 运输期间保持唯一所有者，交付后无复制、丢失或机器人货物残留。
- 详细证据见 `results/stage7/stage7_cargo_ownership_fixed_200_report.md`。
- 下一阶段为阶段 8 正式交接事务门禁：车到狗、狗到车、同层三类各 100 次，
  验证成功提交、非法条件拒绝和失败回滚，目标成功率不低于 95%。
- 阶段 8 已于 2026-08-04 完成：三类共 `300/300` 测试项通过，每类正常
  提交 96 次、安全拒绝 2 次、安全回滚 2 次，正常提交率均为 96%。
- 阶段 8 证据见 `results/stage8/stage8_handover_fixed_300_report.md`。
- 下一阶段进入阶段 9 七类故障注入与有限时间资源释放测试。
- 阶段 9 已于 2026-08-04 完成：七类故障各 50 次，共 `350/350 PASS`。
- 全部测试均释放任务、货物、区域、交接区和楼梯资源，恢复唯一货物所有权，
  且后续任务可以重新获得相同资源。
- 阶段 9 证据见 `results/stage9/stage9_failure_recovery_fixed_350_report.md`。
- 下一阶段进入阶段 10 规则调度基线：覆盖全部任务类型的 200 个固定任务。
- 阶段 10 已于 2026-08-04 完成：同区直达、同层交接、跨层上楼、跨层下楼
  四类各 50 个，共 `200/200 PASS`。
- 修正二楼到一楼任务的反向选梯距离计算，并增加回归测试。
- 阶段 10 证据见 `results/stage10/stage10_rule_baseline_fixed_200_report.md`。
- 2026-08-04 方向修订：阶段 11 不再以 2–4 个任务同时执行为主线，改为异步
  连续任务。一次执行一个主任务，任务间留调度间隔，机器人位置、楼层、累计
  路程和任务次数不复位。
- 阶段 11 将先建立持久状态规则基线并连续运行 30 分钟；阶段 12 再封装事件驱动
  强化学习组合动作，用于选择取货车、Go2、楼梯、接货车、交接点和任务后待命
  策略。
- 并发任务、随机起点和动态障碍降为规则与强化学习稳定后的鲁棒性扩展。
- 正式决策见 `docs/architecture/decision_20260804_async_persistent_dispatch.md`。

随机初始扰动、随机目标和动态障碍属于后续鲁棒性扩展，不属于本次600次固定路线
正式门禁。
