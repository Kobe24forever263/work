# 阶段18：语义隔离消融长训练执行说明

## 当前状态

- Medium接口门禁：42/42通过。
- Medium、Dense、Burst短程门禁：全部通过。
- 非法动作：0。
- 资源泄漏：0。
- Medium正式长训练：50/50完成，并已完成v3新鲜种子locked test。
- Dense、Burst正式长训练尚未启动，必须由用户手动启动。

## 五项正式消融

1. `no_position_only`：删除绝对楼层与XYZ，保留ETA、成本和历史。
2. `no_eta_cost_only`：删除ETA、估计成本和成本比，保留位置与历史。
3. `no_history_only`：删除累计里程、完成数、空闲时间及全局完成/失败历史。
4. `no_explicit_queue_resource`：删除显式队列/资源遥测；安全动作掩码保留。
5. `no_explicit_handover_risk`：删除交接次数和超时概率；运输模式语义保留。

## 正式启动命令

在终端执行：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python scripts/run_stage18_clean_ablation_long.py --profiles MEDIUM DENSE BURST --seed-start 1 --seed-end 10 --updates 2000 --execute
```

完整计划为150个训练运行、每个运行2000 updates/8000 episodes，共1,200,000个PPO训练episode。按此前每个运行约41–44分钟估算，单路顺序运行约103–110小时。

建议先运行DENSE作为下一批：

```bash
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python scripts/run_stage18_clean_ablation_long.py --profiles DENSE --seed-start 1 --seed-end 10 --updates 2000 --execute
```

DENSE或BURST各包含50个运行，单路约34–37小时。建议先完成DENSE并冻结，再运行BURST；两者一起顺序运行约68–74小时。

## 输出位置

- MEDIUM：`results/stage18/<condition>/seed_01..10/`
- DENSE：`results/stage18_dense/<condition>/seed_01..10/`
- BURST：`results/stage18_burst/<condition>/seed_01..10/`
- 每个目录包含 `stage18_ppo.pt`、`stage18_ppo.best.pt`、history、summary和周期checkpoint。
- Medium状态：`results/stage18_clean_ablation/long_campaign_medium.status.json`
- Dense状态：`results/stage18_clean_ablation/long_campaign_dense.status.json`
- Burst状态：`results/stage18_clean_ablation/long_campaign_burst.status.json`

## 正式验收边界

长训练结束后先冻结模型及SHA256，再用全新的locked-test种子评估。不得使用v2的45000000–45000099继续调参。最终统计使用TASK_KEYED共同随机数、训练种子轴×测试种子轴crossed bootstrap、成功交付吞吐和预定义五项主要假设族Holm校正。
