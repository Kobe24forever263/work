# 阶段 11 异步持续调度 20 任务自动门禁报告

日期：2026-08-05

## 结论

固定种子 `20260805` 的20个异步连续任务全部完成：`20/20 PASS`。同种子完整
重放得到相同任务顺序与机器人分配结果。

## 覆盖与结果

| 任务类型 | 次数 | 结果 |
|---|---:|---:|
| 一楼同层 | 5 | 5/5 |
| 二楼同层 | 5 | 5/5 |
| 一楼到二楼 | 5 | 5/5 |
| 二楼到一楼 | 5 | 5/5 |

- 动态分配组合：14种；
- 被动作掩码拒绝的非法探针：40个；
- 10台机器人均发生了持续状态变化；
- 全部任务结束后任务队列清空；
- 全部机器人 `cargo_id` 与 `task_id` 清空；
- 所有SMDP记录均包含有效的状态、动作、掩码、奖励、`delta_time` 和下一状态；
- 同种子双跑结果一致；
- 核心回归测试：`13/13 PASS`。

## 队列基线

本轮任务到达间隔为2–6秒，而单任务逻辑执行时间明显更长，因此平均任务等待
时间达到 `412.14 s`。这不是任务丢失或死锁，而是输入负载高于串行服务能力的
排队结果。后续应保留该压力序列作为高负载基线，同时增加与服务时间匹配的正常
到达率序列，分别衡量稳定负载与过载情况下的策略表现。

## 证据

- 逐决策结果：`results/stage11/stage11_persistent_fixed_20.jsonl`
- 汇总：`results/stage11/stage11_persistent_fixed_20.summary.json`
- 执行入口：`scripts/run_stage11_persistent_20.py`
- 调度环境：`src/warehouse_core/warehouse_core/persistent_dispatch.py`

下一步为RViz 3–5个连续任务人工验收，重点观察模型是否保留在任务终点、下一
任务是否从当前状态开始、Go2楼层是否连续，以及所选任务与协作组合标签是否
正确显示。

