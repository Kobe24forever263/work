# 文件检索的指引

目标期刊：**IEEE Transactions on Robotics（T-RO）**  
工作区：`/Users/lab4099/Desktop/Mujoco/work`  
更新日期：2026-08-10

## 1. 本指引的用途

本文件用于指导后续 Agent、论文作者和实验复核者在当前工作区中检索可信证据。核心原则是：

1. 论文中的每个技术描述都要能追溯到源代码或冻结配置；
2. 每个定量结论都要能追溯到正式评估 JSON，而不是截图、PPT或终端最后一行；
3. 区分训练集、内部留出集、独立测试集和烟雾测试；
4. 区分原始环境回报与PPO训练使用的归一化回报；
5. 不把RViz/Gazebo演示描述成真实机器人实验；
6. 不把阶段门禁通过直接写成方法全面优于基线。

T-RO论文写作应优先保证可复现性、实验公平性、统计证据和结论边界。检索文件时不要只寻找支持
预期结论的结果，也要同步保留反例、失败模式和置信区间。

## 2. 十分钟快速入口

进入工作区：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
```

先阅读当前总进展：

```bash
sed -n '1,460p' 0809交接文档.md
```

再阅读强化学习训练与正式验收说明：

```bash
sed -n '1,220p' 阶段14_SMDP_PPO训练与测试说明.md
sed -n '1,240p' 阶段15_SERIAL_PPO训练与验收说明.md
sed -n '1,200p' 阶段15_SERIAL回报对比.md
```

查看目前最重要的正式结果：

```bash
sed -n '1,180p' results/stage15/stage15_three_way_100ep.md
sed -n '1,180p' results/stage15/stage15_mixed_continuous_100ep.md
sed -n '1,180p' results/stage15/stage15_serial_medium_three_way_100ep.md
sed -n '1,180p' results/stage15/stage15_serial_vs_concurrent_100ep.md
```

如果只需要快速理解系统，不要从`build/`、`install/`、`log/`或历史运行日志开始。

## 3. 证据等级

### A级：论文定量结论的主证据

- `results/stage15/*.json`中的100-episode正式评估；
- `results/stage14/ppo/long/*context_v2/*.summary.json`；
- 正式best权重及其独立评估结果；
- `src/warehouse_core/warehouse_core/`中的实际实现；
- `src/warehouse_bringup/config/`中的冻结运行配置；
- 与结果完全对应的复现实验脚本。

### B级：系统设计和阶段验收证据

- `0809交接文档.md`；
- `0807_MDP_SMDP设计修正版.md`；
- `docs/architecture/*.md`；
- `results/stage10`至`results/stage14`中的正式门禁报告；
- `src/warehouse_core/test/test_core.py`及测试输出。

### C级：解释和展示材料

- `0809项目进展与强化学习汇报.pptx`；
- `0806进展与问题汇报.md/.pptx`；
- RViz/Gazebo截图和演示视频。

这些文件可帮助组织叙事或制作示意图，但定量数据必须回查A级JSON。

### D级：不得直接用于论文结论

- 文件名包含`smoke`、`5ep`的结果；
- `stage15_serial_medium_three_way_30ep.*`等初步小样本结果；
- 终端进度条最后一次rollout回报；
- `__pycache__/`、`.pytest_cache/`、`build/`、`install/`和`log/`；
- `*.pptx.inspect.ndjson`；
- 未注明Reward版本或Context版本的旧权重；
- 临时截图中的视觉估计值。

## 4. 工作区目录地图

| 路径 | 主要内容 | 论文用途 |
|---|---|---|
| `src/warehouse_core/warehouse_core/` | 任务、资源、SMDP环境、编码器、PPO | 方法与实现细节 |
| `src/warehouse_interfaces/` | ROS 2消息、服务、动作接口 | 系统接口定义 |
| `src/warehouse_bringup/` | 世界、机器人、楼梯、规划器、RViz配置 | 实验平台与系统集成 |
| `docs/architecture/` | 阶段设计、决策记录、验收矩阵 | 系统设计依据 |
| `scripts/` | 生成、训练、评估、复现入口 | 实验复现 |
| `results/stage5`～`stage9` | 本体、货物、交接、故障恢复门禁 | 基础能力验证 |
| `results/stage10`～`stage13` | 规则基线、持久调度、编码、奖励 | MDP/SMDP前置验证 |
| `results/stage14` | Reward V2、Context V2、PPO权重 | 训练配置与权重 |
| `results/stage15` | 压力、混合负载、串并行消融 | 正式论文实验 |
| `build/ install/ log/` | ROS构建和运行生成物 | 调试使用，不作源代码引用 |

## 5. 按论文部分检索

### 5.1 系统概述与实验平台

优先文件：

- `src/warehouse_bringup/worlds/two_floor_warehouse.sdf`：两层仓库世界；
- `src/warehouse_bringup/config/scene.yaml`：场景参数；
- `src/warehouse_bringup/config/robots.yaml`：机器人配置；
- `src/warehouse_bringup/config/stairs.yaml`：四条楼梯；
- `src/warehouse_bringup/config/system.yaml`：系统级配置；
- `src/warehouse_bringup/config/robot_models.yaml`：RViz模型；
- `src/warehouse_bringup/launch/heterogeneous_rviz.launch.py`：异构系统RViz入口；
- `src/warehouse_bringup/launch/simulation.launch.py`：Gazebo入口；
- `docs/architecture/asset_sources.md`：模型和资源来源。

机器人数量与初始位置应回查：

- `src/warehouse_core/warehouse_core/stage14_training.py`。

当前逻辑环境使用4只Go2、4台一楼Carter和2台二楼Carter，共10台机器人。正式任务代价中
Carter名义速度为2.0 m/s，Go2为0.8 m/s。

### 5.2 货物、交接和故障恢复

- `cargo_task.py`：任务和货物状态；
- `handover.py`：交接逻辑；
- `handover_timing.py`：交接持续时间及超时采样；
- `recovery.py`：失败后的恢复；
- `reservations.py`：楼梯、区域、泊位等资源预约；
- `spatial.py`：空间状态；
- `results/stage7/stage7_cargo_ownership_fixed_200_report.md`：货物所有权；
- `results/stage8/stage8_handover_fixed_300_report.md`：交接；
- `results/stage9/stage9_failure_recovery_fixed_350_report.md`：失败恢复；
- `results/stage14/stage14_handover_timeout_gate_report.md`：P95交接超时门禁。

论文中应明确：货物吸附和交接采用逻辑所有权切换，不模拟真实抓取与重力接触。当前P95交接
超时分布属于仿真随机模型，除非未来补充硬件测量，否则不能写成真实机器人实测分布。

### 5.3 MDP/SMDP问题定义

- `0807_MDP_SMDP设计修正版.md`：问题定义草案；
- `0809交接文档.md`：最终实施口径；
- `persistent_dispatch.py`：串行事件驱动转移、奖励和折扣；
- `concurrent_dispatch.py`：并发任务执行与资源锁；
- `stage12_encoding.py`：固定维度状态、候选动作和掩码；
- `results/stage12/stage12_normalization_spec.md`：归一化规范；
- `results/stage12/stage12_encoding_medium_20_report.md`：编码门禁。

当前正式口径：

- 状态维度：284；
- 最大候选数：1536；
- 每个候选特征：28维；
- 高层动作：选择任务、运输模式和参与机器人/楼梯/交接点；
- 低层导航：由SCAN-Planner执行；
- 时间折扣：`gamma_k = 0.99^(delta_time / 10 s)`；
- 任务结束后机器人位置、楼层和累计状态不重置。

快速定位：

```bash
rg -n "STATE|ACTION|1536|284|candidate|action_mask" \
  src/warehouse_core/warehouse_core/stage12_encoding.py

rg -n "gamma0|discount_tau_s|RewardConfig|delta_time" \
  src/warehouse_core/warehouse_core/persistent_dispatch.py
```

### 5.4 运输模式和可行性约束

检索：

```bash
rg -n "SINGLE_CAR|SINGLE_DOG|CAR_DOG_CAR|is_assignment_legal" \
  src/warehouse_core/warehouse_core
```

正式动作空间只包含：

- `SINGLE_CAR`；
- `SINGLE_DOG`；
- `CAR_DOG_CAR`。

不存在车—车交接。Carter不能走楼梯。策略必须在动作采样前使用合法动作掩码排除机器人、
楼梯、区域、交接点和货物所有权冲突。

### 5.5 Reward V2

主文件：

- `persistent_dispatch.py`中的`RewardConfig`；
- `stage14_training.py`中的`STAGE14_REWARD_CONFIG`；
- `results/stage14/reward_v2/stage14_reward_v2_gate_report.md`；
- `阶段14_SMDP_PPO训练与测试说明.md`。

Reward V2正式改动包括：

- 未完成任务时间尺度：30 s；
- 交接超时/任务失败惩罚：2.0；
- 机器人占用时间系数：0.0002；
- 没有单车、单狗或车—狗—车专属奖励。

训练使用运行回报标准差归一化，不减均值并裁剪到±10。评估报告展示的是原始20任务episode
累计回报。DENSE/BURST出现负回报主要表示排队时间成本超过完成奖励，不表示任务普遍失败。

### 5.6 策略网络与PPO

- `stage14_policy.py`：Context V2候选动作策略；
- `stage14_ppo.py`：PPO、GAE、检查点和奖励归一化；
- `stage14_training.py`：环境、任务负载和正式Reward配置；
- `scripts/run_stage14_smdp_ppo.py`：训练入口；
- `阶段14_SMDP_PPO训练与测试说明.md`：训练说明。

正式网络和PPO参数以对应训练`*.summary.json`为准，不要从PPT手工抄录。当前正式配置为：

- `contextual_masked_candidate_actor_critic_v2`；
- 状态宽度284、候选宽度28、隐藏宽度96；
- 学习率`3e-4`；
- clip ratio 0.2；
- GAE lambda 0.95；
- value coefficient 0.5；
- entropy coefficient 0.01；
- max gradient norm 1.0；
- 4 PPO epochs；
- minibatch 32。

### 5.7 规则基线

论文至少区分以下两种规则基线：

1. **时间贪心规则**：优先级/任务顺序固定，在合法候选中按当前ETA选择；
2. **风险感知规则**：ETA加Reward V2等价的交接失败、交接次数和机器人占用期望代价。

实现入口：

- `WarehouseDispatchGymEnv.rule_action()`；
- `scripts/run_stage15_stress_evaluation.py`中的`reward_aligned_risk_rule_action()`；
- `results/stage15/stage15_three_way_100ep.md`中的基线定义。

风险规则不知道已经抽中的交接随机样本，只知道公开P95风险，因此属于因果基线，不是预知未来
的oracle。T-RO投稿前仍应补充与相关领域代表性学习调度方法的对比；当前两种规则基线不足以
单独支撑“优于所有先进方法”的表述。

## 6. 正式权重索引

### CONCURRENT主策略

```text
results/stage14/ppo/long/realistic_concurrent_medium_reward_v2_context_v2/
  stage14_realistic_concurrent_medium_reward_v2_context_v2.best.pt
  stage14_realistic_concurrent_medium_reward_v2_context_v2.pt
  stage14_realistic_concurrent_medium_reward_v2_context_v2.summary.json
  stage14_realistic_concurrent_medium_reward_v2_context_v2.history.json
```

正式评估使用`best.pt`，最佳检查点为update 160。`.pt`是不一定最优的最后update权重。

### SERIAL降级策略

```text
results/stage14/ppo/long/realistic_serial_medium_reward_v2_context_v2/
  stage14_realistic_serial_medium_reward_v2_context_v2.best.pt
  stage14_realistic_serial_medium_reward_v2_context_v2.pt
  stage14_realistic_serial_medium_reward_v2_context_v2.summary.json
  stage14_realistic_serial_medium_reward_v2_context_v2.history.json
```

正式评估使用`best.pt`，最佳检查点为update 940。

旧目录`realistic_concurrent_medium/`属于早期Reward/Context版本，不得与Reward V2 + Context V2
正式结果混合。

## 7. 正式实验结果索引

| 文件 | 规模 | 用途 |
|---|---:|---|
| `stage15_three_way_100ep.json/.md` | 4场景×100ep×20任务×3策略=24000结果 | PPO与两种规则的主压力评估 |
| `stage15_mixed_continuous_100ep.json/.md` | 100ep×60任务×3策略=18000结果 | 同一权重随NORMAL→DENSE→BURST自适应 |
| `stage15_serial_medium_three_way_100ep.json/.md` | 100ep×20任务×3策略=6000结果 | SERIAL与规则对比 |
| `stage15_serial_vs_concurrent_100ep.json/.md` | 3场景×100ep×20任务×2策略=12000结果 | 并发能力配对消融 |

优先读Markdown理解口径，再从JSON提取数值。论文表格和绘图必须由JSON生成。

不要用以下文件作为正式主结论：

- `stage15_serial_vs_concurrent_5ep.*`；
- `stage15_serial_medium_three_way_30ep.*`；
- `stage15_stress_100ep.*`中的早期两方结果，如果已有更强的三方结果可用。

## 8. JSON取数规范

查看单个正式场景：

```bash
jq '.results.MEDIUM' results/stage15/stage15_three_way_100ep.json
jq '.results.DENSE' results/stage15/stage15_three_way_100ep.json
jq '.results.BURST' results/stage15/stage15_three_way_100ep.json
```

查看PPO、规则和风险规则的核心指标：

```bash
jq '.results.DENSE | {
  ppo: .policy,
  time_rule: .rule_baseline,
  risk_rule: .risk_aware_rule_baseline,
  versus_risk: .paired_comparison_vs_risk_aware_rule
}' results/stage15/stage15_three_way_100ep.json
```

查看串并行消融：

```bash
jq '.results.BURST | {
  serial,
  concurrent,
  difference: .paired_comparison
}' results/stage15/stage15_serial_vs_concurrent_100ep.json
```

取数时必须同时记录：

- 场景和到达间隔；
- execution mode；
- 权重路径及checkpoint update；
- episode数和每episode任务数；
- 固定种子范围；
- task fingerprint是否一致；
- 交接采样是否为`TASK_KEYED_COMMON_RANDOM_NUMBERS`；
- 差值方向是`PPO − rule`还是`CONCURRENT − SERIAL`；
- 配对95%置信区间；
- 指标单位。

## 9. 容易混淆的指标

### 9.1 原始回报与归一化回报

- `raw_episode_reward`：原始20任务episode总回报，用于策略比较；
- `normalized_step_reward`：PPO内部优化量，不能直接与规则基线回报比较。

### 9.2 两种吞吐

- `throughput_tasks_per_hour`：完成或受控失败均算已解决；
- `successful_throughput_tasks_per_hour`：只统计成功完成任务。

论文正文优先报告成功吞吐，同时在附录说明已解决吞吐。

### 9.3 PASS的含义

评估文件中的`passed: true`通常只证明：

- 任务流严格配对；
- 全部任务有明确结果；
- 动作合法；
- 无资源泄漏。

它不自动证明PPO显著优于规则。是否显著必须查看差值和配对95%置信区间。

### 9.4 训练最后一行不是最佳成绩

训练终端显示的是最后一批随机rollout。正式结论应使用best权重的独立留出或压力评估。

## 10. 图表制作规则

1. 所有图表从正式JSON自动生成，不从PPT、Markdown表格或截图重新录入；
2. 同一张比较图使用严格相同的任务种子和随机交接样本；
3. 误差线优先使用配对bootstrap 95%置信区间；
4. 回报为负时保留原始零点，不把负数误写成失败率；
5. 同时展示绝对值和相对差值，避免只展示百分比提升；
6. 至少同时报告成功率、成功吞吐、等待、流转、里程和失败原因；
7. 明确标注`higher is better`或`lower is better`；
8. MEDIUM、DENSE、BURST和CROSS_HEAVY不要混用不同种子批次后直接做配对差值；
9. 将训练曲线、独立评估和消融实验分图展示；
10. 论文图的源数据和生成脚本应一同归档。

## 11. 当前可以支持的论文结论

现有正式证据支持：

- 一个统一权重能根据任务与资源上下文选择单车、单狗或车—狗—车；
- 在连续NORMAL→DENSE→BURST场景中，PPO会随负载减少资源密集型协作链；
- 相对风险规则，PPO在DENSE/BURST中显著降低等待、流转和里程并提高原始回报；
- CONCURRENT相对SERIAL的优势随负载增大；
- 非法动作和资源泄漏在正式逻辑评估中为0；
- SERIAL可作为降级策略，但不应替代CONCURRENT主方向。

现有证据不支持：

- “所有场景、所有指标全面优于规则”；
- “已经优于T-RO相关领域的所有先进方法”；
- “已经完成真实机器人验证”；
- “交接超时分布来自硬件实测”；
- “SCAN-Planner低层导航性能由强化学习直接提升”；
- “阶段15已经完全结束”。

## 12. T-RO投稿前必须补齐的证据

1. **上下文消融**：屏蔽队列/资源、交接风险、持久位置，证明性能提升来自哪些输入；
2. **独立训练种子**：不能只对单个正式best权重使用大量评估种子；应报告多个独立训练种子；
3. **学习基线**：补充与问题最接近的代表性RL/MARL/调度方法；
4. **恢复场景**：增加BURST→NORMAL，验证负载下降后的策略恢复；
5. **导航闭环**：将高层策略决定映射到RViz/SCAN-Planner完整任务并记录成功率和执行时间；
6. **物理或硬件边界**：若无法进行真实机器人实验，应明确仿真层级、动力学缺失和sim-to-real限制；
7. **计算开销**：报告决策延迟、模型参数量、CPU/GPU和实时性；
8. **可复现快照**：冻结代码、配置、权重、环境依赖和生成图表脚本。

当前`work`根目录未检测到Git元数据。投稿实验开始前必须建立版本化快照，至少保存commit ID
或归档SHA256，否则后续无法证明结果对应哪一版代码。

## 13. 复现实验入口

正式CONCURRENT训练：

```bash
cd /Users/lab4099/Desktop/Mujoco/work/scripts
./run_stage14_realistic_ppo_long.command
```

正式SERIAL训练：

```bash
./run_stage14_realistic_serial_ppo_long.command
```

三方压力评估：

```bash
./run_stage15_stress_evaluation.command
```

连续混合负载评估：

```bash
./run_stage15_mixed_continuous_evaluation.command
```

SERIAL与CONCURRENT配对消融：

```bash
./run_stage15_serial_concurrent_comparison.command 100
```

运行长训练前必须确认输出目录，避免覆盖正式best权重。长训练结束后记录终端命令、开始/结束
时间、机器环境、种子、best update、final update和完整summary路径。

## 14. 推荐的论文证据清单

为每个论文表格或图建立一条记录：

```text
Figure/Table ID:
Claim:
Source JSON:
Source keys:
Weights:
Checkpoint update:
Execution mode:
Scenario:
Seeds:
Task count:
Difference direction:
95% confidence interval:
Generation script:
Known limitations:
```

任何无法填写完整记录的数值，都不应进入T-RO论文主文。
