# 阶段22：算法级消融实验执行说明

更新日期：2026-08-26  
工作边界：本文件只用于实验执行、质量控制与结果追踪，不承担论文写作。

## 1. 当前结论

阶段22的协议、训练开关和短门禁已经就绪。短门禁只能证明实现语义、训练接口与安全约束正确，不能用于性能结论。

已经通过的检查：

- 阶段22语义接口门禁：17/17；
- 四项两更新短训练：全部通过；
- 四项短训练的非法动作数均为0；
- 四项短训练的资源泄漏数均为0；
- `no_relay` 的教师数据、PPO采样和评估均只允许 `SINGLE_CAR` 与 `SINGLE_DOG`；
- 阶段14、17、18既有回归门禁全部通过。

短门禁汇总：

`results/stage22_algorithm_ablation/short_gate/stage22_algorithm_ablation_smokes.summary.json`

## 2. 四项正式消融

| 条件 | 唯一移除或替换的组件 | 保持不变的组件 |
|---|---|---|
| `fixed_gamma` | 按实际持续时间变化的SMDP折扣改为每决策固定0.99 | 完整上下文网络、热启动、三种运输模式 |
| `no_context_interaction` | 移除上下文与候选动作的逐元素交互，使用扁平掩码PPO | 状态、奖励、折扣、热启动、三种运输模式 |
| `no_warm_start` | 移除规则教师蒸馏热启动，随机初始化 | 完整网络、可变SMDP折扣、三种运输模式 |
| `no_relay` | 禁用 `CAR_DOG_CAR` | 完整网络、可变SMDP折扣、热启动、单车与单狗模式 |

完整模型不重复训练，直接使用阶段20已经冻结的10个权重作为配对参照。

## 3. 冻结预算与数据隔离

- 每项消融：10个独立训练种子；
- 每个种子：2000 updates；
- 每个 update：4 episodes；
- 每个权重：8000 episodes；
- 到达过程：`MIXED_CURRICULUM`；
- 执行模式：`CONCURRENT`；
- 训练种子：与阶段20完整模型逐一配对；
- 验证种子：62000000–62000099，只用于检查点选择；
- 锁定测试种子：67100000–67100099，在全部权重冻结后只运行一次；
- 锁定测试禁止用于调参或重新选择权重。

完整协议：

`src/warehouse_bringup/config/experiment_seeds_stage22_algorithm_ablation.yaml`

## 4. 正式训练前的推荐顺序

先只完成四个条件的 `seed_01`。它们属于正式10种子队列，不是额外试跑。检查权重、历史、摘要和安全断言后，再补 `seed_02` 至 `seed_10`。

### 终端A

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage22_algorithm_ablation_campaign.py \
  --conditions fixed_gamma no_context_interaction \
  --seed-start 1 --seed-end 1 \
  --execute --confirm-long
```

### 终端B

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage22_algorithm_ablation_campaign.py \
  --conditions no_warm_start no_relay \
  --seed-start 1 --seed-end 1 \
  --execute --confirm-long
```

不要在同一结果目录重复启动同一条件、同一种子。编排器会跳过已经完整结束的权重，并对存在合法检查点的中断任务执行续跑。

## 5. seed 1 验收后补齐正式队列

终端A：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage22_algorithm_ablation_campaign.py \
  --conditions fixed_gamma no_context_interaction \
  --seed-start 2 --seed-end 10 \
  --execute --confirm-long
```

终端B：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage22_algorithm_ablation_campaign.py \
  --conditions no_warm_start no_relay \
  --seed-start 2 --seed-end 10 \
  --execute --confirm-long
```

## 6. 时间估计

阶段20相同预算的10个正式权重，从第50个检查点到最终摘要平均用时2.83小时，范围2.48–3.06小时；热启动和第一个检查点还会增加少量时间。因此：

- 单个权重：按约3小时预留；
- 四个 `seed_01`：两路并行约6–7小时；
- 全部40个消融权重：单路约113–125小时；
- 全部40个消融权重：两路并行约57–65小时。

两路并行是当前建议上限。更多并行会造成OpenMP/CPU竞争，既可能延长总时间，也会降低运行时间记录的可比性。

## 7. 输出位置

每个正式权重：

```text
results/stage22_algorithm_ablation/long/<condition>/seed_<NN>/stage22_ppo.pt
```

最优验证权重：

```text
results/stage22_algorithm_ablation/long/<condition>/seed_<NN>/stage22_ppo.best.pt
```

训练历史：

```text
results/stage22_algorithm_ablation/long/<condition>/seed_<NN>/stage22_ppo.history.json
```

单次摘要：

```text
results/stage22_algorithm_ablation/long/<condition>/seed_<NN>/stage22_ppo.summary.json
```

编排状态：

```text
results/stage22_algorithm_ablation/long/stage22_campaign_*.status.json
```

## 8. 正式权重的单次验收标准

每个摘要必须同时满足：

- `updates == 2000`；
- `episodes_trained_total == 8000`；
- 验证集恰为62000000–62000099；
- 100个验证episode、8000个验证任务全部具有终态；
- 非法动作数为0；
- 资源与货物泄漏数为0；
- 状态、动作、折扣、奖励与损失均为有限值；
- 条件元数据与该消融定义完全一致；
- 模式塌缩只记录，不删除、不补跑、不人为筛除。

## 9. 全部权重冻结后的下一步

只有40个消融权重全部完成并通过完整性冻结检查后，才能启动共享锁定测试。正式比较采用：

- 完整模型减去各消融模型的配对差；
- 训练种子 × 共享测试任务的 crossed bootstrap；
- 10000次bootstrap；
- 主指标族使用Holm校正；
- 主指标为成功率、成功吞吐量、平均等待时间和P95等待时间。

在锁定测试完成前，不根据测试结果修改奖励、网络、训练预算、候选动作或运输模式定义。
