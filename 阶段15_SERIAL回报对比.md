# 阶段15 SERIAL回报对比

日期：2026-08-09

## 1. 口径

本报告中的回报为留出评估产生的原始episode累计回报，每个episode包含20个异步到达任务。
数值越高越好。它不是PPO训练过程中用于优化的标准差归一化step reward，因此不会被限制在
`[-1,1]`，在DENSE和BURST排队压力下也可以出现较大的负数。

不同随机种子批次的绝对回报不直接做差。下表中的差值均来自同一批任务、相同初态和按任务
ID绑定的相同交接耗时样本，并使用episode级配对bootstrap计算95%置信区间。

## 2. SERIAL PPO与SERIAL规则基线

MEDIUM场景100个episode，每个策略2000任务。

| 策略 | 平均episode回报 | 相对PPO的差距 |
|---|---:|---:|
| SERIAL PPO | **15.994** | — |
| 时间贪心规则 | 15.444 | PPO高 **0.551** |
| 风险感知规则 | 15.825 | PPO高 **0.170** |

配对统计：

| 对比 | PPO回报差 | 配对95%置信区间 | 判断 |
|---|---:|---:|---|
| PPO − 时间规则 | +0.551 | [+0.237, +0.891] | 明确高于时间规则 |
| PPO − 风险规则 | +0.170 | [-0.018, +0.396] | 区间跨0，不能宣称显著更高 |

这说明SERIAL PPO相对简单时间规则存在稳定回报优势，但相对更强的风险规则只有小幅数值
优势，当前样本不足以证明回报显著更高。

## 3. SERIAL PPO与CONCURRENT PPO

两套独立训练的best权重在相同任务流上比较。每个场景100个episode、每套策略2000任务。
所有差值均为`CONCURRENT − SERIAL`。

| 场景 | SERIAL回报 | CONCURRENT回报 | 并发回报提升 | 配对95%置信区间 |
|---|---:|---:|---:|---:|
| MEDIUM | 16.215 | **18.741** | +2.526 | [+2.170, +2.931] |
| DENSE | -83.462 | **-17.830** | +65.632 | [+63.308, +67.961] |
| BURST | -218.935 | **-86.668** | +132.268 | [+126.750, +137.498] |

三个场景的置信区间均不跨0。MEDIUM下并发优势有限；随着任务到达变密，SERIAL只能逐项
执行，等待和未完成任务惩罚快速累积，CONCURRENT的回报优势明显扩大。负回报不表示所有
任务都失败：DENSE和BURST中两套策略的成功率仍约为97%～98%，负数主要反映严重排队。

## 4. 结论

1. SERIAL PPO对时间规则的回报优势已通过统计门禁。
2. SERIAL PPO对风险规则只有小幅数值优势，尚无显著回报优势。
3. CONCURRENT PPO在MEDIUM、DENSE、BURST均显著高于SERIAL PPO，且负载越高优势越大。
4. 因此SERIAL适合作为单任务降级模式；正式多任务运行仍应使用CONCURRENT策略。

原始证据：

- `results/stage15/stage15_serial_medium_three_way_100ep.json`；
- `results/stage15/stage15_serial_vs_concurrent_100ep.json`。
