# 8月6日项目进展与问题汇报

> 用途：本文件既是截至 2026 年 8 月 6 日的项目阶段汇报，也是后续 Agent 制作项目进展 PPT 的唯一内容依据。制作 PPT 时必须区分“已实现并验收”“仅完成逻辑验证”“下一阶段计划”，不得把计划描述成已有成果。

## 一、项目目标与当前技术路线

项目面向双层仓储场景中的异构机器人协作运输。系统由一楼和二楼 Carter 小车、4 只 Go2 四足机器人、4 条跨楼层楼梯、装卸货平台、交接点及待命泊位组成。典型跨楼层任务链为：

1. 起点楼层 Carter 前往装货平台取货；
2. Carter 将货物运送到所选楼梯的发送交接点；
3. 货物瞬时切换并吸附到 Go2，Go2 携货通过楼梯；
4. Go2 在目标楼层楼梯口与 Carter 交接；
5. 目标楼层 Carter 将货物送至目标平台并卸货；
6. 参与任务的机器人前往互不重叠的空闲待命泊位，位置和累计状态保留给下一任务。

当前统一技术路线如下：

- Carter 和 Go2 均使用 SCAN-Planner 执行具体路径；
- 高层调度器不直接控制速度、关节或步态，只选择任务、机器人组合、楼梯和任务后策略；
- 当前优先使用既定确定性路线，不加入随机扰动；
- 当前使用 RViz 做功能和路线验收，Gazebo 留到项目收尾阶段统一优化；
- 任务异步到达并排队，但阶段 11 的实际运输链采用串行执行；
- 机器人在任务之间不整体复位，下一任务从上一任务结束后的真实逻辑状态继续；
- 货物采用可视化方块和逻辑吸附，不模拟货物重力，交接时允许瞬时转移。

## 二、截至8月6日已经完成的工作

### 2.1 双层仓库与基础场景

- 已建立双层仓库、中央镂空区域、四条楼梯和护栏结构；
- 修正楼梯顶部与二楼平台连接，避免 Go2 在最后一级台阶掉落；
- 楼梯采用黄色材质便于观察；
- 一楼增加照明和 5 根二楼支撑柱；
- 原有障碍物区域改造为装货、卸货及运输平台；
- 一楼外围 Carter 航线已经扩展至楼梯交接区域；
- 4 只 Go2 和多辆 Carter 已纳入统一仓储任务场景。

### 2.2 RViz、模型与点云显示

- 已生成供 SCAN-Planner 使用的 PCD 点云和 YAML 配置；
- 已解决 Go2 RobotModel 因 TF 链不完整而在 RViz 中爆红的问题；
- 已实现仓库三维点云、占据区域、规划轨迹、机器人标签、Go2/Carter 模型的统一显示；
- 已验证四个楼梯方向的跨楼层车—狗—车路线；
- 后续演示要求继续显示 Go2 和 Carter 本体，不能只显示路径、标签或简化标记。

### 2.3 路线规划与协作运输

- Carter 已由 Nav2 方案统一切换为 SCAN-Planner；
- Carter 路线尽量采用接近 Nav2 的直线行驶效果；
- Go2 与 Carter 使用不同运动能力模型、但共享高层任务调度是可行的；
- 已验证从一楼取货、楼梯口交接、Go2 上楼、二楼 Carter 接货和平台卸货的完整路线；
- 已验证其余楼梯的同类交接路线；
- 二楼交接逻辑已调整为 Go2 走出楼梯口少量距离后交货，由二楼 Carter 承担主要平面运输距离；
- 楼梯不再与任务象限固定绑定，同一跨层任务保留多个合法楼梯候选。

### 2.4 货物显示与交接

- 货物使用缩小后的方块模型表示，并靠近当前承运机器人本体吸附；
- 货物初始位于具有一定高度的装卸货平台，而不是地面；
- Carter 取货、Carter—Go2、Go2—Carter 交接和目标平台卸货均已实现可视化所有权切换；
- 交接完成后无需额外长时间停顿，确认吸附成功即可继续执行；
- 货物显示、吸附、转移和卸货逻辑已经人工验收通过，不再作为当前主要风险。

### 2.5 阶段11：持久状态与事件驱动调度

阶段 11 已正式验收并封板，形成可供规则策略和后续强化学习策略共同使用的事件驱动 SMDP 环境，主要能力包括：

- 无损异步任务队列，策略每次观察优先级最高的固定 `K=8` 个任务；
- 一楼同层、二楼同层、跨层上行和跨层下行四类任务；
- 动态选择取货 Carter、Go2、楼梯和接货 Carter；
- 机器人位置、楼层、累计距离、累计任务数和上一任务结果跨任务保留；
- `task_reset` 只清理本任务状态，机器人不整体回到初始位置；
- 机器人完成任务后进入同楼层、同类型且互不重叠的最近空闲泊位；
- 动作掩码拒绝错误楼层、错误类型、忙碌、故障、已有任务或带货机器人；
- 区域、交接点和楼梯资源具有真实的占用与释放审计事件；
- SMDP 回报按异步任务的实际到达时刻积分未完成任务时间；
- 每次决策保存完整的 `state/action/action_mask/reward/delta_time/next_state/terminated/truncated`。

## 三、阶段11正式验收数据

正式验收采用 `medium` 到达负载和固定种子 `20260805`，同种子完整运行两遍。

| 指标 | 验收结果 |
|---|---:|
| 核心自动测试 | 17/17 PASS |
| 连续任务 | 20/20 PASS |
| 失败任务 | 0 |
| 一楼同层任务 | 5 |
| 二楼同层任务 | 5 |
| 跨层上行任务 | 5 |
| 跨层下行任务 | 5 |
| 动态协作组合 | 14 种 |
| 非法动作探针 | 40/40 被拒绝 |
| 资源占用/释放事件 | 120 条，严格配对 |
| 最终资源残留 | 0 |
| 最终货物/任务残留 | 0 |
| 最终机器人位置重叠 | 0 |
| 同种子重放 | 完全一致 |
| 仿真总时间 | 1408.0392 秒 |
| 平均任务持续时间 | 70.002 秒 |
| 平均等待时间 | 296.2206 秒 |
| P95 等待时间 | 558.2898 秒 |
| 最大等待时间 | 782.3613 秒 |
| 队列峰值 | 10 |

验收证据：

- `results/stage11/stage11_persistent_medium_20_final_gate.summary.json`
- `results/stage11/stage11_persistent_medium_20_final_gate.jsonl`
- `results/stage11/stage11_persistent_medium_20_final_gate_report.md`

## 四、当前问题与原因判断

### 4.1 Medium 负载等待时间问题（已修复）

阶段11正式封板时的平均等待时间为296.22秒。核查发现原时长模型把取货 Carter、
Go2、接货 Carter 可以同时进行的接近动作串行相加，并把多台机器人同时前往泊位的
距离也串行相加，导致平均任务时长被高估到70.00秒。与此同时旧 medium 发布间隔
为30～50秒，平均到达速度高于串行系统服务能力，因此形成持续积压。

修复分为两部分：任务时长改为协作链关键路径，多机器人进场和离场按并行过程计算，
但累计里程仍按各机器人真实总距离相加；任务发布档位按修复后的约50秒平均服务时间
重新标定，medium 调整为55～75秒，目标负载约75%。

修复后 medium 20任务结果为：平均任务时长49.65秒，平均等待5.64秒，P95等待
20.13秒，最大等待32.61秒，队列峰值1，20/20任务完成且无资源、货物或任务残留。
相较封板基线，平均等待下降约98.1%。旧结果继续保留为历史对比，不再代表当前
medium 档位。

### 4.2 楼梯选择存在真实的全局权衡

当一楼取货点投影与二楼卸货点距离较远时，“距离取货点最近的楼梯”不一定具有最短总完成时间。调度需要同时考虑：

- 取货 Carter 到货物和发送交接点的距离；
- Go2 当前点到楼梯入口的空驶距离；
- 楼梯通过成本、占用状态和可能的等待；
- 目标楼层 Carter 到接收交接点的距离；
- 接货后 Carter 到目标平台的运输距离；
- 各机器人近期任务负载和未来位置价值。

这正是阶段 12/后续强化学习需要解决的问题，不能再使用“象限对应固定楼梯”的规则。

### 4.3 当前资源模型是逻辑级，尚非多任务并发级

阶段 11 已经验证资源占用与释放不会泄漏，但当前任务链串行执行，因此尚未验证两条运输链同时竞争同一楼梯、交接点或区域时的排队、公平性和死锁恢复。并发运输属于后续扩展，不应混入阶段 12 第一版训练接口。

### 4.4 RViz 与 Gazebo 的验证边界

当前主线只在 RViz 中做规划和协作逻辑验证，以降低 Mac M4 上同时运行 Gazebo、RViz、点云和多个机器人模型的压力。Gazebo 中的动力学步态、模型抖动、碰撞和最终视觉效果留到项目收尾统一优化。PPT 必须明确：当前通过的是逻辑规划、状态机和 RViz 可视化验收，不应表述为完整物理仿真已经最终通过。

### 4.5 泊位仍需最终物理可达性复核

离散泊位在逻辑层已保证唯一且不重叠；当前 20 任务门禁证明不会出现相同最终坐标。但在最终 Gazebo 优化阶段，仍需检查泊位周围模型占用、机器人实际外形尺寸和 SCAN-Planner 末端可达性。

## 五、下一阶段任务：阶段12

阶段 12 的目标不是立即训练强化学习，而是先把阶段 11 的可变结构状态和组合动作转换成稳定、可学习、可测试的策略接口。

### 5.1 固定维度观测编码

- 固定任务槽位为 `K=8`，不足补零并提供 `task_mask`；
- 固定机器人排列顺序，编码类型、楼层、位置、空闲状态、载货状态、故障、累计任务和负载；
- 编码四条楼梯的占用、两侧位置和候选代价；
- 所有距离、时间和累计量统一尺度并归一化；
- 同一状态重复编码必须逐元素一致。

### 5.2 组合动作候选编码

每个候选动作完整表示：

`任务 × 取货 Carter × Go2/不使用 Go2 × 楼梯 × 发送交接点 × 接收交接点 × 接货 Carter × 任务后策略`

策略只在合法候选中选择。跨层任务必须同时保留多个楼梯选项，并显式提供取货侧成本、Go2 成本、跨楼层成本、接货侧成本和目标平台成本，保证策略能够学习全局权衡。

### 5.3 动作掩码压力测试

- 忙碌、故障、带货和已有任务的机器人不能再次接单；
- Carter 不能上下楼；
- 同层任务不能错误调用 Go2；
- 机器人楼层必须与任务链角色一致；
- 楼梯、交接点或区域被占用时冲突动作必须屏蔽；
- 候选数量不足时补齐位置的 mask 必须为 false；
- 没有合法任务时应进入受控 `WAIT`，不能崩溃。

### 5.4 规则策略与学习策略统一接口

规则调度器和未来学习策略必须读取同一份观测、候选动作和动作掩码，并调用同一个环境 `step()`。阶段 12 不替换 SCAN-Planner，也不训练低层运动控制。

## 六、阶段12建议验收标准

1. 观测张量和候选动作张量形状固定，连续运行 20 个任务不发生维度变化；
2. 相同种子、相同状态的编码结果完全一致；
3. 四类任务均至少生成一个合法动作；
4. 每个跨层任务至少保留两个不同楼梯的合法候选；
5. 构造忙碌、故障、带货、楼梯占用等异常状态，非法动作屏蔽率为 100%；
6. 编码后的动作可无歧义解码为原始协作组合；
7. 规则策略通过新接口再次完成 medium 20/20，且不破坏阶段 11 封板结果；
8. 输出阶段 12 的张量规格说明、自动测试报告和一份可直接供强化学习使用的样本数据。

## 七、供PPT制作Agent使用的页面结构

建议制作 12～14 页，采用“问题—方案—证据—风险—下一步”的叙事结构。不要堆砌终端日志，优先使用流程图、场景截图、指标卡片和少量表格。

### 第1页：封面

- 标题：双层仓储异构机器人协作运输项目进展与问题汇报
- 副标题：Carter + Go2 + SCAN-Planner + 事件驱动 SMDP
- 日期：2026年8月6日

### 第2页：项目目标与场景

- 展示双层仓库、四条楼梯、一二楼平台、Carter 和 Go2；
- 用一句话说明目标：完成同层和跨楼层货物运输，并为强化学习调度建立统一环境；
- 优先选用能同时看见仓库点云、Go2、Carter 和楼梯的 RViz 截图。

### 第3页：完整跨楼层任务链

- 使用六步水平流程图：一楼取货 → Carter 运送 → Go2 交接上楼 → 二楼交接 → Carter 送货 → 泊位待命；
- 用橙色方块强调货物所有权在三种载体之间切换。

### 第4页：当前系统架构

- 高层：事件驱动调度器/SMDP；
- 中层：任务、货物、资源锁与交接状态机；
- 执行层：Carter 与 Go2 均使用 SCAN-Planner；
- 显示层：RViz 三维点云、模型和路径；
- 强调高层策略不直接输出速度和关节动作。

### 第5页：已完成的场景与规划工作

- 双层仓库、楼梯连接、照明、支柱、平台和外围路线；
- PCD/YAML、完整 URDF/TF、四楼梯路线；
- 建议采用“场景改造前后”或“四楼梯路线覆盖”的组合图。

### 第6页：货物与交接可视化

- 展示货物位于平台、吸附 Carter、切换 Go2、切换二楼 Carter、最终卸货；
- 标注“无重力逻辑吸附、交接瞬时切换、确认后立即出发”；
- 明确该部分已人工验收通过。

### 第7页：阶段11做了什么

- 使用关键词卡片：异步队列、串行执行、持久状态、动态组合、动作掩码、资源锁、SMDP 记录、唯一泊位；
- 避免在一页放完整公式，只保留“按实际到达时刻累计未完成任务时间成本”。

### 第8页：阶段11正式验收结果

- 用大数字展示：20/20、17/17、14 种组合、40/40、120 条事件、0 残留；
- 用四等分图展示四类任务各5次；
- 页面结论必须写“阶段11纯逻辑环境正式验收通过”。

### 第9页：机器人状态持续与泊位策略

- 画出“任务结束位置 → 最近空闲泊位 → 下一任务从该位置继续”的状态流；
- 强调不重置、泊位唯一、作业点及时释放；
- 不要把逻辑泊位表述为已经完成 Gazebo 物理验收。

### 第10页：等待时间问题与修复结果

- 对比修复前后：平均等待296.22→5.64秒、P95 558.29→20.13秒、队列峰值10→1；
- 配一张“错误串行求和→关键路径计时”和“发布间隔重新标定”的简图；
- 结论应为“原问题是时长建模与负载档位不匹配，不是死锁”；
- 本页可用绿色表示已修复，同时注明更高压力仍由 overload 档测试。

### 第11页：为什么需要强化学习

- 用一个跨层任务示例对比“取货侧近的楼梯”和“目标侧近的楼梯”；
- 显示总成本由两层 Carter、Go2、楼梯占用、等待和未来位置共同决定；
- 说明规则调度器可用，但难以针对长期等待、负载和平衡未来任务进行全局优化。

### 第12页：阶段12工作与验收标准

- 左侧列固定维度观测、动作候选、动作掩码和统一接口；
- 右侧列 20 任务维度稳定、跨层至少两楼梯候选、异常动作 100% 屏蔽、可逆解码和规则回归 20/20；
- 明确阶段 12 是“强化学习接口准备”，不是直接宣称训练完成。

### 可选第13页：风险与边界

- 当前为 RViz/逻辑验收；
- 实际并发资源竞争尚未验证；
- Gazebo 动力学与泊位物理可达性留待最终优化；
- 随机扰动暂时关闭。

### 可选第14页：后续路线图

- 阶段12：张量与动作接口；
- 阶段13：奖励、终止条件和训练规则；
- 后续：规则基线对比、强化学习训练、泛化/压力测试；
- 收尾：Gazebo 动力学、碰撞、步态和最终演示。

## 八、PPT视觉与内容规范

- 主色建议使用深灰或深蓝背景，Carter 使用蓝色，Go2 使用白色或浅灰，货物固定使用橙色，楼梯使用黄色；
- 每页只表达一个核心结论，标题尽量写成结论句；
- 所有指标必须来自本文件或阶段 11 正式 JSON，不得自行推算新数据；
- 截图要裁掉无关桌面和终端区域，同时保留 RViz 左侧模型状态或关键图例；
- 路径、机器人与点云颜色冲突时，可降低点云透明度，但不能隐藏机器人本体；
- 不要使用“完全真实仿真”“已实现并发运输”“强化学习已训练”等不符合当前事实的表述；
- 对阶段 11 使用绿色“已通过”，对等待性能和 Gazebo 物理验证使用黄色“待优化”，对阶段 12 使用蓝色“下一步”；
- 最后一页必须给出明确的下一阶段验收目标，而不是仅写“继续优化”。

## 九、PPT制作前应读取的文件

制作 Agent 至少应读取以下文件后再开始排版：

1. `0809交接文档.md`：完整技术方向和阶段定义；
2. `results/stage11/stage11_persistent_medium_20_final_gate_report.md`：阶段11结论；
3. `results/stage11/stage11_persistent_medium_20_final_gate.summary.json`：正式指标；
4. `results/stage11/stage11_persistent_medium_20_final_gate.jsonl`：逐任务动作与状态证据；
5. `src/warehouse_core/warehouse_core/persistent_dispatch.py`：阶段11环境实现；
6. `src/warehouse_core/warehouse_core/stage12_encoding.py`：阶段12编码接口现状（如存在）；
7. 工作区中的 RViz 配置与已验收截图：用于场景、模型、路线和货物交接页面。

若已有截图无法清楚显示 Go2、Carter、橙色货物和路线，应先重新运行一次已验收 RViz 演示并截图，不得使用仅有点云而缺少机器人本体的旧截图作为最终成果图。

## 十、8月6日阶段12第一轮实施结果

阶段12的固定张量与组合动作接口第一轮门禁已经完成：任务观测补充了目标点坐标，
该处初版观测272维和`1024 × 13`动作张量已被2026-08-07修正版替代。编码器增加了合法动作的
可逆解码检查，以及没有可执行候选时唯一合法的受控 `WAIT` 动作。核心自动测试为
`19/19 PASS`。

使用阶段11相同的 medium 负载和固定种子完成 20 个任务回归，结果 `20/20 PASS`、
失败 0 次，四类任务各 5 次。每个跨楼层任务均保留 4 条不同楼梯候选；单次决策
合法候选数量为 16～344，所有决策重复编码完全一致，选中动作均可无歧义解码，
并且阶段11的队列、货物、任务和资源清理门禁继续通过。

第一轮结果证明“固定维度观测—合法组合动作—原始协作任务”链路已经接通。下一步
仍需完成更全面的异常状态动作掩码矩阵、观测归一化规格说明和可供学习策略直接读取
的样本数据，然后再决定是否进入策略网络与训练阶段。

## 十一、Medium等待过程修复（2026-08-06）

medium等待问题已完成修复和20任务双跑复验。时长模型由“所有机器人路程求和”
改为“协作关键路径”：取货 Carter 与 Go2 接近楼梯可并行，接货 Carter 可提前接近
出口，多台参与机器人完成配送后可并行驶向不同泊位。里程统计仍保持逐机器人累加，
因此不会牺牲能耗与负载统计的真实性。

在保持旧30～50秒发布间隔时，平均等待已由296.22秒下降到72.46秒，但平均服务
时间49.55秒仍高于平均到达间隔40秒，队列仍会缓慢积压。随后将 medium 重新标定为
55～75秒，最终平均等待降至5.64秒、P95降至20.13秒、最大等待32.61秒、队列峰值1。
20个任务全部完成，所有阶段11门禁继续通过。正式对比证据见
`results/stage11/stage11_medium_wait_optimization_report.md`。

## 十二、阶段12正式验收（2026-08-07）

阶段12剩余门禁已经补齐。异常动作矩阵覆盖取货 Carter、接货 Carter 和 Go2 的
不可用、故障、忙碌、带货状态，以及楼梯占用、错误楼层和同层任务错误调用 Go2，
共15类，动作掩码拒绝率100%。归一化规格已经固化，20组训练样本已导出为NPZ，
包含观测、候选动作特征、动作掩码、选中索引、回报和 `delta_time`。

修复后的medium回归为20/20；当前正式规格后续已更新为固定284维观测、
Context V2 `1536 × 28`动作张量、编码
确定性、动作可逆解码、每个跨层任务4楼梯候选及阶段11清理回归。因此阶段12训练
接口正式验收通过。下一阶段可直接进入策略网络和训练实验设计；若称为“MDP设计”，
应注意状态、动作、转移、奖励基础形式和重置边界已在阶段11完成，接下来主要是奖励
权重、时间感知折扣、训练终止条件、基线策略和学习算法的实验设计。

## 十三、任务模式与速度模型修正（2026-08-07）

原先以车—狗—车为默认转移链的描述不完整，现已按原始需求修正。任务覆盖一楼同
区域/跨区域、二楼同区域/跨区域、上行和下行六类；策略根据当前可行性选择单车、
单狗或车—狗—车。不存在车—车交接。同层任务只允许单车/单狗，跨层任务只允许
单狗/车—狗—车，无法到达源楼层的机器人不会成为候选。

Carter名义速度和SCAN-Planner上限更新为2.0m/s，Go2更新为0.8m/s。观测显式
编码速度，动作使用模式one-hot，并按ETA而非裸距离计算成本。正式六类任务回归
20/20通过，规则基线选择14次单车与6次车—狗—车；单狗模式保留为可学习候选，
但没有因少交接而在该固定序列中被错误地全部选中。
