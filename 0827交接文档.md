# 0827 交接文档：T-RO 排版重构、Overleaf 同步与后续论文工作

更新日期：2026-08-28  
目标期刊：IEEE Transactions on Robotics（T-RO）  
本会话职责：只处理论文写作、图表、引用和 Overleaf；下一阶段实验由另一个终端继续执行

## 1. 今日完成结论

本轮已对论文的分页、浮动体、宏命令空格和图中文字进行整体重构，新增强化学习验证轨迹，并把最终源文件与 Figure 2–8
同步到 Overleaf。Overleaf 当前可正常编译：**13 页、0 个编译错误**。当前交付 PDF 位于：

`examples/manuscript/main.pdf`

当前版本已经解决此前最严重的可读性问题：

- 首页标题与 Abstract 之间不再出现异常大空白；
- `SINGLE-CARand`、`SINGLE-CARaction`、`SINGLE-DOGcandidate`、`CAR–DOG–CARat` 等粘词已从宏定义层面修复；
- 第 8–10 页不再出现正文被拉到页面顶部、中部和底部、其间留出大块空白的情况；
- Figure 3 改为三种固定负载 PPO cohort 的综合 loss 与验证成功率轨迹，原有结果图顺延为 Figure 4–8；
- Figure 4–8 按编号连续出现，Table VII 不再通过手工计数器伪装在 `figure*` 中；
- Figure 2–8 的最终缩放字体已统一检查，Figure 2 的框线、箭头、机器人插图和节点文字在论文尺寸下可辨认；
- 取消了会在第二栏触发警告并干扰结论/参考文献流动的 `\balance`；
- Overleaf 网页端的 `main.tex` 和七个结果/框图 PDF 均已覆盖为本地最新版。

## 2. 当前文件与同步状态

### 2.1 论文主文件

- 主源文件：`examples/manuscript/main.tex`
- Overleaf 项目：`https://www.overleaf.com/project/6a7c1feea03e2b0827b8076c`
- Overleaf 最终 PDF：`examples/manuscript/main.pdf`
- Draw.io 源图：`examples/manuscript/figures/architecture_drawio.drawio`

### 2.2 已同步的图文件

- `figures/fig_architecture_drawio.pdf`
- `figures/fig_training_convergence.pdf`
- `figures/fig_stage19_paired_differences.pdf`
- `figures/fig_stage19_adaptation_recovery.pdf`
- `figures/fig_stage20_generalization.pdf`
- `figures/fig_stage21_latency_scaling.pdf`
- `figures/fig_stage21_fault_waiting_effects.pdf`

### 2.3 仓库状态

审阅时本地 `HEAD` 与 `origin/main` 均为：

`14110377c0607fe7deec0367513720f1874ffb7c`

论文目录存在多项未提交修改和新增数据/图表。后续操作不得用 `git reset --hard`、`git checkout --` 或
批量覆盖清除这些成果。尤其需要保留 `main.tex`、Draw.io 源文件、Figure 2–8、Stage 18/20/21 数据和绘图脚本。

## 3. 本轮排版修改

### 3.1 首页与宏命令

在 `main.tex` 中加入 `xspace`，并把方法名和三种运输模式定义为稳健命令：

```latex
\DeclareRobustCommand{\method}{E-SMDP-PPO\xspace}
\DeclareRobustCommand{\modecar}{\mbox{\textsc{Single-Car}}\xspace}
\DeclareRobustCommand{\modedog}{\mbox{\textsc{Single-Dog}}\xspace}
\DeclareRobustCommand{\modechain}{\mbox{\textsc{Car--Dog--Car}}\xspace}
```

正文中宏后直接接普通单词的位置又显式加入了不可断行空格，因此 `SINGLE-CARaction`、
`SINGLE-DOGcandidate`、`CAR–DOG–CARat` 等 PDF 文字层粘连也已消除。首页使用
`\IEEEaftertitletext` 收紧标题后间距，没有用负值过大的全局 `\vspace` 破坏正文版心。

### 3.2 浮动体策略

统一把跨栏浮动体设为 `[!t]`，调整顶部浮动比例和正文最低占比，并删除中间强制清空浮动队列的屏障。
现在只在 Conclusion 之后、bibliography 之前保留一个 `\FloatBarrier`：让 Limitations 和 Conclusion 填满前一页，
同时保证最后两张 Study III 证据图不会越过参考文献。

原来把 Table VII 和 Figure 5 塞进同一个 `figure*`、再手工修改 table counter 的写法已经删除。二者现在是
正常的独立 `table*` 和 `figure*`，标题元数据、编号和超链接语义均正确。Figure 7、Figure 8 也分别放到
各自分析段落附近声明，不再连续堆在 Study III 开头。

没有使用 `[H]`、`\clearpage`、`\newpage`、`stfloats` 或 `dblfloatfix`，也没有重新引入会造成大块空白的
章节级强制分页。

### 3.3 当前 Overleaf 页面分布

- 第 1 页：标题、Abstract、Introduction 连续排版，无标题后异常空白；
- 第 4 页：Table I、Figure 1、Figure 2 及后续方法内容，页面被充分利用；
- 第 7 页：Training Stability 正文与 Figure 3 验证轨迹；
- 第 8 页：Table IV，随后继续 Study I/II/III 正文；
- 第 9 页：Table V–VII 与 Figure 4；
- 第 10 页：Figure 5–6；
- 第 11 页：Study III 正文、Limitations 和 Conclusion 连续排版；
- 第 12 页：Figure 7–8，并开始 References；
- 第 13 页：References 续页。

若以后还要进一步降低“证据页较密集”的观感，应该从论文
结构上把协议明细或部分绝对结果表移入补充材料，而不是继续增加 `FloatBarrier` 或强行缩小图片。

## 4. 图表可读性修改

### Figure 2

- Draw.io 源文件中的标题、节点和注释字体整体增大；
- 三种 legal modes 改为明确的三行布局；
- 保持所有主连接线横平竖直；
- Carter 和 Go2 图片区域保留足够面积；
- PDF 采用裁边后的矢量输出，最终页面中边线清晰。

### Figure 3–8

- Figure 3 使用三种固定负载 cohort 的十种子中位数与 IQR，不做时间平滑；
- Figure 4 为 paired locked-test differences；
- Figure 5 的恢复面板注释缩短为 `Negative = queue clearance`，避免边缘裁切；
- Figure 6 保持“正值始终代表 mixed PPO 更好”的方向解释；
- Figure 7 明确 top-8 visibility cap 和 100 ms engineering budget；
- Figure 8 保留 time-greedy 与 single-dog 两类对照，但通过不同符号和分面避免误读。

## 5. 已完成的质量检查

- [x] Overleaf 在线编译 0 errors；
- [x] 本地 Tectonic 编译无未定义引用、未定义文献和 overfull hbox；
- [x] Overleaf 最终 PDF 下载后确认 13 页、Letter 页面尺寸，并回存为 `examples/manuscript/main.pdf`；
- [x] 对本地最终 PDF 的全部 13 页进行渲染和视觉检查；
- [x] 检查首页空白、表格裁切、图中文字、Figure 3–8 顺序和参考文献起始位置；
- [x] 检查四类已知宏粘词；最终视觉文本中均已分开；
- [x] Figure 2–8 的 Overleaf 文件与本地版本一致；
- [x] 本地和 Overleaf 当前均为 13 页，图号与章节顺序一致。
- [x] 2026-08-28 在线核验 Figure 3 已显示 `PPO objective loss`，旧的 return-change 纵轴已完全移除。

## 6. 下一步论文工作（实验结果回来后）

### P0：接入正在运行的下一阶段实验

另一个终端正在执行下一阶段实验，本会话不要重复启动训练。结果完成后，应按以下顺序接入论文：

1. 核对训练条件、测试种子、策略冻结规则和指标定义；
2. 优先加入 `duration-aware discount vs fixed gamma`、`context–candidate interaction vs flat scorer`、
   `no relay/fixed mode` 等算法级消融；
3. 使用配对或 crossed-bootstrap 95% CI，不只报告点估计；
4. 决定主文保留的 1 张消融图/表，其余放补充材料；
5. 新增图表后重新检查 Figure/Table 首次引用距离，避免再次形成跨栏队列。

### P1：闭环机器人证据

若目标仍是 T-RO，最有价值的新增证据是少量但真实闭环的 SCAN/Gazebo 随机跨层任务：PPO 选择
mode/robot/stair，规划器真实执行，实际导航时长与失败反馈回 scheduler。必须继续区分：

- 逻辑调度仿真；
- ROS/规划器闭环仿真；
- 接触动力学或硬件实验。

在没有新增闭环数据前，不得把当前工作写成硬件或动力学验证。

### P1：方法表达

后续可继续补强：

- 候选生成、资源互斥、所有权回滚和事件推进的伪代码；
- duration-aware SMDP update 与普通 fixed-step PPO 的对比说明；
- 1536 candidate slots 的推导和固定形状局限；
- 强规则或小规模优化基线，避免只与 time-greedy/single-dog 比较。

## 7. 后续编辑规则

1. Overleaf 是最终排版真源；修改后必须在线复编译并检查实际 PDF，而不能只依据本地 Tectonic 页数。
2. 新增跨栏图表前先评估当前 Figure/Table 队列；不要随意加入 `\FloatBarrier`、`\clearpage` 或 `[H]`。
3. 图中最小有效文字按最终页面尺寸检查，不能只看绘图软件中的原始大图。
4. 三个模式名统一使用 `\modecar`、`\modedog`、`\modechain`，不要在正文手敲不同拼写。
5. 实验结论继续围绕“等待与尾部等待改善、队列清理和运行区间覆盖”，不写成全指标普遍支配。
6. 当前 45 篇参考文献数量已经足够；只在新增方法、基线、协议或闭环证据确实需要时增加文献。

## 8. 强化学习优化与验证轨迹说明（2026-08-28 更新）

本轮新增 Figure 3，用于回答“强化学习是否训练稳定、是否收敛”的问题。数据来自 Stage 18 三个固定负载训练 cohort：

- Medium：`results/stage18/full_context_v2/seed_*/stage18_ppo.history.json`；
- Dense：`results/stage18_dense/full_context_v2/seed_*/stage18_ppo.history.json`；
- Burst：`results/stage18_burst/full_context_v2/seed_*/stage18_ppo.history.json`。

每个 cohort 含 10 个训练种子、2000 次 PPO update。全部 60,000 条 update 记录均包含有限的 `loss`、
`actor_loss`、`value_loss`、`entropy`、`approximate_kl`、`clip_fraction` 和 `gradient_norm`；三组 PPO 超参数一致。
代码中综合目标定义为 `loss = actor_loss + 0.5 * value_loss - 0.01 * entropy`，日志值与该公式一致。

Figure 3(a) 现改为 PPO 综合 loss：先在每个种子内对不重叠的 50-update 区间取中位数，再画十种子中位数与 IQR；
这种区间聚合与每 50 updates 一次的验证频率对齐，没有使用重叠滑动平滑。Figure 3(b) 保留验证任务成功率；
每个验证点包含每种子 100 个 profile-matched episode（2000 个任务），验证流与正式 locked test 完全分离。

关键读数：Medium loss 从首个区间的 0.142 降至终点 0.043；Dense 为 0.082→0.080；Burst 为
0.0126→0.0117。三类训练的验证成功率始终约在 96.45%–98.03% 范围。PPO 的 target 与 on-policy rollout
分布持续变化，而且三种负载产生的 rollout 不同，因此 loss 只用于判断各自的优化稳定性，不能按绝对值横向排名策略性能，
也不能单凭下降宣称策略收敛。正式 locked tests 仍使用预注册的 terminal checkpoint，并以任务指标作最终判断。

可复现文件：

- 绘图脚本：`examples/manuscript/scripts/make_training_convergence_figure.py`；
- 长格式数据：`examples/manuscript/figures/data/training_convergence.csv`；
- 数据校验：`examples/manuscript/figures/data/training_convergence_validation.json`；
- 矢量图：`examples/manuscript/figures/fig_training_convergence.pdf`。

## 9. 下一次接手时的最短检查清单

- [ ] 先查看另一个终端的新实验是否完成；
- [ ] 拉取/核对 GitHub 最新代码与 release 数据，但不要覆盖本地未提交论文成果；
- [ ] 读取本文件和 `0826交接文档.md`；
- [ ] 在 `examples/manuscript/main.tex` 中接入新实验结论；
- [ ] 重新生成对应矢量 PDF 图；
- [ ] 同步 Overleaf 并在线编译；
- [ ] 最后重点检查第 7–12 页浮动顺序、图中文字和首次引用距离。
