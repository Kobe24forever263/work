# Stage 26 循环记忆策略实验入口（2026-09-01）

## 1. 为什么进入 Stage 26

Stage 25 已完成严格在线因果观测（`CAUSAL_RELEASED_V4`）的长训练，但哨兵门禁未通过。其安全性合格，主要瓶颈是策略在规则基线判为 `SINGLE_DOG` 的上下文中只匹配约 17.6%，并且新种子平均回报低于规则基线。因此不继续扩展同一前馈架构的种子，而是测试能够保留历史信息的循环策略。

## 2. 本阶段唯一结构变化

- 保持：三种运输模式、混合负载连续场景、机器人位置不重置、严格 released-only 观测、TASK_KEYED 随机性、连续时间奖励、变量 SMDP 折扣。
- 新增：96 维 `GRUCell` 回合记忆。
- 记忆边界：每个 episode 开始清零，episode 内跨决策持续。
- 训练方式：完整回合 BPTT，不能把相邻决策随机打散。
- Stage 25 与更早阶段源文件保持冻结，Stage 26 使用独立代码、配置和结果目录。

## 3. 已完成的短门禁

短门禁只训练 1 个 episode，不评价收敛。结果：

| 检查项 | 结果 |
|---|---:|
| Stage 26 单元测试 | 2/2 通过 |
| 参数是否更新 | 通过，L2 变化约 0.119 |
| episode 开始记忆是否为 0 | 通过 |
| episode 内记忆是否变化 | 通过，独立验证平均 L2 约 0.085 |
| 新种子任务是否全部结算 | 160/160 |
| 非法动作 | 0 |
| 资源泄漏 | 0 |
| 短门禁结论 | 通过，可手动训练 seed 01 |

短训练的单狗上下文匹配率约 7.4%，低于正式门槛 20%。该值只说明随机初始化尚未收敛，不能据此判断循环策略最终有效或无效。

## 4. 正式长训练（必须由用户手动执行）

在工作区根目录运行：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage26_recurrent_long.py 1 --execute --confirm-long
```

中断后续训：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage26_recurrent_long.py 1 --resume-latest --execute --confirm-long
```

计划预算：2000 updates × 4 episodes/update = 8000 episodes，即 640000 个任务。只先训练 seed 01，不能直接启动 seed 02–10。

## 5. 权重与日志位置

- 最终权重：`results/stage26_recurrent_causal/long/seed_01/stage26_recurrent_ppo.pt`
- 最佳权重：`results/stage26_recurrent_causal/long/seed_01/stage26_recurrent_ppo.best.pt`
- 训练历史：`results/stage26_recurrent_causal/long/seed_01/stage26_recurrent_ppo.history.json`
- 训练摘要：`results/stage26_recurrent_causal/long/seed_01/stage26_recurrent_ppo.summary.json`
- 中间权重：`results/stage26_recurrent_causal/long/seed_01/checkpoints/`

### 5.1 种子队列扩展（2026-09-01）

seed 01 和 seed 02 已完成。下一个需要手动启动的是 seed 03：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage26_recurrent_long.py 3 --execute --confirm-long
```

一般中断可从最新的每 100 updates 检查点续训；当前 seed 03 的恢复应优先使用下文的批处理入口，避免误选仍保留的旧 update 2000 检查点：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage26_recurrent_long.py 3 --resume-latest \
  --execute --confirm-long
```

训练完成后用与 seed 01/02 相同的 40 个未见 episode 独立验证：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage26_recurrent_validation.py \
  results/stage26_recurrent_causal/long/seed_03/stage26_recurrent_ppo.pt \
  --seed-start 73250000 --episodes 40 \
  --output results/stage26_recurrent_causal/long/seed_03/fresh_validation.json
```

seed 03 的最终权重保存在 `results/stage26_recurrent_causal/long/seed_03/stage26_recurrent_ppo.pt`，最佳中间权重保存在 `results/stage26_recurrent_causal/long/seed_03/stage26_recurrent_ppo.best.pt`。只有 fresh-seed 验证通过后，该种子才会进入 RViz 自动择优候选集。

由于 seed 03 的一次重复启动截断了 history/best 文件，原始最终权重已先备份到 `results/stage26_recurrent_causal/long/seed_03/recovery_before_resume_20260902/`。下列手动脚本会先从与现有 600 条 history 精确对应的 update 600 检查点恢复 seed 03 至 update 2000，并将重建后的最终权重与原始备份做 SHA-256 一致性校验；通过后再串行训练 seed 04–10：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
./scripts/run_stage26_recurrent_cohort_04_10.command
```

该脚本会按 03（修复）→04→…→10 顺序执行，自动跳过已完成且验证通过的种子；seed 03 使用显式 update 600 检查点，其他存在检查点的种子使用 `--resume-latest`。每个种子完成后自动执行同一组 40 episode fresh-seed 验证。训练和验证日志保存在 `results/stage26_recurrent_causal/cohort_04_10_logs/`。中断后重新执行同一条命令即可续训。

只检查批处理计划而不启动时：

```bash
TASK2_DRY_RUN=1 ./scripts/run_stage26_recurrent_cohort_04_10.command
```

## 6. 长训练完成后的验收

运行：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage26_recurrent_sentinel_gate.py
```

哨兵门禁使用 40 个未参与训练的新 episode，要求：

1. 全部任务结算；
2. 非法动作与资源泄漏均为 0；
3. 循环记忆在任务过程中有效变化；
4. 三种模式都可见且都被选择；
5. 三类规则最优上下文的对角匹配率均不低于 20%；
6. 策略平均回报不低于规则基线。

全部通过后才允许扩展 seed 02–10；任何一项失败都先停下分析，不盲目追加训练。

## 7. 十种子正式 locked test

截至 2026-09-03，seed 01–10 均完成 `2000 updates / 8000 episodes`，并通过 `73250000–73250039` 的 40 个 fresh-seed 安全预验收。随后先冻结十个最终权重的 SHA-256，再统一使用从未参与训练、验证或权重选择的 `73350000–73350099` 共 100 个任务流执行正式 locked test。每个训练种子保留 100 条逐 episode 数据，共 1000 条策略记录；规则基线仅有 100 个独立任务流，跨训练种子复用用于配对统计，不按 1000 个独立样本计算。

正式结果为：策略平均回报 `-292.2447`，规则基线 `-391.9208`，策略提升 `+99.6761`，双向 crossed bootstrap 95% CI 为 `[+83.5653, +116.9099]`，精确双侧 sign-flip `p=0.0020`、Holm 校正后 `p=0.0078`，十个训练种子方向全部为正。成功率提升 `+0.385` 个百分点、成功吞吐量提升 `+0.243` 任务/小时，但二者 95% CI 均跨零，因此只能报告小幅数值改善，不能宣称显著提升。非法动作与资源泄漏均为 0，所有任务均完成结算。

追加 locked controls 后得到：相对四 Go2、仅 `SINGLE_DOG` 空白对照，完整循环策略平均回报提升 `+534.7480`，95% CI `[+491.4222, +580.2828]`，Holm `p=0.0078`；平均仿真时长减少 `23.7272 s`，95% CI `[-31.7731, -16.6013]`，Holm `p=0.0078`。单狗对照的 8000 个任务中完成 7784、失败 59、测试窗口结束时未完成 157，所有任务均被计入，且非法动作与资源泄漏为 0。

但是，相对“使用同一权重、每个决策前清空 GRU hidden state”的推理干预，完整循环策略平均回报反而低 `6.3808`，95% CI `[-12.0030, -1.4699]`，Holm `p=0.0234`；10 个训练种子中仅 1 个方向有利于保留记忆。成功率和成功吞吐量的差异不显著。因此 Stage 26 可以支持“多智能体调度相对规则与单狗方案显著降低综合代价”，但**不能**支持“跨决策循环记忆带来性能提升”。该干预不是独立重训的架构消融，后续仍需在新训练/测试命名空间中验证时间感知记忆或受控重置方案，且不得重复使用本组 locked-test 结果做模型选择。

运行与汇总入口：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/run_stage26_recurrent_locked_test.py --confirm-locked-test
/Users/lab4099/Desktop/Mujoco/task2_handover/.pixi/envs/default/bin/python \
  scripts/summarize_stage26_recurrent_locked_test.py
```

可审计结果位于 `results/stage26_recurrent_causal/locked_test_v1/`，其中包括冻结清单、十个逐 episode JSON、种子级 CSV、统计 CSV/JSON 和中文质量审查报告。locked-test 结果不得用于再次选择或调整这十个权重。

## 8. 可审计文件

- 协议：`src/warehouse_bringup/config/experiment_stage26_recurrent_causal.yaml`
- 循环网络：`src/warehouse_core/warehouse_core/stage26_recurrent_policy.py`
- 序列 PPO：`src/warehouse_core/warehouse_core/stage26_recurrent_ppo.py`
- 短门禁：`scripts/run_stage26_recurrent_interface_gate.py`
- 新种子验证：`scripts/run_stage26_recurrent_validation.py`
- 长训练入口：`scripts/run_stage26_recurrent_long.py`
- 哨兵门禁：`scripts/run_stage26_recurrent_sentinel_gate.py`
- 正式 locked test：`scripts/run_stage26_recurrent_locked_test.py`
- 正式统计汇总：`scripts/summarize_stage26_recurrent_locked_test.py`
- locked controls：`scripts/run_stage26_recurrent_locked_controls.py`
- controls 统计汇总：`scripts/summarize_stage26_locked_controls.py`
- 短门禁结果：`results/stage26_recurrent_causal/gates/stage26_recurrent_interface_gate.json`
- 正式 locked-test 结果：`results/stage26_recurrent_causal/locked_test_v1/`

## 9. 单任务随机 RViz 验收

该入口只用于观察强化学习策略对**一个随机任务**的决策和执行过程。每次启动时从当前六类任务中随机抽取一个任务，自动扫描已经完成且通过门禁的 Stage 26 种子，并选择最终评估平均回报最高的权重。任务完成后保持终点状态，不会发布第二个任务。

运行：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
./scripts/run_stage26_random_single_task_rviz.command
```

需要复现同一随机任务时，在命令末尾给出任务随机种子：

```bash
./scripts/run_stage26_random_single_task_rviz.command 74001005
```

如只想加快动画播放而不改变路径和策略决策，可设置时间倍率：

```bash
TASK2_TIME_SCALE=1.5 ./scripts/run_stage26_random_single_task_rviz.command
```

验收画面包括仓库三维点云、全部 10 个智能体（4 辆一楼 Carter、4 只 Go2、2 辆二楼 Carter）的完整 URDF 本体、起终点、策略规划路径、橙色货物及其吸附/交接/卸货状态。仍只发布一个随机任务，未被策略选中的机器人保持在待命位。左上方状态文字显示所选 Stage 26 种子、策略运输模式和当前阶段。终点状态应显示“单任务验收完成；不会继续发布任务”。关闭 RViz 窗口即可结束全部进程。

每次生成的可审计计划保存在：

`results/stage26_recurrent_causal/rviz_single_task/latest_plan.json`

其中记录随机任务、候选种子及选中依据、策略动作概率、规则基线参考、使用的机器人、全部路径分段和货物状态。当前如果只有 seed 01 完成长训练与门禁，程序会自动使用 seed 01；以后其他种子完成后会自动重新比较，不需要修改脚本。

该画面验收的是 Stage 26 强化学习调度结果与任务语义。路径沿用已经验收的任务点和楼梯几何进行运动学回放，不替代 SCAN-Planner 可达性、Gazebo 动力学或真实机器人控制验收。

## 10. 连续 10 任务真实 SCAN-Planner RViz 验收

> 2026-09-01 范围更新：本节已降级为**可选的低层规划器联调**，不再作为 Stage 26 强化学习策略的验收门禁，也不再要求连续 10 任务达到 10/10 通过。Stage 26 策略验收以 fresh-seed 安全性、上下文模式切换、相对规则基线改善和无非法动作/资源泄漏为准。

第 8 节的单任务入口是运动学策略回放，**没有启动 SCAN-Planner**，因此不会产生局部雷达点云。需要检查低层规划器与 Stage 26 策略衔接时，使用本节的新入口：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
./scripts/run_stage26_scan_campaign_rviz.command
```

该入口随机生成 10 个任务并依次发布，任意时刻最多执行一个任务。上一任务完成后才发布下一任务；机器人位置和 96 维 GRU 记忆均不在任务之间重置。策略仍自动选择“完成 2000 updates 长训练且通过独立 fresh-seed 验证”的种子中平均回报最高者。需要复现同一批任务时，可提供 campaign seed：

```bash
./scripts/run_stage26_scan_campaign_rviz.command 76001001
```

执行边界如下：

- 高层策略在每次发布后选择 `SINGLE_CAR`、`SINGLE_DOG` 或 `CAR_DOG_CAR`，不固定运输模式；
- 大于等于 2 米的导航和楼梯路段启动真实 `scan_planner_node`、PCD 激光雷达仿真和开环执行器；
- 小于 2 米的交接贴近/对齐段明确标记为 `POSE_SYNC`，因为 SCAN 对这一距离的轨迹可能低于动态可行性下限；它不是 SCAN 成功记录；
- RViz 显示全部 10 台 Carter/Go2 完整 URDF，并把当前活动机器人的 `/cloud`、`grid_map/occupancy`、`grid_map/occupancy_inflate`、滑动地图边界和已走路径中继到固定显示话题；
- 每个任务独立显示起点、终点、货物吸附/交接/卸货以及当前策略模式；
- 任一真实 SCAN 路段超时会立即停止后续任务并显示失败，不会用运动学回放代替该路段；
- 送货后的清场路线先反向复用已经通过 SCAN 的进场/送货路段，再返回待命位，避免在装卸货平台旁生成“超短直线 + 90° 转弯”导致加速度门禁失败；
- 若主动执行本可选联调，完成后终端会出现 `STAGE 26 SCAN CAMPAIGN PASS: 10/10 tasks resolved`；该结果不参与 Stage 26 学习策略成败判定。

如果某段真实 SCAN 超时，修复后可使用原 campaign seed 从失败段续跑，不必重复前面已验收的路段。例如 campaign `76791501` 的任务 1 第 3 段：

```bash
cd /Users/lab4099/Desktop/Mujoco/work
TASK2_START_TASK=1 TASK2_START_SEGMENT=3 \
  ./scripts/run_stage26_scan_campaign_rviz.command 76791501
```

断点续跑仍会重新生成同一随机种子的冻结任务清单，并从指定段恢复货物状态。续跑后的后续任务仍顺序执行。

计划和运行日志位置：

- 10 任务计划：`results/stage26_recurrent_causal/rviz_scan_campaign/latest_campaign.json`
- 生成的 SCAN waypoint：`results/stage26_recurrent_causal/rviz_scan_campaign/routes/`
- 每段 SCAN 原始日志：`results/stage26_recurrent_causal/rviz_scan_campaign/runtime_logs/<campaign_seed>/`

固定种子 `76001001` 的预生成检查包含 10 个任务、52 个真实 SCAN 路段和 4 个短距离 `POSE_SYNC` 路段，三种运输模式均被同一策略选出。该入口提供 RViz/SCAN 运动学闭环证据，但仍不等于 Gazebo 动力学或真实硬件实验。

截至 2026-09-03，seed 01–10 均完成长训练、40-episode fresh-seed 预验收和 100-episode 正式 locked test。RViz 演示仍只依据训练/预验收命名空间中的既定选择规则选取权重，不读取 locked-test 结果进行事后挑选。
