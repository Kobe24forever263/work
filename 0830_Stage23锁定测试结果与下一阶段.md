# 0830 Stage 23 锁定测试结果与下一阶段

更新日期：2026-08-30  
目标期刊：IEEE Transactions on Robotics（T-RO）  
本文件范围：只记录实验协议、冻结证据、正式结果、结论边界和下一阶段实验；不负责论文改写。

## 1. 本阶段结论

Stage 23 已完成从训练审计、10 权重冻结、非正式 smoke 到一次性正式 locked test 的闭环。

正式结果支持以下受限结论：在修复后的 `MARKOV_CONTEXT_V3 + CONTINUOUS_TIME_V3` 逻辑持续调度模拟器中，冻结的 Stage 23 策略相对 time-greedy rule 显著降低平均 episode-level P95 等待时间，同时在本批独立测试流上获得更高的成功率和成功吞吐量点估计及 95% crossed-bootstrap 区间。

不得扩张为以下结论：

- 不得声称 ROS、SCAN-Planner、Gazebo、动力学或硬件性能已经得到验证；
- 不得声称 success/throughput 已通过非劣检验，因为正式测试前没有冻结工程非劣界值；
- 不得声称 10 个训练种子逐一支配规则基线；本结果是预先定义的训练种子总体平均效应；
- 不得复用本批 locked seeds 选择新 checkpoint、修改模型或调参。

## 2. 冻结协议

### 2.1 权重选择

10 个预声明训练种子全部保留，不因训练结果或诊断标签而剔除。正式测试使用验证集上 `mean raw episode reward` 最大的 checkpoint；完全相同的并列值取最早 update。

冻结 update 依次为：

`[100, 100, 200, 100, 400, 100, 100, 300, 100, 500]`

这组结果确认了 terminal checkpoint 不是合适的正式代表；多数训练在 500 update 之后出现性能漂移。因此 Stage 23 的正式策略是预先由验证流选择的 best checkpoint，而不是 2000 update terminal checkpoint。

### 2.2 冻结门

冻结门检查并保存：

- 10 个 best/final checkpoint 的 SHA-256；
- 2000 条 update 历史、20 次验证和 40 个周期 checkpoint 的完整性；
- 训练、验证、正式测试与 bootstrap seed 的互斥；
- 模型、encoder、reward normalizer、RNG 和 optimizer 状态；
- 状态充分性构造审计、event-partition 审计、接口门和 exact-resume 门；
- 训练源码、正式评估源码、训练协议和统计协议的 SHA-256；
- 每个 best checkpoint 对三种运输模式的上下文软门。

冻结门结果为 `passed=true`。seed 02 的 strict imitation diagnostic 未达到 0.90，但其接口安全门通过；按照预声明的 admission rule 允许进入 PPO，且在冻结时保留，不做事后剔除。

## 3. 正式测试设计

- 训练种子：10；
- 正式独立测试流：`71100000–71100099`，共 100 条；
- 每条测试流：80 个连续到达任务；
- 状态：任务之间不重置机器人位置，phase 之间不重置；
- phase：NORMAL、DENSE、BURST、RECOVERY 的随机持续课程；
- 对照：`TIME_GREEDY_RULE` 与 `SINGLE_DOG_ONLY`；
- handover：`TASK_KEYED_COMMON_RANDOM_NUMBERS`；
- 主指标：先在每条 80-task 测试流内计算 P95 waiting，再跨 10×100 crossed factors 汇总；
- 置信区间：10,000 次 crossed bootstrap，训练种子与共享测试流分别有放回重采样；
- bootstrap seed：`71200000`；
- guardrails：success rate、successful throughput；只做描述性 CI，不作非劣声明。

正式评估共包含 12 个方法实例、1200 个 episode 和 96,000 条任务记录。所有方法共用一个 task-stream fingerprint 和一个 handover-potential fingerprint；非法动作和资源泄漏均为 0。

## 4. 正式结果

### 4.1 与 time-greedy rule 比较

| 指标 | Stage 23 | Time-greedy | 策略减基线 | 95% crossed-bootstrap CI | 方向 |
|---|---:|---:|---:|---:|---|
| Mean episode P95 waiting (s) | 453.124 | 592.272 | -139.149 | [-176.149, -90.678] | 策略更优 |
| Success rate | 97.530% | 96.7125% | +0.8175 pp | [+0.3950, +1.2725] pp | 策略更优 |
| Successful throughput (tasks/h) | 68.724 | 68.139 | +0.585 | [+0.298, +0.893] | 策略更优 |
| Mean waiting (s) | 107.240 | 138.443 | -31.203 | [-39.044, -21.403] | 策略更优 |
| Mean flow time (s) | 151.265 | 181.670 | -30.405 | [-38.502, -20.360] | 策略更优 |
| Mean episode P95 flow (s) | 504.294 | 623.199 | -118.905 | [-154.034, -75.212] | 策略更优 |
| Distance/task | 64.268 | 72.214 | -7.946 | [-11.477, -4.521] | 策略更优 |
| Raw episode reward | -256.569 | -352.236 | +95.667 | [+66.078, +118.756] | 策略更优 |

相对 time-greedy，主指标降低约 23.5%，平均等待降低约 22.5%。主指标在 10 个策略种子中有 9 个优于规则基线；seed 10 的主指标比规则高约 41.8 s，因此不能写成“每个 seed 都获益”。

### 4.2 与 single-dog blank control 比较

| 指标 | Stage 23 | Single-dog | 策略减基线 | 95% crossed-bootstrap CI | 解释 |
|---|---:|---:|---:|---:|---|
| Mean episode P95 waiting (s) | 453.124 | 1136.951 | -683.827 | [-771.509, -599.095] | 策略明显更优 |
| Success rate | 97.530% | 97.1875% | +0.3425 pp | [-0.5437, +1.3150] pp | 未检测到清晰差异 |
| Successful throughput (tasks/h) | 68.724 | 68.121 | +0.603 | [-0.054, +1.336] | 未检测到清晰差异 |
| Distance/task | 64.268 | 46.313 | +17.955 | [+14.419, +21.404] | single-dog 距离更低 |

Single-dog 的低距离来自只移动一种载体，不代表其服务质量更好：其尾部等待约为 Stage 23 的 2.5 倍，且有 225 条任务因受限模式没有可行运输链而被显式记为 `FAILED/TERMINAL_UNRESOLVED`。

### 4.3 模式使用

80,000 个 Stage 23 决策任务的模式份额为：

- `SINGLE_CAR`：49.37%；
- `SINGLE_DOG`：26.09%；
- `CAR_DOG_CAR`：24.54%。

Relay 份额随压力增加：RECOVERY 21.90%、NORMAL 23.42%、DENSE 25.61%、BURST 27.38%。该结果目前只能说明条件相关性；若要解释策略偏好，还需同时统计每个 phase 的 relay feasible opportunity rate 与 conditional relay selection rate。

## 5. 数据质量与统计边界

质量门全部通过：

- campaign 完成；
- 10 个冻结策略文件齐全；
- 100 个正式共享测试 seed 齐全；
- task 与 handover 指纹完全一致；
- 每个 episode 恰有 80 条唯一任务终态记录；
- 0 illegal action；
- 0 resource/cargo leak；
- primary endpoint 与 crossed-bootstrap 口径在读取正式结果前已经冻结。

统计解释：任务行是 episode 内的聚类观测，不能把 80,000 条任务当作 80,000 个独立重复。独立训练单位为 10 个 policy seeds，共享环境单位为 100 个 locked streams。当前 95% CI 使用 percentile crossed bootstrap。由于没有预注册 success/throughput 非劣界值，二者只报告差值与区间。

## 6. 复现文件

- 训练协议：`src/warehouse_bringup/config/experiment_seeds_stage23_markov_continuous.yaml`
- 正式分析协议：`src/warehouse_bringup/config/experiment_stage23_locked_test.yaml`
- 冻结脚本：`scripts/run_stage23_policy_freeze_gate.py`
- 冻结清单：`results/stage23_markov_continuous/freeze/stage23_policy_freeze_manifest.json`
- 正式执行器：`scripts/run_stage23_locked_test.py`
- 汇总器：`scripts/summarize_stage23_locked_test.py`
- 正式任务级结果：`results/stage23_markov_continuous/locked_test_v1/`
- 正式汇总：`results/stage23_markov_continuous/stage23_locked_test_summary.json`

## 7. 下一阶段：Stage 24 强优化基线

根据 2026-08-28 T-RO 模拟审稿，状态充分性与 continuous-time return 两个形式化阻断项已经在 Stage 23 修复并重新训练；当前最优先的剩余实验缺口是 objective-matched 强调度基线。

Stage 24 建议先实现一个 causal rolling-horizon optimization baseline，而不是继续增加 PPO update：

1. 与 PPO 共用完全相同的候选生成器、feasibility mask、机器人/楼梯状态、任务可见范围和 task-keyed handover 条件；
2. 每个事件只使用当前可观测信息，不读取未来任务或已实现的交接时长；
3. 在可配置短视窗内联合选择任务与合法 assignment，目标与 `CONTINUOUS_TIME_V3` 的等待、失败、距离和交接风险保持可解释映射；
4. 固定求解预算并记录 p50/p95/p99 wall-clock latency、最优性 gap、timeout 和 fallback 次数；
5. 先用非正式 seeds 验证可行性与计算预算，再为强基线冻结新的独立测试 seeds；
6. 比较 Stage 23 PPO、time-greedy、single-dog 与 rolling optimizer，主指标继续使用 episode-level P95 waiting；
7. 强基线完成后再进入 corrected-contract 核心组件消融与未见布局/队伍比例鲁棒性。

Stage 24 的验收标准：强调度基线在全部测试流上无非法动作、无资源泄漏、与 PPO 共用任务/交接指纹，并在预定计算预算内完成至少 99% 的决策；无论其性能是否超过 PPO都完整报告，不能按结果筛选基线参数。
