# Stage 26 正式测试结论与下一阶段

日期：2026-09-03

## 1. 已完成证据

Stage 26 的十个独立训练种子均完成 2000 updates / 8000 episodes。十个最终权重在正式测试前以 SHA-256 冻结，随后统一使用 73350000–73350099 共 100 个未见任务流测试。逐 episode 原始结果、冻结清单和统计输出保存在 `results/stage26_recurrent_causal/locked_test_v1/`。

完整循环策略相对规则基线的平均回报提升 99.6761，crossed bootstrap 95% CI 为 [83.5653, 116.9099]，精确 sign-flip p=0.0020，Holm 校正 p=0.0078。十个训练种子回报改善方向全部一致。成功率和成功吞吐量仅为小幅数值改善，置信区间跨零。

相对四 Go2、仅允许 SINGLE_DOG 的空白对照，完整策略平均回报提升 534.7480，平均仿真时长减少 23.7272 秒，两项 Holm 校正 p 均为 0.0078。单狗对照没有非法动作或资源泄漏，但在 8000 个任务中有 59 个失败和 157 个窗口结束时未完成任务。

## 2. 阻断性结果

同一批训练权重在推理时每次决策清空 GRU hidden state 后，平均回报为 -285.8639；保留循环记忆时为 -292.2447。完整记忆相对清空记忆的差值为 -6.3808，95% CI 为 [-12.0030, -1.4699]，Holm 校正 p=0.0234。10 个训练种子中仅 1 个在保留记忆后回报更高。

因此当前证据拒绝“Stage 26 的跨决策 GRU 记忆提高性能”这一表述。可以保留的结论是：Stage 26 策略整体相对规则基线和单狗空白对照显著改善综合代价；不能把改善归因于循环记忆。

## 3. 实现审计

训练代码确实在每次 PPO 更新时从 episode 起点的零状态开始，按完整 episode 顺序重建 hidden state，并通过整段序列执行 BPTT；当前没有发现 hidden state 被错误 detach 后用于训练的问题。

待验证的机制假设是时间尺度失配：每个 episode 有 80 个任务并跨越约 4000 秒，当前 GRU hidden state 只在 episode 开始时清零，不根据 SMDP 的 `delta_time` 衰减，也不在 NORMAL、DENSE、BURST、RECOVERY 阶段变化时受控重置。即时观测本身已经包含较丰富的队列与资源状态，长期累积的旧信息可能成为干扰。该解释目前是推断，不能作为论文事实。

## 4. 下一阶段任务

下一阶段先做短诊断，不立即追加十种子长训练：

1. 在新的诊断种子上记录每个决策的 hidden-state 范数、更新幅度、时间间隔、负载阶段和动作变化；
2. 比较正常记忆、每步清空、按时间指数衰减、阶段切换重置四种推理干预；
3. 检查性能下降是否集中在长空闲间隔、阶段切换或 hidden-state 饱和区间；
4. 根据诊断确定 Stage 27 架构：优先考虑使用 `exp(-delta_time/tau)` 的时间感知记忆衰减，并把 `delta_time` 显式输入记忆单元；
5. 仅在短门禁显示记忆干预相对无记忆具有稳定正向趋势后，才由用户手动启动新的长训练。

## 5. 验收标准

短诊断必须使用与 Stage 26 locked test 不重叠的新种子，不得根据 73350000–73350099 调参。进入长训练前至少满足：

- 非法动作和资源泄漏均为 0；
- 所有任务被完整计入完成、失败或未完成；
- 时间感知记忆相对每步清空记忆的平均回报方向为正；
- 改善不是由单个任务流或单个负载阶段驱动；
- 训练、诊断、最终新 locked-test 种子三者严格不重叠。

Stage 25 目前只有一个完整长训练种子，且其训练摘要 `passed=false`，只能作为探索性诊断，不能进入与 Stage 26 十种子同等级的正式显著性比较。
