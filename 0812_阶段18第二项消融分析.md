# 8月12日：阶段18第二项消融分析

## 结论

第二项消融 `no_handover_cues` 已完成10个配对训练种子。该消融只将候选动作特征中的“交接次数”和“预测交接超时概率”置零，候选动作、状态转移、奖励、动力学和其他状态保持不变。

以每个训练种子的最佳validation checkpoint进行统一复核后，完整模型与该消融没有稳定差异。因此，当前MEDIUM负载下不能声称这两个显式交接提示带来显著性能提升，也不能声称它们有害。

## 最佳检查点配对结果

| 指标 | 完整模型 | 去除交接提示 | 配对差值（完整－消融） | 配对bootstrap 95%区间 |
|---|---:|---:|---:|---:|
| 平均回报 | 18.7270 | 18.7116 | +0.0154 | [-0.0325, 0.0715] |
| 成功率 | 97.790% | 97.735% | +0.055个百分点 | [-0.100, 0.220]个百分点 |
| 吞吐量（task/h） | 56.0206 | 56.0199 | +0.0006 | [-0.0365, 0.0381] |
| 平均仿真时间 | 1285.2424 | 1285.2577 | -0.0153 | [-0.8814, 0.8458] |
| 参考模式一致率 | 86.785% | 87.235% | -0.450个百分点 | [-1.820, 0.910]个百分点 |

所有区间均跨过0。20个最佳检查点评估中，非法动作和资源泄漏均为0。

## 运输模式分布

20,000个validation任务中：

| 条件 | 单车 | 单狗 | 车－狗－车 |
|---|---:|---:|---:|
| 完整模型 | 9990 | 5632 | 4378 |
| 去除交接提示 | 9992 | 5543 | 4465 |

两种策略的运输模式分布接近。去除提示后仅多87次车－狗－车、少89次单狗，没有形成明显策略坍缩。

## 为什么最终权重曾显示消融更好

直接读取训练结束时的最终权重，曾得到完整模型回报比消融低0.1945，且区间不跨0。但每个种子的最佳更新位置差异很大：完整模型best update从250到1950不等；消融从50到1850不等。训练到2000 updates后存在不同程度的策略漂移。

论文约定是用固定validation seeds选择每个种子的best checkpoint，再在锁定test seeds上比较。因此，最终权重结果只能作为训练稳定性诊断，不能作为消融主结论。最佳检查点复核表明先前的反向差异主要来自末期漂移。

## 工具修复

通用评估脚本此前加载消融权重时没有将检查点的 `observation_variant` 传给环境。训练过程和训练摘要不受影响，但旧脚本不能正确重评消融best checkpoint。现已修复，并新增十种子最佳检查点配对评估脚本。

## 科研解释边界

当前证据仅来自MEDIUM、最大同时活跃任务数为2的checkpoint-selection validation集。显式交接次数和超时概率可能与运输模式、估计代价等其他动作特征存在信息冗余，其价值更可能在DENSE、BURST、交接超时风险变化或跨楼层任务比例升高时体现。

正式T-RO结论需等待：

1. 所有消融训练完成；
2. 每个种子的best checkpoint锁定；
3. 在锁定test seeds和预注册压力场景上一次性评估；
4. 执行配对、分层bootstrap并报告效应量与区间。

## 下一步建议

继续第三项MEDIUM消融 `no_persistent_position`。该消融对应“机器人位置不重置、任务连续到达”的核心建模假设，理论上比两个显式交接提示更可能影响持续调度。与此同时，后续DENSE实验应复测 `no_handover_cues`，判断交接提示是否只在高负载时有贡献。
