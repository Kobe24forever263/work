# 0826 交接文档：T-RO 全文审阅、证据边界与后续实验路线

更新日期：2026-08-26  
目标期刊：IEEE Transactions on Robotics（T-RO）  
当前主线：双层仓储中 Carter–Go2 异构机器人团队的事件驱动 SMDP 调度、动态运输模式选择与持续负载适应

## 1. 今日结论

当前工作已经越过“PPO 能否训练起来”的阶段，进入正式论文实验与论证补强阶段。Stage 19、20、21
已经分别提供了持续非平稳负载、mixed-curriculum 泛化、固定输入形状下的调度延迟和逻辑故障压力测试。
后续工作的重点不应是简单增加 PPO 更新次数，而应依次完成：

1. 把现有 Stage 18/20 证据正式写入论文并补齐图表；
2. 做能够解释方法必要性的算法级消融；
3. 增加强规则、优化或学习基线；
4. 做未见布局、团队构成和任务分布的零样本泛化；
5. 完成一批调度器真实驱动低层规划器的闭环仿真实验；
6. 补充候选集合、资源互斥和任务所有权方面的形式化说明。

论文最稳妥的中心结论是：**在成功率和吞吐保持竞争力的同时，事件驱动 masked SMDP-PPO 能够通过
动态运输模式和共享资源调度降低平均等待与尾部等待，并在持续负载变化后清理积压。** 不应把论文写成
“PPO 在所有成功率和吞吐指标上全面碾压 greedy”。

## 2. 当前仓库与论文基线

### 2.1 关键位置

- GitHub 工作区：`/Users/lab4099/Desktop/Mujoco/work`
- 论文主文件：`examples/manuscript/main.tex`
- Overleaf 本地镜像：`/Users/lab4099/Documents/Codex/2026-08-10/yu/outputs/TRO_overleaf_draft/main.tex`
- 当前 Git HEAD：`14110377c0607fe7deec0367513720f1874ffb7c`
- 当前 `origin/main`：`14110377c0607fe7deec0367513720f1874ffb7c`

截至今日审阅时，仓库存在下列未提交成果，后续工作不得覆盖或删除：

```text
 M examples/manuscript/README.md
 M examples/manuscript/main.tex
?? examples/manuscript/TRO_Overleaf_Draft_stage21.zip
?? examples/manuscript/data/stage20/
?? examples/manuscript/data/stage21/stage21_figure_validation.json
?? examples/manuscript/figures/fig_stage21_fault_waiting_effects.pdf
?? examples/manuscript/figures/fig_stage21_latency_scaling.pdf
?? examples/manuscript/scripts/make_stage21_figures.py
```

### 2.2 当前稿件状态

- 当前约 11 页，45 篇参考文献均已被正文引用；引用数量不是当前短板。
- 摘要约 200 词，已到 T-RO 摘要上限，后续修改摘要必须重新计数并保持不超过 200 词。
- T-RO 首次投稿上限为 18 页；预计补完图表和消融后控制在 14–16 页较合适。
- 当前本地论文与 Overleaf 镜像内容一致；后续修改前仍应先比较文件，避免双向编辑造成覆盖。

## 3. 当前方法主线

当前方法由以下部分构成：

- 场景：双层仓储、6 台 Carter、4 台 Go2、4 处楼梯和共享交接区域；
- 决策模型：事件驱动连续时间 SMDP；
- 高层动作：联合选择任务、运输模式、参与机器人和楼梯；
- 三种运输模式：`SINGLE_CAR`、`SINGLE_DOG`、`CAR_DOG_CAR`；
- 输入：284 维系统状态和 30 维候选特征；
- 候选规模：最多 8 个可见任务、10 台机器人、4 处楼梯，共最多 1536 个候选槽；
- 策略：动态 feasibility mask、context–candidate actor、duration-aware discounting 和 warm start；
- 状态持续性：机器人位置、楼层、载荷、任务所有权、队列、楼梯与交接资源在决策事件之间持续保留；
- 作用边界：PPO 负责高层调度，导航和机器人底层控制由外部规划器/技能执行。

建议把贡献改写为 3 项方法贡献和 1 项实验贡献。当前“多阶段评估协议”不宜与算法组件并列成方法创新。

## 4. 已有实验与可用证据

### 4.1 Stage 18：固定负载与上下文特征消融

Stage 18 已经完成 MEDIUM、DENSE、BURST 多训练种子完整策略和 fresh locked tests。最终最稳定的
消融结论是：**ETA/cost context 具有最明确的独立贡献**。

必须注意：早期 Medium checkpoint-selection validation 中，`no_persistent_position` 曾出现
`SINGLE_DOG` 使用率降为 0 的 mode collapse；但最终 fresh locked high-load v4 并未稳定复现位置特征的
独立显著作用。因此只能写成诊断性观察，不能写成确认性因果结论。

建议正文表述：

> Early validation revealed a qualitative mode-collapse case after removing persistent position features; however,
> the final locked high-load tests did not consistently reproduce an independent position effect. We therefore treat
> this observation as diagnostic rather than confirmatory.

正式数据位置：

`results/stage18_clean_ablation_locked_test_v4/stage18_clean_ablation_locked_test_v4_statistics.json`

现有条件包括：

- `no_eta_cost_only`
- `no_position_only`
- `no_history_only`
- `no_explicit_queue_resource`
- `no_explicit_handover_risk`

### 4.2 Stage 19：持续非平稳负载

协议：30 个固定负载训练策略（MEDIUM/DENSE/BURST 各 10 个种子）在 100 个共享 locked test streams
上运行 `NORMAL → DENSE → BURST → RECOVERY`，形成 3,000 个 PPO episodes 和 240,000 条 PPO 任务记录。

关键绝对结果：

| 方法 | 成功率 | 吞吐（task/h） | 平均等待（s） | P95 等待（s） | 距离/任务 |
|---|---:|---:|---:|---:|---:|
| MEDIUM PPO | 99.159% | 71.62 | 166.02 | 659.34 | 50.87 |
| DENSE PPO | 97.990% | 70.80 | 124.96 | 519.09 | 60.91 |
| BURST PPO | 98.066% | 70.85 | 123.03 | 503.34 | 60.07 |
| time-greedy | 97.087% | 70.16 | 173.38 | 727.11 | 71.09 |
| single-dog | 96.938% | 69.82 | 330.87 | 1286.68 | 45.23 |

DENSE/BURST PPO 相对 time-greedy 的平均等待分别减少约 48.42 s 和 50.36 s，P95 等待分别减少
约 208.02 s 和 223.77 s。MEDIUM 的平均等待差置信区间跨 0，不应宣称其平均等待显著改善。

MEDIUM/DENSE/BURST 指**训练负载 profile**，不是三个不同测试环境。三组策略最终面对的是同一套
持续测试流。MEDIUM 策略表现为高成功、低距离、较高等待的保守权衡；DENSE/BURST 策略更积极地
使用资源降低积压，但现有 mode share 只能提供机制线索，不能单独证明因果关系。

### 4.3 Stage 20：mixed-curriculum 泛化

协议：10 个 mixed-curriculum 策略、100 个 fresh shared seeds，共约 320,000 条 policy tasks。

- 成功率：96.514%
- 吞吐：68.401 task/h
- 平均等待：116.60 s
- episode P95 等待：466.18 s
- 距离/任务：72.12

相对 time-greedy：

- 成功率 `+0.039 pp`，95% CI 跨 0；
- 吞吐 `+0.048 task/h`，95% CI 跨 0；
- 回报 `+89.46`；
- 平均等待 `-30.14 s`；
- P95 等待 `-132.82 s`。

正确结论是：**一个 generalist 能覆盖随机混合运行条件并保持成功率/吞吐竞争力，同时明显降低等待，
但它并不在所有匹配条件下全面超过 fixed-load specialists。** 论文可使用“coverage across operating
regimes rather than universal dominance”。当前最大缺口是 Stage 20 尚无独立主图。

### 4.4 Stage 21：固定形状延迟与逻辑故障

延迟实验覆盖团队规模 4/7/10、楼梯数 1/2/4、队列长度 1/2/4/8/16/32，共 54 个场景；每个组件
测量 500 次。最坏端到端 p99 为 4.815 ms。

必须写成“fixed-shape latency characterization”，不能写成任意规模扩展性证明，因为模型仍固定为
10 robots、4 stairs、top-8 visible tasks 和 1536 candidate slots。

逻辑故障实验包含 control、navigation failure、stair outage 和 robot outage：10 policies × 100 seeds ×
4 scenarios，共 4,000 个 PPO episodes。相对 time-greedy，四种场景的平均等待均减少约 30–34 s，P95
等待减少约 123–139 s；navigation failure 下成功率下降约 8.04 个百分点。

这些结果只能说明调度逻辑在预定义故障窗口中的表现，不能写成 ROS、规划器或硬件故障鲁棒性。
故障协议实现见：

`src/warehouse_core/warehouse_core/stage21_faults.py`

## 5. 必须遵守的主张边界

后续写作与作图必须遵守以下边界：

- [ ] 不把逻辑 SMDP 仿真称为真实机器人或动力学仿真验证；
- [ ] 不把 transactional cargo ownership 称为真实抓取、吸附或接触交接；
- [ ] 不把 SCAN-Planner 描述成 Carter 原生规划器；SCAN 仅用于 Go2，Carter 应采用轮式导航规划器；
- [ ] 不声称 learned scheduler 已完成 randomized closed-loop SCAN missions，除非新增实验真实执行；
- [ ] 不把 Stage 21 延迟实验写成大规模 scalability；
- [ ] 不把早期 `no_persistent_position` mode collapse 写成稳定复现的消融结论；
- [ ] 不声称 mixed-curriculum 全面超过 specialists；
- [ ] 不只依据点估计使用“显著优于”，必须检查对应置信区间和检验族；
- [ ] 不把 Recovery 等待下降自动解释为比 greedy 恢复更快；当前能证明的是 queue clearance；
- [ ] 不把 45 篇参考文献继续机械扩充作为主要工作，新增文献应服务于新基线、协议或理论定位。

## 6. 可立即完成的论文修改（不需要重新训练）

### P0-1：加入 Stage 18 正式消融表

- [ ] 从 v4 statistics 提取 full context 与五个特征消融的主要指标和 95% CI；
- [ ] 主文保留成功率、回报、吞吐、平均/P95 等待，完整指标放附录或补充材料；
- [ ] 正文只突出 ETA/cost 的稳定独立贡献；其余写成未稳定显著；
- [ ] 把早期 position mode collapse 放在定性诊断或附录，不作为标题级结论。

验收标准：表格数值可追溯至 fresh locked v4；结论与最终统计一致，不混入 checkpoint-selection 结果。

### P0-2：为 Stage 20 增加主图和主表

建议图：mixed PPO、time-greedy、single-dog 和 fixed-load specialists 的 forest/Pareto comparison。

- 面板 A：成功率与吞吐差；
- 面板 B：平均等待与 P95 等待差；
- 面板 C：成功率—P95 等待 Pareto；
- 统一采用共享测试流配对差与 95% CI；
- 视觉上强调 mixed generalist 的覆盖能力，而非所有指标最优。

验收标准：正文不再只靠摘要和文字声明 mixed-curriculum 泛化；图表能直接看出成功率/吞吐 CI 跨 0、
等待改善 CI 不跨 0。

### P0-3：增加实验协议参数表

至少列出：

- MEDIUM 到达间隔 `(55, 75) s`
- DENSE 到达间隔 `(15, 25) s`
- NORMAL 到达间隔 `(80, 100) s`
- BURST 到达间隔 `(1, 3) s`
- RECOVERY 恢复为 NORMAL
- 任务类别比例、优先级和 deadline 分布
- handover duration 分布
- 训练/测试种子数、测试任务数和 freeze rules
- 故障窗口、每段失败概率和附加延迟
- bootstrap 层级与重复次数
- 硬件、CPU 和软件版本

验收标准：评审者无需读代码即可重建主实验协议。

### P0-4：重构实验章节命名

建议将内部 Stage 编号改成论文语言：

- Study I — Nonstationary Load Adaptation
- Study II — Mixed-Regime Generalization
- Study III — Compute and Logical-Fault Stress Tests

仓库和文件名可继续使用 Stage 编号，但论文正文不应依赖项目内部开发编号。

### P0-5：补方法复现细节

- [ ] 用表格解释 284D state 的各组成和维度；
- [ ] 用表格解释 30D candidate feature 的各组成和维度；
- [ ] 列出 encoder、actor、critic 的层数、宽度、激活函数和参数量；
- [ ] 给出 1536 候选上限的明确推导；
- [ ] 增加候选生成、mask、事件推进和 SMDP update 的伪代码；
- [ ] 算法化定义 time-greedy 与 single-dog baseline，避免只用自然语言描述。

## 7. 必须补做的实验

### P0-A：算法级消融

现有 Stage 18 主要是输入特征消融，仍不能充分回答核心设计是否必要。建议在锁定训练/测试协议下补：

1. `fixed_gamma`：固定步折扣，比较 duration-aware discount；
2. `no_context_interaction`：移除 `z_s ⊙ z_c` 或改为 flat candidate scorer；
3. `no_warm_start`：从随机策略训练；
4. `no_relay` 或 fixed mode：禁止 `CAR_DOG_CAR`，量化异构接力本身的贡献。

最低配置建议：每个条件 5–10 个训练种子、与主实验共享至少 50–100 个 locked test seeds；训练预算、模型
宽度和候选集合保持一致。预先锁定 primary metrics：成功率、吞吐、平均等待、P95 等待和训练稳定性。

验收标准：至少能够分别回答“连续时间折扣是否必要”“context–candidate interaction 是否必要”以及
“收益来自调度学习还是仅来自开放 relay 模式”。

### P0-B：增强基线

当前 time-greedy 和 single-dog 不足以覆盖 T-RO 评审可能提出的“是否只比弱规则好”。建议增加：

- FIFO / earliest-deadline-first；
- minimum estimated completion time；
- minimum incremental system cost；
- 小规模 rolling-horizon MILP 或 exact oracle；
- 若实现成本可控，再增加 RTAW、Dai 等相关学习/任务分配方法的可比变体；
- 对优化基线报告 `100 ms / 1 s / 10 s` 计算预算下的解质量和运行时间。

验收标准：在小规模上报告相对 exact/near-exact 的最优性差距，在完整规模上报告质量—计算时间折中。

### P0-C：未见条件零样本泛化

使用冻结 Stage 20 mixed 权重，建议测试：

- 至少 3 个未见仓库布局；
- 2 层与 3 层、不同楼梯拓扑；
- Carter:Go2 比例 `8:2`、`6:4`、`4:6`；
- 不同跨层任务比例、任务优先级、deadline 和速度分布；
- 不同楼梯/交接区容量；
- 每个条件 50–100 个共享 locked seeds。

若当前网络的固定输入维度不支持某些团队规模，应如实把实验限定在可表示范围内，或先实现 padding/mask
适配；不能通过改变输入定义后仍称为原权重零样本。

验收标准：明确区分 in-distribution、interpolation 和 out-of-distribution；同时报告绝对性能、相对基线差和 CI。

### P0-D：闭环 SCAN/导航仿真实验

这是当前面向 T-RO 的最大外部有效性缺口。建议先完成小规模但真实闭环的证据，不必追求海量 episode：

- Go2 使用 SCAN-Planner 或实际可运行的四足规划器；
- Carter 使用轮式导航规划器；
- 高层 scheduler 真正输出 task/mode/robot/stair；
- 至少执行完整的 `CAR_DOG_CAR` 两次交接链；
- 20–30 个 randomized cross-floor tasks；
- 导航实际时长和失败结果反馈给 scheduler，并触发事件更新或重新分配；
- 记录 navigation success、collision、实际 duration、handover duration、replan/reassignment 和 end-to-end task time。

当前社区 SCAN ROS2 port 的静态点云、预设 waypoint 和开环轨迹回放不等于闭环证据。新实验必须让
learned scheduler 真实驱动执行流程，并接收执行结果。

验收标准：至少提供 1 张系统闭环图、1 张任务时间线、1 段完整接力视频/序列帧和 20–30 个任务的定量表。

### P1：故障时间线与恢复分析

在现有 Stage 21 逐事件数据上增加：

- fault start/end；
- 可用机器人/楼梯数；
- queue length、active tasks 和 P95 waiting；
- mode share；
- failure/reassignment 事件；
- fault removal 后 recovery time 或 queue AUC。

验收标准：图能解释故障影响何时发生、策略如何改变分配、故障结束后何时恢复；不仅是一组最终柱状均值。

### P1：deadline 指标

deadline 已进入状态或奖励，但结果章节缺少 deadline 结果。补充：

- on-time completion rate；
- mean/median tardiness；
- deadline-miss rate；
- 按 priority 分层的等待和超期；
- 负载与故障条件下的 deadline degradation。

验收标准：每个 deadline 指标在代码、表格和正文中定义一致，失败任务的分母处理明确。

### P1：真正的规模扩展实验

Stage 21 只证明固定输入形状内的计算延迟。若要声明 scalability，至少补其中一项：

- top-8 truncation 对质量的影响：可见任务数 `8/16/32/64`；
- 随机器人、楼梯和候选数增长的内存、延迟和任务质量；
- 改用 set/graph encoder 后测试超过 10 机器人与 4 楼梯。

验收标准：同时报告计算复杂度和调度质量，避免只画 p99 latency。

### P1：形式化性质与安全不变量

建议补充并在代码测试中对应验证：

- 候选 soundness：mask 后候选满足类型、位置和资源约束；
- task ownership uniqueness：任一时刻任务仅有一个合法所有者；
- stair/handover resource mutual exclusion；
- 事件推进不会重复完成或遗漏任务；
- 候选生成复杂度和 1536 上限推导；
- 对 top-8 截断明确说明不主张全局 candidate completeness。

## 8. 图表修改清单与论文参照

### Fig. 3：主结果 forest plot

- 当前是论文中最强的统计图，可保留点估计和 crossed-bootstrap 95% CI；
- 建议把等待指标改成 `baseline − PPO`，使四个面板均为“向右代表 PPO 更好”；
- 图注必须说明百分点、task/h 和秒的方向。

参照：Fu et al., T-RO 2023 的主结果/优化对比表达；使用统计 CI 时继续保持本论文现有 forest 风格。

### Fig. 4：持续负载与模式变化

- 弱化训练种子浅色散点，加粗 cohort 均值；
- 加清楚的 `NORMAL/DENSE/BURST/RECOVERY` 阶段背景或竖线；
- 不写“PPO recovery faster than greedy”，只写队列在 Recovery 内被清理；
- 增加 phase-wise mean wait、robot/stair utilization 与 mode share 的联合机制分析。

参照：Notomista et al., T-RO 2022 的故障/状态时间线组织方式。

### Fig. 5：延迟图

- 保留 p50/p95/p99 或主文只保留 p99；
- 标题和正文统一使用 fixed-shape latency；
- 清楚标出 top-8 visibility cap 导致的候选数量平台。

参照：Calvo et al., T-RO 2025 的计算时间与规模分析表达。

### Fig. 6：故障等待图

- 将 time-greedy 和 single-dog 拆成不同面板或不同尺度；
- 避免 single-dog 的数百秒等待压缩 PPO-vs-time-greedy 的主要差异；
- 增加成功率或 failure composition 辅助面板；
- 最终配套一张 fault-event timeline。

参照：Notomista et al., T-RO 2022 的故障时间线、系统快照和控制曲线组合。

### 新增 Stage 20 图

- mixed generalist 与 time-greedy、single-dog、fixed specialists 的配对差 forest plot；
- 或 success–P95 waiting Pareto，加一张 paired difference 小图；
- 这是当前必须新增的正文图。

参照：Dai et al., RA-L 2025 的多基线和未见规模泛化组织方式。

### 新增闭环系统图

- 将 Go2/Carter 模型、地图或点云、轨迹、楼梯和两次 handover 节点整合在同一图中；
- 图中区分 high-level scheduler、Carter navigation、Go2 SCAN/legged navigation 和 feedback events；
- 使用矢量框图加真实仿真截图，不用低清模型图撑满框格。

参照：SCAN-Planner 论文的“机器人/环境/轨迹/系统框图”视觉结构。SCAN 为 arXiv 预印本，只作为视觉和
闭环设计参考，不作为 T-RO 已接收标准的唯一依据。

## 9. 同类型论文给出的实验标准

- Fu et al., T-RO 2023：强优化基线、消融和大规模测试；没有硬件，但用理论与优化证据补足。
- Notomista et al., T-RO 2022：Robotarium 实验、故障时间线、系统快照和控制曲线，并有理论支撑。
- Calvo et al., T-RO 2025：消融、exact-vs-heuristic、规模测试、大量 delay trials 和 ROS/Gazebo/PX4 SIL。
- IQ-ASyMTRe, T-RO 2013：soundness/completeness 与实体机器人配置实验结合。
- Dai et al., RA-L 2025：CTAS-D/TACO/SAS/Greedy 多基线、未见规模泛化和 Crazyflie 演示。
- SCAN-Planner 2026：适合作为四足闭环执行和图形呈现参考，但目前是 arXiv 预印本。

对照这些工作，本文最缺的不是参考文献数量，而是“核心组件消融 + 强基线 + 未见条件泛化 + 至少小规模
闭环执行 + 形式化约束说明”的组合证据。

## 10. Zotero 与参考文献清理

当前 45 篇参考文献数量足够。后续新增实验数据本身不需要引用；只有采用新 baseline、协议、模型或统计方法时
才补相应来源。

- [ ] 检查 Notomista 条目：Zotero 中可能仍显示 preprint/2021，BibTeX 已是 T-RO 2022 正式版本；
- [ ] 将 SCAN 条目类型从 journalArticle 改成 arXiv/preprint；
- [ ] 新增基线后再补该算法的原始论文，不引用二手综述代替原始方法；
- [ ] 保持 BibTeX citation key 稳定，文件名自然数编号只用于本地 PDF 整理，不把编号写入 citation key；
- [ ] 最终导出前检查 DOI、页码、卷期、年份和预印本/正式版是否重复。

## 11. 推荐执行顺序

### 第一批：现有数据即可完成

1. [x] 加入 Stage 18 正式消融表；
2. [x] 生成 Stage 20 主图和主表；
3. [x] 加入实验/故障协议参数表；
4. [x] 将正文 Stage 编号改成 Study I/II/III；
5. [ ] 修订 Fig. 3、Fig. 4 和 Fig. 6；
6. [ ] 补 284D/30D 特征表、网络结构和伪代码；
7. [ ] 对全文做一次 claim-to-evidence 检查。

### 第二批：需要重新运行，但不依赖 ROS 闭环

1. [ ] `fixed_gamma` 消融；
2. [ ] `no_context_interaction` 消融；
3. [ ] `no_warm_start` 消融；
4. [ ] `no_relay` / fixed-mode 消融；
5. [ ] 强规则与 rolling-horizon MILP/oracle baseline；
6. [ ] 未见布局、机器人比例和任务分布泛化；
7. [ ] deadline 与 fault timeline 分析。

### 第三批：机器人闭环证据

1. [ ] Carter 轮式导航闭环；
2. [ ] Go2 SCAN/四足导航闭环；
3. [ ] scheduler 驱动完整 `CAR_DOG_CAR`；
4. [ ] 实际 duration/failure feedback；
5. [ ] 20–30 个随机跨层任务；
6. [ ] 视频、时间线、系统图和定量表。

### 第四批：最终投稿整理

1. [ ] 形式化不变量和候选复杂度；
2. [ ] 统一图中文字、线宽、配色和术语；
3. [ ] 将重复的“not hardware evidence”合并到实验边界和 Limitations；
4. [ ] 缩短 Conclusion，避免重复摘要；
5. [ ] 检查摘要不超过 200 词、正文不超过 18 页；
6. [ ] Zotero 元数据和 BibTeX 最终清理；
7. [ ] Overleaf clean compile，并逐页检查浮动体、空白和参考文献错位。

## 12. 下一阶段验收门槛

在准备正式投稿前，建议至少满足以下条件：

- [x] Stage 18、19、20、21 均有正文表或图，不只在文字中出现；
- [ ] 至少 3 个核心算法组件有多种子消融；
- [ ] 至少 3 个规则/优化基线，其中一个小规模 exact 或 near-exact；
- [ ] 至少 3 个未见布局，并覆盖不同机器人比例或楼层拓扑；
- [ ] 至少 20–30 个真实闭环仿真跨层任务；
- [ ] 论文明确区分逻辑仿真、运动学/规划闭环和真实硬件证据；
- [ ] 所有主结论均能对应具体图表、数据文件和 95% CI；
- [ ] 所有图表可由仓库脚本从锁定数据一键重建；
- [x] clean compile 无 error，无 overfull、未定义引用或未定义文献；
- [x] 当前主稿 13 页，不超过 T-RO 首投 18 页上限。

## 13. 关键文件索引

- 论文：`examples/manuscript/main.tex`
- 论文说明：`examples/manuscript/README.md`
- Stage 18 正式消融：`results/stage18_clean_ablation_locked_test_v4/stage18_clean_ablation_locked_test_v4_statistics.json`
- Stage 20 数据：`examples/manuscript/data/stage20/`
- Stage 21 故障协议：`src/warehouse_core/warehouse_core/stage21_faults.py`
- Stage 21 图验证：`examples/manuscript/data/stage21/stage21_figure_validation.json`
- Stage 21 延迟图：`examples/manuscript/figures/fig_stage21_latency_scaling.pdf`
- Stage 21 故障图：`examples/manuscript/figures/fig_stage21_fault_waiting_effects.pdf`
- Stage 21 作图脚本：`examples/manuscript/scripts/make_stage21_figures.py`
- 上一次交接：`0823handover.md`

## 14. 防止后续误用

1. 开始任何新实验前先冻结协议、primary metrics、种子范围和 baseline 版本；
2. 所有比较尽量共享 task streams 和 task-keyed randomness；
3. 保留 train seed × test seed 两个随机层级，避免只对 episode 行做普通 bootstrap；
4. 不删除失败或未完成任务；必须显式记入成功率分母并报告失败类型；
5. 不从多个 checkpoint 中根据测试集挑最好结果；
6. 不用单一回报指标替代成功率、吞吐、等待、距离和安全指标；
7. 不继续机械增加 PPO 更新次数，除非新消融或基线的训练预算需要公平对齐；
8. 修改 Overleaf 前先核对本地 `main.tex` 与 Overleaf 版本，修改后重新下载或同步，防止覆盖；
9. 当前未提交文件属于在制成果，任何清理、reset、覆盖或重新生成前必须先比较内容；
10. 论文中的每个强主张都应能回答：对应哪组 locked data、哪张图、哪种 CI、证据边界是什么。

## 15. 本次论文专用会话完成情况（2026-08-26）

本会话只处理论文及其可复现图表，未启动、终止或修改另一终端正在运行的下一阶段实验。

### 15.1 已完成

1. [x] 在方法部分加入 284 维全局状态和 30 维候选特征的逐组维度表，并核对总维度；
2. [x] 将贡献表述改为“三项方法贡献 + 一项实证贡献”，避免把实验本身包装为算法贡献；
3. [x] 重组实验协议为 Feature Ablation、Study I、Study II 和 Study III，并补充负载、任务构成、优先级/截止期、handover 校准、故障窗口、种子与 bootstrap 边界；
4. [x] 加入 Stage 18 fresh locked feature-ablation 正式表，正文明确：ETA/cost 是最稳定、幅度最大的效应，但多重比较校正后不宣称 confirmatory significance；
5. [x] 加入 Study II mixed-curriculum 绝对结果表与 2×2 crossed-bootstrap 主图，所有面板统一为“正值有利于 mixed PPO”；
6. [x] 保留 single-dog 在绝对表中，但从 Study II 主图中省略，避免其极大等待差异压缩 specialist 比较；
7. [x] 修正两张宽表越界，并设置 IEEE 双栏浮动体顶端对齐；删除导致大块空白页的强制 `clearpage`；
8. [x] 对参考文献末页做双栏平衡；
9. [x] 使用 Tectonic 完成 clean compile 并逐页渲染检查。当前 `main.pdf` 为 13 页，无编译错误、overfull、未定义引用或未定义文献；
10. [x] 更新 `examples/manuscript/README.md`，记录 Stage 18/20 复现入口和数据边界。

### 15.2 新增或更新文件

- 主稿：`examples/manuscript/main.tex`
- 编译稿：`examples/manuscript/main.pdf`
- Study II 图：`examples/manuscript/figures/fig_stage20_generalization.pdf`
- Study II 作图脚本：`examples/manuscript/scripts/make_stage20_figure.py`
- Study II 图表数值：`examples/manuscript/figures/data/stage20_generalization_differences.csv`
- Study II 图验证记录：`examples/manuscript/data/stage20/stage20_figure_validation.json`
- Overleaf 备份包：`examples/manuscript/TRO_Overleaf_Draft_0826.zip`

### 15.3 等待另一终端新实验结果后再做

1. 将新消融和新基线按冻结协议补入表格，优先补 `fixed_gamma`、`no_context_interaction`、`no_warm_start` 和 `no_relay`；
2. 新结果不得仅报告最好 checkpoint，应继续保留 train-seed × test-seed 两层 crossed bootstrap；
3. 若新增强规则、MILP/oracle 或未见布局泛化，优先用 paired effect plot，不用无不确定性的柱状图；
4. 根据新实验结果更新 Abstract、Introduction contribution、Results 和 Conclusion 中的数值，避免只改 Results；
5. 闭环 SCAN/Gazebo 证据完成前，继续保留 scheduling-only、logical-fault-only 与 no-hardware-evidence 的边界表述；
6. Overleaf 同步后必须重新 clean compile，并重点核对 Fig. 3–7、Table IV–VII、Limitations、Conclusion 和参考文献末页。
